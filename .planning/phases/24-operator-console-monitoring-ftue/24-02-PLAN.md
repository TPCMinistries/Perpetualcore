# Phase 24-02 Plan — First-Run FTUE Path

## Requirements

- FTUE-01: New org reaches first scored opportunities in one session via ≤5-field setup.
- FTUE-02: Guided checklist moves org → voice → vault → first draft → review.
- FTUE-03: Discovery / Proposals / Vault / Voice empty states have a real CTA toward the first qualified draft.

## Scope

Improve the first-run path without adding schema. Use the existing `rfp_orgs.capacity_summary`, voice fingerprint, vault artifacts, proposal, reviewer, compliance, and submission-task signals.

## Tasks

1. Replace NAICS-required org creation with a five-field setup:
   - organization name
   - organization type
   - mission
   - geography
   - funding types
2. Persist the first-run setup into `capacity_summary`.
3. Keep initial scoring automatic after org creation.
4. Add `profile_complete` to derived onboarding state.
5. Rework the Discovery checklist into the org profile → voice → vault → match/draft → review/export path.
6. Make step CTAs visible and keyboard-accessible.
7. Tighten empty states for Discovery, Vault, and Voice; preserve the existing Proposals activation branch.
8. Remove stale Vault copy that contradicted current PDF/DOCX and drafter retrieval support.

## Verification

- Focused ESLint on touched FTUE files.
- Runtime import checks for touched client/server modules.
- Local dev server smoke:
  - `/orgs/new` unauthenticated redirects to login without browser console/page errors.

## Deferred

- Authenticated visual pass through a real seeded org.
- Full end-to-end test for create org → score → first draft.
