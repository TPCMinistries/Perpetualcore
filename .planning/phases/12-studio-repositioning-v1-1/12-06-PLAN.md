---
phase: 12-studio-repositioning-v1-1
plan: 06
type: execute
wave: 3
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-05
files_modified:
  - app/solutions/healthcare/page.tsx
  - app/solutions/non-profits/page.tsx
  - app/solutions/education/page.tsx
  - app/solutions/churches/page.tsx
  - app/solutions/law-firms/page.tsx
  - app/solutions/accountants/page.tsx
  - app/solutions/consulting/page.tsx
  - app/solutions/financial-advisors/page.tsx
  - app/solutions/real-estate/page.tsx
  - app/solutions/it-services/page.tsx
  - app/solutions/sales/page.tsx
  - app/solutions/agencies/page.tsx
  - app/features/intelligence/page.tsx
  - app/agents/page.tsx
  - app/enterprise-demo/page.tsx
  - app/consulting/page.tsx
  - .planning/repositioning/AGENT_LOG.md
  - .planning/phases/12-studio-repositioning-v1-1/12-06-MOBILE-QA.md
autonomous: true
requirements:
  - STUDIO-PL-01

# Plan-level note on autonomous flag:
# Tasks 1 + 2 are type="auto" (Navbar consolidation + year audit) — fully autonomous.
# Tasks 3 (mobile QA) and 4 (logo decision) are checkpoints (human-verify and decision)
# that block execution regardless of the frontmatter `autonomous` value. The execute-plan
# orchestrator pauses on any checkpoint regardless of frontmatter; setting `autonomous: true`
# accurately describes the autonomous portion of the plan and is consistent with Tasks 1+2
# being type="auto". The orchestrator will still pause at Tasks 3 and 4 for human signal.

must_haves:
  truths:
    - "grep -r '<header' app/ components/ --include='*.tsx' returns no results from per-page bespoke header implementations except documented exceptions"
    - "Every page that previously shipped a bespoke header now imports and renders <Navbar/> from components/landing/Navbar"
    - "grep -rn '© 2024' app/ components/ --include='*.tsx' returns zero matches"
    - "Mobile QA report covers 375 / 768 / 1024 viewports for all 17 repositioning pages with no horizontal scroll and no overflow"
    - "Logo decision is documented in .planning/repositioning/AGENT_LOG.md — either real SVG wired or 'PC' gradient placeholder explicitly accepted for v1"
  artifacts:
    - path: "app/solutions/healthcare/page.tsx"
      provides: "Bespoke <header> replaced with <Navbar/>"
      contains: "Navbar"
    - path: ".planning/phases/12-studio-repositioning-v1-1/12-06-MOBILE-QA.md"
      provides: "Mobile QA report with screenshots and pass/fail per viewport per page"
      min_lines: 80
    - path: ".planning/repositioning/AGENT_LOG.md"
      provides: "Logo decision section appended"
      contains: "Logo decision"
  key_links:
    - from: "app/solutions/*/page.tsx"
      to: "components/landing/Navbar.tsx"
      via: "import { Navbar } from '@/components/landing/Navbar'; render at top of page"
      pattern: "import.*Navbar.*landing/Navbar"
    - from: "All retired-year footer copy"
      to: "© 2026 Perpetual Core"
      via: "Bulk replacement — already done in Session 3 commit 0fa9d34, but verify no stragglers"
      pattern: "© 2024"
---

<objective>
Close all MERGE_PLAN.md "deferred to v1.1" items. This is the final polish plan for the repositioning sprint — depends on Plans 01–05 having landed so all new pages exist and the file inventory is stable. Four sub-tasks: (a) navbar consolidation across the 12 /solutions/* pages and any other bespoke-header pages flagged in Session reports, (b) verify zero © 2024 matches site-wide (Session 3 commit `0fa9d34` already did the bulk bump — confirm no stragglers in the new Phase 12 work), (c) run the mobile QA pass and produce a report artifact, (d) record the logo decision in AGENT_LOG.md.

This plan is mostly tedious-but-mechanical work plus two human checkpoints (mobile QA + logo decision). Tasks 1+2 are autonomous (`type="auto"`); Tasks 3+4 are checkpoints that pause execution regardless of the frontmatter `autonomous: true` value (the execute-plan orchestrator pauses at any checkpoint).

Purpose: STUDIO-PL-01. The repositioning sprint shipped a canonical `<Navbar/>` and `<Footer/>` (Session 1 commits `4398839` + `605141a`) but Session reports explicitly deferred replacing the bespoke headers in `/solutions/*` and a few other pages. The merge plan blocks on this work.

Output: Every public-facing page uses the canonical Navbar + Footer; mobile QA report with screenshots; logo decision documented; AGENT_LOG.md updated with closure notes.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/repositioning/MERGE_PLAN.md
@.planning/repositioning/SESSION_1_REPORT.md
@.planning/repositioning/SESSION_2_REPORT.md
@.planning/repositioning/SESSION_3_REPORT.md
@.planning/repositioning/UI_AUDIT.md
@.planning/repositioning/AGENT_LOG.md
@components/landing/Navbar.tsx
@components/landing/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Navbar across all /solutions/* and other bespoke-header pages</name>
  <files>
    app/solutions/healthcare/page.tsx
    app/solutions/non-profits/page.tsx
    app/solutions/education/page.tsx
    app/solutions/churches/page.tsx
    app/solutions/law-firms/page.tsx
    app/solutions/accountants/page.tsx
    app/solutions/consulting/page.tsx
    app/solutions/financial-advisors/page.tsx
    app/solutions/real-estate/page.tsx
    app/solutions/it-services/page.tsx
    app/solutions/sales/page.tsx
    app/solutions/agencies/page.tsx
    app/features/intelligence/page.tsx
    app/agents/page.tsx
    app/enterprise-demo/page.tsx
    app/consulting/page.tsx
  </files>
  <action>
**Discovery first.** Run a precise grep to find every page with a bespoke header. The grep MUST exclude RFP-marketing territory (per phase coordination constraint — RFP files are owned by a different sprint and must not be touched here):

```bash
grep -rln '<header' app/ --include='*.tsx' \
  | grep -v 'components/landing/Navbar.tsx' \
  | grep -v 'rfp-marketing\|lib/rfp\|components/rfp\|api/cron' \
  | sort
```

This produces the canonical list. Compare against the file list in this plan's frontmatter — add any pages the grep finds that aren't listed (this catches drift), and skip any pages that the grep proves no longer have bespoke headers.

**RFP exclusion rule (non-negotiable):** If the unfiltered grep returns ANY path containing `rfp-marketing`, `lib/rfp`, `components/rfp`, or `api/cron`, those paths MUST be skipped. Document each skipped path in the SUMMARY under "Coordination-protected exclusions." The RFP territory belongs to a separate sprint per phase coordination constraint; modifying those files in this plan would create a merge conflict with the RFP sprint and is explicitly out of scope.

**Scope cap:** If the filtered discovery grep surfaces **more than 20 files**, do NOT expand 12-06 scope mid-execution. Instead:
1. Stop the navbar consolidation work in 12-06.
2. Process the first 20 files (or whatever fits within the original frontmatter `files_modified` list — never add new files beyond what's already declared).
3. Create overflow plan 12-07 documenting the remaining files. Use the same task pattern as Task 1 here. Plan 12-07 inherits the same RFP-exclusion rule.
4. Note the overflow split in the SUMMARY and update ROADMAP.md to add plan 12-07.

This guardrail prevents 12-06 from ballooning past its context budget if the codebase has more bespoke-header drift than the Session reports indicated.

**Per-page edit pattern:**

For each file in the (filtered, scope-capped) list:

1. Read the file. Identify the bespoke `<header>...</header>` block (or the inline navigation markup if it's not wrapped in `<header>`).
2. Replace the entire bespoke header with:
   ```tsx
   import { Navbar } from "@/components/landing/Navbar";
   import { Footer } from "@/components/landing/Footer";
   ```
   At the top of imports, then:
   ```tsx
   <Navbar />
   ```
   Where the bespoke header used to be.
3. If the page also has a bespoke footer, replace it with `<Footer />` from the same import.
4. Preserve all other page content. The bespoke header may have included an EngagementBanner (Session 3 added these to /solutions/*) — check whether the canonical Navbar replaces it cleanly OR whether the EngagementBanner is a separate sibling element. Do NOT remove the EngagementBanner — it ships from Session 3 and is the studio-engagement-CTA-on-solutions-pages link.
5. After the swap, verify the page still compiles and renders. Specifically:
   - Imports list is clean (no unused imports left over from the bespoke header).
   - Tailwind classes used by the canonical Navbar are present in the project's Tailwind config (they should be — Navbar already ships).
   - No duplicate Navbar renders if the file accidentally had Navbar + bespoke header coexisting.

**For `app/consulting/page.tsx`:** Per Session 3 deferral, this file is dead code — `/consulting` 301-redirects to `/studio/engagements` via next.config.mjs. Two options:
- **Option A (preferred):** Delete the file entirely. The 301 redirect handles the URL.
- **Option B:** Keep the file but replace its body with a server-component redirect: `import { redirect } from 'next/navigation'; export default function ConsultingPage() { redirect('/studio/engagements'); }`. Belt-and-suspenders over the next.config.mjs redirect.

Pick Option A unless lint/build errors surface. Document the choice in the per-file diff summary.

**For `app/enterprise-demo/page.tsx`:** UI_AUDIT §6 flagged this page for using raw slate colors. The navbar consolidation fix is independent of the slate-color refactor — do NOT refactor the slate-to-token-system swap in this plan (that's a v1.2 polish item). Just swap the header.

**Hard rules:**
- Do NOT touch any RFP-related file (`app/(rfp-marketing)/*`, `lib/rfp/*`, `components/rfp/*`, `app/api/cron/*`) per phase coordination constraint. The discovery grep excludes them; if any RFP path surfaces, document under "Coordination-protected exclusions."
- Do NOT modify the canonical Navbar or Footer themselves — they're already correct.
- Do NOT remove EngagementBanner imports/renders that Session 3 added to /solutions/*.
- Atomic commits per file are NOT required — a single commit titled `feat(12-06): consolidate Navbar across solutions and feature pages` is fine.
- Scope cap is non-negotiable: > 20 files in the discovery → split into 12-07 overflow plan, do not expand 12-06 mid-execution.
  </action>
  <verify>
1. **Filtered discovery grep returns zero results:**
   ```bash
   grep -rln '<header' app/ --include='*.tsx' \
     | grep -v 'components/landing/Navbar.tsx' \
     | grep -v 'rfp-marketing\|lib/rfp\|components/rfp\|api/cron'
   ```
   Returns empty (or returns only documented exceptions like dashboard/layout headers — capture exceptions in the summary).
2. For each file in the frontmatter list:
   - `grep -c "import.*Navbar.*landing/Navbar" {file}` returns 1.
   - `grep -c "<Navbar" {file}` returns 1.
3. `npm run lint -- {each file}` returns no new errors.
4. `npm run dev` → spot-check 3 of the changed pages (e.g., /solutions/healthcare, /solutions/non-profits, /enterprise-demo) → canonical navbar renders, dropdowns work, primary CTA is "Start Engagement."
5. EngagementBanner still renders on /solutions/* pages (verify via grep `grep -c "EngagementBanner" app/solutions/*/page.tsx | head -3`).
6. **Coordination-protected exclusions documented:** If the unfiltered grep surfaces any RFP paths, those paths appear in the SUMMARY under "Coordination-protected exclusions" with explicit reason.
7. **Scope cap respected:** If the filtered grep surfaced >20 files, plan 12-07 was created and ROADMAP.md updated. Otherwise, the work fit cleanly within 12-06's declared file list.
  </verify>
  <done>
All bespoke headers (excluding RFP territory) replaced with `<Navbar/>` (and bespoke footers with `<Footer/>` where applicable). EngagementBanner preserved on /solutions/*. RFP territory paths skipped and documented as coordination-protected exclusions. No regression in navbar dropdowns or sticky behavior. If > 20 files were discovered, overflow plan 12-07 was created without expanding 12-06's scope.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify zero © 2024 matches and capture grep evidence</name>
  <files>
    .planning/phases/12-studio-repositioning-v1-1/12-06-YEAR-AUDIT.md
  </files>
  <action>
Session 3 commit `0fa9d34` already did the bulk bump (per AGENT_LOG.md). This task is to VERIFY zero stragglers and capture the evidence.

Run the canonical grep:
```bash
grep -rn "© 2024" app/ components/ --include='*.tsx' --include='*.ts' --include='*.mdx'
```

If the output is empty, the requirement is satisfied. If any matches surface (likely from Phase 12 plans 01–05's new pages — though they shouldn't), fix them by replacing `© 2024` with `© 2026`.

Also grep for the alternative formats:
```bash
grep -rn "Copyright 2024\|copyright 2024" app/ components/ --include='*.tsx' --include='*.ts'
grep -rn "©2024" app/ components/ --include='*.tsx'  # no space
```

Both should also return zero matches.

Create `.planning/phases/12-studio-repositioning-v1-1/12-06-YEAR-AUDIT.md` with:

```markdown
# Year audit (STUDIO-PL-01b)

Run date: {date}

## Grep results

### `© 2024` (canonical):
```
grep -rn "© 2024" app/ components/ --include='*.tsx' --include='*.ts' --include='*.mdx'
```
Output: {paste output}
Result: {0 matches expected}

### `Copyright 2024`:
```
{paste output}
```

### `©2024` (no space):
```
{paste output}
```

## Conclusion

{All clean / Fixed N stragglers in {files}}
```

If any stragglers were fixed, list them in the conclusion.
  </action>
  <verify>
1. `test -f .planning/phases/12-studio-repositioning-v1-1/12-06-YEAR-AUDIT.md` returns 0.
2. `grep -c "© 2024" app/ components/ --include='*.tsx' --include='*.ts' --include='*.mdx' -r` returns 0 (truly zero matches).
3. The artifact contains all three grep outputs.
  </verify>
  <done>
Site-wide grep confirms zero `© 2024` matches. Audit artifact records the evidence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile QA pass — Lorenzo verifies all 17 repositioning pages at 375 / 768 / 1024</name>
  <action>Human-verify checkpoint. See <what-built> for context and <how-to-verify> for the exact verification steps the human runs against the dev server. Resume only after the resume-signal phrase.</action>
  <what-built>
The full Phase 12 + base repositioning sprint is now landed:
- Plans 12-01 to 12-05: case studies populated, Atlas Discovery shipped, IHA links wired, Vellum waitlist live
- Plan 12-06 Task 1: bespoke headers replaced with canonical Navbar
- Plan 12-06 Task 2: © 2024 stragglers eliminated

This task is the visual QA pass. Per STUDIO-PL-01 success criterion: mobile responsiveness verified at 375 / 768 / 1024 on every new repositioning page; at least one screenshot per viewport per page; no horizontal scroll; no overflow on hero/grid/timeline components.
  </what-built>
  <how-to-verify>
Run `npm run dev`. Open Chrome DevTools (or Firefox/Safari Responsive Mode). For each of the 17 pages below, switch through three device-frame widths: 375 (iPhone SE), 768 (iPad portrait), 1024 (iPad landscape).

**Pages to verify:**
1. `/`
2. `/studio`
3. `/studio/engagements`
4. `/studio/methodology`
5. `/studio/process`
6. `/studio/case-studies` (Plan 12-01 — three cards with INSTALL/OUTCOME prose)
7. `/engine`
8. `/about`
9. `/pricing`
10. `/products`
11. `/products/platform`
12. `/products/atlas`
13. `/products/atlas-discovery` (Plan 12-02 — new)
14. `/products/sentinel`
15. `/products/sage`
16. `/products/vellum` (Plan 12-05 — has new EarlyAccessForm; Stripe Elements step is part of QA)
17. `/products/rfp-sentry`

**Per page, per viewport, check:**
- No horizontal scroll (drag horizontally — content should not extend past viewport).
- Hero text is readable (not cut off, not overflowing).
- Grids stack cleanly (4-col → 2-col → 1-col across breakpoints).
- Buttons / CTAs are tap-target friendly (≥44px tall on mobile).
- Forms (Atlas / Atlas Discovery / Vellum / RFP Sentry) — inputs full-width, labels readable, submit buttons tappable.
- Timeline/alternating components (e.g., /studio/process, /studio/methodology) collapse to vertical stack on mobile.
- 8-node SVG diagram on /engine — does NOT overflow on 375px (this was the specific concern in MERGE_PLAN.md).
- Vellum pricing grid (4 tiers) — stacks cleanly without orphans on every viewport.
- Vellum EarlyAccessForm — radio cards stack cleanly; Stripe Elements payment step is mobile-friendly.

**Capture screenshots.** Take at least ONE screenshot per page per viewport (51 screenshots total). Save to `.planning/phases/12-studio-repositioning-v1-1/screenshots/{page-slug}-{viewport}.png` (or any local archive — the screenshot file paths get referenced in the report). For high-volume QA, a single screenshot of the worst-case viewport (usually 375) per page is acceptable if the others pass without notes.

**Produce the report:**

Write `.planning/phases/12-studio-repositioning-v1-1/12-06-MOBILE-QA.md`:

```markdown
# Mobile QA Report (STUDIO-PL-01c)

Run date: {date}
QA browser: {Chrome 130 DevTools / Safari iOS 18 / etc.}
Total pages: 17
Total viewports: 51 (17 × 3)

## Pass/fail matrix

| Page | 375 | 768 | 1024 | Notes |
|---|---|---|---|---|
| / | ✓ | ✓ | ✓ | |
| /studio | ✓ | ✓ | ✓ | |
| ... | | | | |

## Issues found

{For each fail or visual concern, list:}
- **Page**: /products/vellum
- **Viewport**: 375
- **Issue**: Stripe PaymentElement extends 8px past container width
- **Fix**: {applied in this plan / deferred to v1.2 / blocked-on-Stripe}

## Screenshots

Saved to .planning/phases/12-studio-repositioning-v1-1/screenshots/
{or "Captured locally; not committed to repo per .gitignore"}

## Conclusion

{All pass / N issues fixed in this plan / M deferred}
```

If any visual issues are blocking-severity (horizontal scroll, hidden CTAs, illegible text), apply minimal fixes inline (Tailwind class adjustments only — no structural changes). Document each fix in the report. Anything non-blocking gets deferred to v1.2 with explicit acknowledgment.
  </how-to-verify>
  <resume-signal>
Type "mobile-qa-pass" if all 17 pages clear at all three viewports (or only minor non-blocking issues remain, which are documented). Type "mobile-qa-fix-needed" with the issue list if any blocking issues require this plan to apply fixes.
  </resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Logo decision — wire real SVG OR explicitly accept "PC" gradient placeholder for v1</name>
  <action>Decision checkpoint. See <decision>/<context>/<options> for the decision being made. Resume only after the user responds with the resume-signal phrase.</action>
  <decision>
The canonical Navbar and Footer currently render a "PC" violet-gradient placeholder where the logo should be. Per STUDIO-PL-01(d), the logo decision must be documented before merge.

**Two options:**
  </decision>
  <context>
The placeholder is in `components/landing/Navbar.tsx` and `components/landing/Footer.tsx` (and possibly `components/layout/PublicMobileNav.tsx`). Per Session 1 report, it's marked TODO. UI_AUDIT.md §1 calls it out: "a logo on a studio site that says 'AI' in a square is, frankly, embarrassing for the positioning" — though Session 1 changed it to "PC" gradient, the studio-frame concern stands.

The studio repositioning is otherwise ship-ready. The logo placeholder is the most visible "this is a draft" tell on the site — and a foundation program officer reviewing perpetualcore.com will notice it.
  </context>
  <options>
    <option id="real-logo">
      <name>Wire a real SVG logo now</name>
      <pros>
- Eliminates the most-visible placeholder on the site.
- The studio frame lands in production looking finished.
- One commit, one file change (the SVG asset + Navbar/Footer/PublicMobileNav imports).
      </pros>
      <cons>
- Requires Lorenzo to provide a finished SVG (designed externally or generated).
- If the SVG isn't ready, this option blocks the entire phase merge.
- Real logos rarely land cleanly on first try — risk of having to iterate post-launch.
      </cons>
    </option>
    <option id="placeholder-accepted">
      <name>Explicitly accept "PC" gradient placeholder for v1, replace in v1.2</name>
      <pros>
- Phase ships immediately. No additional Lorenzo dependency.
- The "PC" gradient placeholder is at least on-brand (violet-gradient consistent with primary palette).
- Logo design done thoughtfully in v1.2 with proper visual identity work.
      </pros>
      <cons>
- "PC" placeholder is visible on every public page in production.
- Brand-conscious buyers will note it.
- Risk: v1.2 logo replacement gets indefinitely deferred.
      </cons>
    </option>
  </options>
  <resume-signal>
Type "logo-real" if you have an SVG ready (provide path or paste the SVG content), OR "logo-placeholder-v1.2" to formally accept the placeholder for v1 with explicit v1.2 replacement commitment.

Then update `.planning/repositioning/AGENT_LOG.md` with a new section:

```markdown
## Logo decision (2026-05-XX, Lorenzo)

Decision: {real-logo / placeholder-accepted-v1.2}
Rationale: {why}
{If real-logo:} SVG path: {path}
{If placeholder-accepted-v1.2:} v1.2 replacement target date: {date}
```

After the AGENT_LOG.md update, this checkpoint resolves and the phase is mergeable.
  </resume-signal>
</task>

</tasks>

<verification>
Phase-level closure check for STUDIO-PL-01:

1. `grep -rln '<header' app/ --include='*.tsx' | grep -v 'components/landing/Navbar.tsx' | grep -v 'rfp-marketing\|lib/rfp\|components/rfp\|api/cron'` returns zero (or only documented non-RFP exceptions).
2. `grep -rn '© 2024' app/ components/ --include='*.tsx' --include='*.ts' --include='*.mdx'` returns zero.
3. Mobile QA report covers all 17 pages at 375/768/1024 with pass status documented.
4. Logo decision recorded in AGENT_LOG.md with explicit v1 disposition.
5. Two checkpoints resolved (mobile QA + logo decision).
6. RFP territory exclusions documented (if any surfaced) under "Coordination-protected exclusions" in SUMMARY.
7. If discovery surfaced > 20 files, plan 12-07 was created without expanding 12-06's scope.
</verification>

<success_criteria>
- [ ] Every public page (excluding RFP territory) uses the canonical `<Navbar/>` (no bespoke headers remain)
- [ ] Year audit confirms zero `© 2024` matches
- [ ] Mobile QA report exists with all 17 pages × 3 viewports documented
- [ ] Logo decision documented in AGENT_LOG.md
- [ ] No blocking visual issues remain (or all are explicitly deferred to v1.2)
- [ ] Phase 12 is mergeable from `feat/studio-repositioning` (with Phase 12 commits) to main
- [ ] Lint clean on all modified files
- [ ] RFP-territory exclusions documented; scope cap respected (overflow → 12-07 if needed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-studio-repositioning-v1-1/12-06-SUMMARY.md` documenting:
- File-by-file diff summary for navbar consolidation (which files swapped, which were dead code, which had EngagementBanner preserved)
- **Coordination-protected exclusions:** any RFP-territory paths surfaced by the unfiltered grep that were skipped per the RFP exclusion rule
- **Scope cap disposition:** did the filtered discovery surface ≤ 20 files (work fit in 12-06) or > 20 files (overflow plan 12-07 created)?
- Year audit result (clean / fixed N stragglers)
- Mobile QA outcome (all pass / N issues fixed inline / M deferred)
- Logo decision (real SVG path / placeholder accepted with v1.2 commitment)
- Final pre-merge checklist for Lorenzo:
  - [ ] All Phase 12 commits land on the working branch
  - [ ] Counsel review of compliance hedging (carried over from base sprint)
  - [ ] Push branch and squash-merge or rebase per MERGE_PLAN.md
  - [ ] Deploy to Vercel preview, smoke-test, promote to production
- Recommendation for v3.0 milestone close-out (next phase to plan / what to add to roadmap)
</output>
