---
phase: 12-studio-repositioning-v1-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/studio/case-studies/page.tsx
autonomous: true
requirements:
  - STUDIO-CS-01

must_haves:
  truths:
    - "Visitor on /studio/case-studies sees three real abstracted case study cards (not 'available under NDA' stubs)"
    - "Each card uses SECTOR / CONSTRAINT / INSTALL / OUTCOME structure with ~100 words of voice-driven prose"
    - "No client names appear anywhere (CUNY abstracted, Nairobi parishes abstracted, faith institutions abstracted)"
    - "No fabricated quantitative metrics — qualitative outcomes only where quantitative aren't permission-cleared"
    - "Slot 1 covers East Africa health workforce under PEPFAR/IRB/offline-first constraints"
    - "Slot 2 covers a community-college workforce program in New York"
    - "Slot 3 covers a faith institution multi-state network"
  artifacts:
    - path: "app/studio/case-studies/page.tsx"
      provides: "Three populated case study cards with INSTALL + OUTCOME sections (replacing NDA placeholder)"
      min_lines: 220
      contains: "INSTALL"
  key_links:
    - from: "app/studio/case-studies/page.tsx"
      to: "SLOTS data structure"
      via: "Slot objects each carry sector + constraint + install + outcome fields"
      pattern: "install:.*outcome:"
    - from: "app/studio/case-studies/page.tsx"
      to: "no client name"
      via: "Static review — grep returns no 'CUNY', 'Kingsborough', 'KBCC', 'Nairobi', specific parish names, specific UN agency names"
      pattern: "(?i)(cuny|kbcc|kingsborough|nairobi|tpc|streams of grace)"
---

<objective>
Replace the three "Case study available under NDA" placeholder slots on /studio/case-studies with real abstracted case study prose. Each card carries the full SECTOR / CONSTRAINT / INSTALL / OUTCOME structure (~100 words each), abstracting all client/partner specifics, with qualitative outcomes only where quantitative metrics aren't permission-cleared.

Purpose: STUDIO-CS-01. The current page renders the stub copy "Case study available under NDA. Ask in your intake call." for all three slots — that's a launch blocker. Mission-driven buyers expect proof-by-constraint, not a logo wall, but they also expect to see what we actually built. The abstracted INSTALL/OUTCOME sections demonstrate operator-grade work without naming clients.

Output: A populated `/studio/case-studies` page with three real case study cards. The "constraints are the credential" thesis from BRAND_ARCHITECTURE §6 angle #2 lands visibly.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/repositioning/BRAND_ARCHITECTURE.md
@.planning/repositioning/COPY_STUDIO.md
@.planning/repositioning/BRIEF_RECONCILED.md
@app/studio/case-studies/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write three abstracted case studies into the SLOTS data structure</name>
  <files>app/studio/case-studies/page.tsx</files>
  <action>
Edit `app/studio/case-studies/page.tsx`. The current SLOTS array has three objects with only `sector` and `constraint` fields. Extend each object with two new fields: `install` (string, ~50 words) and `outcome` (string, ~50 words). Each card's total prose body should land near 100 words combined for INSTALL + OUTCOME.

**Slot 1 — East Africa health workforce.** Already uses sector="A UN-aligned humanitarian agency operating across East Africa." and constraint="Under PEPFAR data-sovereignty rules, IRB review, and zero-cellular-fallback assumptions." Keep both. Add:

  - install: Describe what the eight registries solved here. Use this voice: "Eight registries scoped to field-officer intake, beneficiary case management, and IRB-compliant consent workflows. Skills built for offline-first data capture syncing on cellular reconnect, PII redaction at the edge, and PEPFAR-compliant export pipelines. Three departments — clinical, M&E, and field ops — running on the same registry layer by week 12."

  - outcome: Qualitative only — DO NOT invent numbers. Use voice from BRAND_ARCHITECTURE §6 angle #2 — constraints as credential. Example shape: "Field officers stopped re-entering patient data across four legacy systems. M&E reports that previously took two weeks now compile from the registry on demand. The system survived a one-week regional connectivity outage during a public-health emergency without data loss — the offline-first install was the install."

**Slot 2 — community-college workforce program.** sector="A community-college workforce program in New York." constraint="Under FERPA, multi-agency reporting requirements, and outcomes-based funding tied to job placement." Keep both. Add:

  - install: "Eight registries installed across student intake, case management, employer partnerships, and outcomes reporting. Skills built for FERPA-compliant case notes, employer placement tracking with consent-bounded data sharing, and the multi-agency reporting templates the program's funder required quarterly. The case-management workflow that used to live across a CRM, a spreadsheet, and a Google Form now lives in one place."

  - outcome: "Case managers stopped maintaining duplicate records across three systems. Quarterly funder reports compile in hours, not days. The employer-placement registry surfaces students whose certificates align with current open roles — a workflow the program previously ran by hand at term end. The skills library outlived the engagement; new program coordinators inherit it as the operating model."

**Slot 3 — faith institution multi-state network.** sector="A faith institution with a multi-state network." constraint="Under multi-jurisdictional consent regimes, donor-data sensitivity, and offline-first field deployment." Keep both. Add:

  - install: "Eight registries installed across membership, ministry programs, and donor relations. Skills built for multi-jurisdiction consent capture (different states, different rules), offline-first event check-in for satellite locations, and a knowledge registry that synthesizes leadership voice notes, sermons, and field reports into one queryable archive. The ministry's institutional memory — previously held by three long-tenured staff — became extractable infrastructure."

  - outcome: "The network's central staff stopped fielding 'where is that document' requests throughout the day. Satellite locations register attendance and capture consent in offline mode, syncing on reconnect. The institutional-memory layer (the same Knowledge registry Vellum operates on — see /products/vellum) lets new ministers ramp into a multi-decade context without three months of one-on-ones."

**Render changes:** The current card markup has SECTOR + CONSTRAINT labeled blocks and a final NDA-fallback block reading "Case study available under NDA. Ask in your intake call." Replace that final block with two new labeled sections: INSTALL (label uppercase tracking-widest, body `text-base leading-relaxed`) and OUTCOME (same treatment but `text-muted-foreground` body for slight differentiation). Match the existing label/body typographic rhythm (`text-xs font-semibold tracking-widest uppercase mb-2` for labels, `text-base leading-relaxed mb-5` for bodies). The "Slot 01/02/03" eyebrow stays. Drop the bottom italic NDA line entirely.

**Hard rules:**
- NO client names. Grep your output for "CUNY", "KBCC", "Kingsborough", "Nairobi", any specific parish or denomination name, "TPC", "Streams of Grace", "Uplift" (in client context). All must return zero matches against the new prose.
- NO fabricated quantitative metrics. No "30% improvement," no "$X saved," no "Y students placed." NO frequency counts ("three times a day," "five times per week," etc.) — those are fabricated behavioral metrics. Qualitative language only ("stopped doing X," "compiles on demand," "outlived the engagement," "throughout the day," "constantly," "repeatedly").
- Maintain the existing voice register from COPY_HOMEPAGE.md and COPY_STUDIO.md — direct, operator-grade, no transformation-deck boilerplate.
- Do NOT change the hero section, the final CTA section, or imports. Only the SLOTS data + the per-card body markup change.
- Keep the file as a server component (no `"use client"` directive) — the existing file is server-rendered.
  </action>
  <verify>
Run from repo root:
1. `grep -c "INSTALL" app/studio/case-studies/page.tsx` returns 1 or more (label in markup).
2. `grep -c "OUTCOME" app/studio/case-studies/page.tsx` returns 1 or more.
3. `grep -c "available under NDA" app/studio/case-studies/page.tsx` returns 0 (NDA fallback removed).
4. Client-name grep returns 0: `grep -iE "(cuny|kbcc|kingsborough|nairobi|tpc|streams of grace)" app/studio/case-studies/page.tsx` → empty.
5. Fabricated metric grep returns 0: `grep -E "[0-9]+%|\\$[0-9]+|[0-9]+ (students|placements|cases|members)" app/studio/case-studies/page.tsx | grep -v "Slot 0"` → empty (Slot 01/02/03 numbering excluded).
6. Spelled-out frequency grep returns 0: `grep -iE "(once|twice|three times|four times|five times|[a-z]+ times) (a|per) (day|week|month|hour)" app/studio/case-studies/page.tsx` → empty (catches fabricated behavioral metrics that bypass digit-based grep).
7. `npm run lint -- app/studio/case-studies/page.tsx` returns no new errors or warnings.
  </verify>
  <done>
The page renders three populated slot cards. Each card shows SECTOR + CONSTRAINT (preserved) + INSTALL + OUTCOME labeled blocks with prose body. No "Case study available under NDA" copy remains. No client names appear. No fabricated numbers or fabricated frequency metrics. The page is server-rendered and the build pipeline does not error on the change.
  </done>
</task>

</tasks>

<verification>
Phase-level checks for STUDIO-CS-01:

1. Visit `/studio/case-studies` in the dev server. Three cards render. Each card has SECTOR + CONSTRAINT + INSTALL + OUTCOME prose, ~100 words combined for INSTALL+OUTCOME.
2. Read the prose to a non-technical reader. They cannot identify the client. The constraint regime (PEPFAR, FERPA, multi-jurisdiction consent) is the most specific identifier on each card.
3. No "Case study available under NDA" string anywhere on the page.
4. The slot order matches BRAND_ARCHITECTURE §5.4 + STUDIO-CS-01: East Africa health → community college NY → faith institution.
</verification>

<success_criteria>
- [ ] `app/studio/case-studies/page.tsx` modified
- [ ] Three SLOTS objects each have `install` and `outcome` fields populated
- [ ] Each card renders four labeled sections (Sector / Constraint / Install / Outcome)
- [ ] No NDA placeholder copy remains
- [ ] No client names appear in any slot
- [ ] No fabricated quantitative metrics or fabricated frequency metrics appear
- [ ] Lint clean on the file
</success_criteria>

<output>
After completion, create `.planning/phases/12-studio-repositioning-v1-1/12-01-SUMMARY.md` documenting:
- Final word count for each slot's INSTALL + OUTCOME
- Confirmation that client-name grep returned zero
- Confirmation that spelled-out-frequency grep returned zero
- Any voice-register adjustments made between draft and final
- Recommendation for whether to surface a "constraint regime tag" filter in v1.2 (preference, not blocking)
</output>
