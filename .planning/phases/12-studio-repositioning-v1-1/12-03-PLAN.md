---
phase: 12-studio-repositioning-v1-1
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/about/page.tsx
  - app/products/sage/page.tsx
  - app/engine/page.tsx
autonomous: false
requirements:
  - STUDIO-LK-01

must_haves:
  truths:
    - "Visitor on /about ecosystem section sees an explicit hyperlink to theiha.org with rel='noopener noreferrer' and target='_blank' on the IHA card"
    - "Visitor on /engine commitment section (§5) sees IHA references as hyperlinks to theiha.org with proper rel attributes"
    - "Visitor on /products/sage footer section sees a hyperlink reading '10–15% to IHA' (or close) routing to https://theiha.org with proper rel attributes"
    - "All outbound links to theiha.org from these three Perpetual Core pages use rel='noopener noreferrer' target='_blank'"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "Ecosystem section IHA card has anchor tag wrapping the Institute reference, with target='_blank' rel='noopener noreferrer'"
      contains: "theiha.org"
    - path: "app/engine/page.tsx"
      provides: "Section 5 (Engine commitment) IHA references hyperlinked to theiha.org"
      contains: "theiha.org"
    - path: "app/products/sage/page.tsx"
      provides: "Footer commitment block links '15% to IHA' to theiha.org"
      contains: "theiha.org"
  key_links:
    - from: "app/about/page.tsx ecosystem card"
      to: "https://theiha.org"
      via: "anchor tag with target=_blank rel=noopener noreferrer"
      pattern: "theiha\\.org.*noopener noreferrer"
    - from: "app/engine/page.tsx commitment section"
      to: "https://theiha.org"
      via: "anchor tag with target=_blank rel=noopener noreferrer"
      pattern: "theiha\\.org.*noopener noreferrer"
    - from: "app/products/sage/page.tsx 15% line"
      to: "https://theiha.org"
      via: "anchor tag with target=_blank rel=noopener noreferrer"
      pattern: "theiha\\.org.*noopener noreferrer"

phase_2_followup:
  truths:
    - "Visitor on theiha.org SUSTAIN pillar block can click 'Perpetual Core' and arrive at perpetualcore.com/engine (cross-repo edit)"
    - "Visitor on theiha.org case-studies (case-studies.ts:110) sees 'Perpetual Core' rendered as a hyperlink to perpetualcore.com/engine"
    - "All outbound links to perpetualcore.com from theiha.org use rel='noopener noreferrer' target='_blank'"
  rationale: "Direction-B (theiha.org → perpetualcore.com) edits live in a different repo (~/ORGANIZED/01_PROJECTS/ACTIVE/iha-website) and require a separate commit on that repo. Lorenzo handles the cross-repo edits. STUDIO-LK-01's success criterion is satisfied when Direction-A (PC → IHA) ships verified post-deploy; Direction-B is a follow-up that does NOT block this phase's merge. The Task 3 checkpoint accepts 'deferred' as a passing resolution because Direction-B is explicitly out-of-scope for the perpetual-core phase plan."
---

<objective>
Complete the IHA→Perpetual Core link audit on the Perpetual Core side. Three specific Perpetual Core pages need link upgrades on /about, /engine, and /products/sage to ensure every IHA reference is an actual hyperlink with proper `rel` attributes (security + SEO). Plan 12-05 owns the Vellum-side IHA link (added unconditionally in that plan); this plan does NOT touch `/products/vellum/page.tsx`.

The IHA-side edits (theiha.org) live in a separate repo and are tracked under `phase_2_followup` in this plan's frontmatter — they are not blockers for STUDIO-LK-01 closure on the Perpetual Core side. Lorenzo can either apply them via the iha-website worktree (Task 3 checkpoint) or formally defer to v1.2.

The Perpetual Core side is the autonomous part. Task 1 wires the three internal hyperlinks. Task 2 produces the link-audit artifact and runs pre-deploy verification. Task 3 is a checkpoint that hands off the iha-website-side change list to Lorenzo since cross-repo edits are out of scope for this phase plan.

Purpose: STUDIO-LK-01. Bidirectional brand links signal the IHA-as-parent-org / Perpetual Core-as-for-profit-arm structure to both audiences. Right now the language is unified (per `0194213` on iha-website) but most references aren't actually anchor tags. This plan completes the PC-side; Direction-B is a phase-2 follow-up.

Output: Three Perpetual Core pages updated with linked IHA references; a link-audit artifact tracking both directions; a checkpoint documenting the iha-website-side change list for Lorenzo to apply via the iha-website worktree (or accept "deferred" without blocking merge).
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/repositioning/BRAND_ARCHITECTURE.md
@.planning/repositioning/COPY_ABOUT.md
@.planning/repositioning/COPY_ENGINE.md
@.planning/repositioning/AGENT_LOG.md
@app/about/page.tsx
@app/engine/page.tsx
@app/products/sage/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire hyperlinks from /about, /engine, /products/sage to theiha.org with proper rel attributes</name>
  <files>
    app/about/page.tsx
    app/engine/page.tsx
    app/products/sage/page.tsx
  </files>
  <action>
**Edit 1: `app/about/page.tsx`**

Per COPY_ABOUT.md Section 3 (the ecosystem section), the IHA card body reads "The Institute for Human Advancement (theiha.org). Our 501(c)(3) parent..." and the section CTA reads "Visit the Institute → https://theiha.org".

Read the existing file. Find the ecosystem section. Verify:
1. The IHA card body wraps "theiha.org" as a clickable link AND wraps "Institute for Human Advancement" as a clickable link OR at minimum wraps the URL string. If only the URL is linked, also wrap the proper-noun "Institute for Human Advancement" with the same anchor pattern.
2. The "Visit the Institute" CTA at the bottom of section 3 (per COPY_ABOUT.md) is an anchor tag with target="_blank" rel="noopener noreferrer".
3. The hero section's CTA secondary "How the Engine works → /engine" stays internal (not a target=_blank link — that's a same-site route).

Edit pattern: where text reads `"Institute for Human Advancement"` or `"theiha.org"` or `"Visit the Institute"`, ensure the surrounding markup is:
```tsx
<a
  href="https://theiha.org"
  target="_blank"
  rel="noopener noreferrer"
  className="text-primary hover:underline underline-offset-4"
>
  Institute for Human Advancement
</a>
```

There may be multiple references in this file. Apply the link wrapper to:
- Subhead in Hero section (mentions "Institute for Human Advancement ecosystem")
- Section 3 IHA ecosystem card body
- Section 3 IHA ecosystem card CTA "Visit the Institute"
- Section 4 "how we work" item 5 references the Institute — link it

Do NOT modify any other content. Do NOT change layout. Only add anchor tags around existing IHA proper-noun references.

**Edit 2: `app/engine/page.tsx`**

Per COPY_ENGINE.md Section 5 (Engine commitment), there are multiple IHA references including:
- The H2 heading: "10% of every engagement — $7,500 to $25,000+ per client — funds the Institute for Human Advancement."
- Body: "The Institute for Human Advancement is our 501(c)(3) parent..."
- Body again: "We don't bury this in a footer."
- CTA primary: "About the Institute for Human Advancement → https://theiha.org"

Read the existing file. Apply the same anchor wrapping pattern as Edit 1. Specifically:
1. Section 5 H2: wrap "Institute for Human Advancement" in anchor → theiha.org with target=_blank rel=noopener noreferrer.
2. Section 5 first body paragraph: wrap "Institute for Human Advancement" in anchor.
3. Section 5 final paragraph mentioning audited annually: wrap "Institute for Human Advancement" in anchor.
4. Section 5 CTA primary: confirm it's an anchor tag with target=_blank rel=noopener noreferrer.

Don't link every instance if it makes the section hard to read. Pattern: link the FIRST occurrence in each subsection, leave subsequent occurrences as plain text. The CTA at the bottom must always be a real link.

**Edit 3: `app/products/sage/page.tsx`**

Per COPY_PRODUCTS.md Card 3 (Sage), the 15% giving rate is the Sage card's differentiator. Read the existing file. Find the section that references "15% of every Sage subscription funds the Institute for Human Advancement" (or similar wording — the Session 3 build agent may have phrased this slightly differently).

Wrap "Institute for Human Advancement" (or "IHA" if abbreviated) in an anchor tag → https://theiha.org with target=_blank rel=noopener noreferrer.

If the page has a footer commitment block reading "10–15% to IHA" or "15% to IHA," ensure that string is also wrapped in an anchor tag.

If the Sage page has a hero or final CTA referencing IHA, link the first occurrence per the same pattern.

**Hard rules across all three edits:**
- Always use `https://theiha.org` (with https:// — not bare `theiha.org`).
- Always include both `target="_blank"` and `rel="noopener noreferrer"` (security: prevents tabnabbing).
- Use `text-primary hover:underline underline-offset-4` (or whatever class pattern the existing file uses for hyperlinks — match local convention).
- Do not introduce a new component; inline anchor tags are fine.
- Do NOT modify `/products/vellum/page.tsx` — that's owned by Plan 12-05 and gets the IHA link added there.
  </action>
  <verify>
1. `grep -c "theiha.org" app/about/page.tsx` returns at least 2 (ecosystem section + CTA, possibly more).
2. `grep -c "noopener noreferrer" app/about/page.tsx` is at least 2 (matches IHA anchors).
3. `grep -c "theiha.org" app/engine/page.tsx` returns at least 2 (commitment section + CTA).
4. `grep -c "noopener noreferrer" app/engine/page.tsx` is at least 2.
5. `grep -c "theiha.org" app/products/sage/page.tsx` returns at least 1.
6. `grep -c "noopener noreferrer" app/products/sage/page.tsx` is at least 1.
7. `npm run lint -- app/about/page.tsx app/engine/page.tsx app/products/sage/page.tsx` returns clean (no new warnings).
8. Visit each page in dev: hover the IHA references → see hyperlink underline. Click → opens theiha.org in a new tab. Inspect element: confirm `target="_blank" rel="noopener noreferrer"`.
  </verify>
  <done>
All three pages render IHA references as proper hyperlinks. Each link opens theiha.org in a new tab and includes the security-correct `rel` attributes. No layout regressions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Production link verification — test all link directions from live URLs after deploy</name>
  <files>
    .planning/phases/12-studio-repositioning-v1-1/12-03-LINK-AUDIT.md
  </files>
  <action>
This task produces a link-audit artifact. The verification can run pre-deploy (against the dev server) but the truth-claim "links test green from production URLs" requires waiting for deploy. Document both.

Create `.planning/phases/12-studio-repositioning-v1-1/12-03-LINK-AUDIT.md` with this structure:

```markdown
# IHA ↔ Perpetual Core Link Audit (STUDIO-LK-01)

## Direction A: Perpetual Core → IHA (this plan's scope)

Source page → target URL → expected anchor text → rel attribute check.

| Source | Target | Anchor text | rel attrs |
|---|---|---|---|
| /about ecosystem section IHA card | https://theiha.org | "Institute for Human Advancement" | noopener noreferrer + target=_blank |
| /about ecosystem section CTA | https://theiha.org | "Visit the Institute" | noopener noreferrer + target=_blank |
| /engine §5 commitment H2 | https://theiha.org | "Institute for Human Advancement" | noopener noreferrer + target=_blank |
| /engine §5 commitment body | https://theiha.org | "Institute for Human Advancement" | noopener noreferrer + target=_blank |
| /engine §5 CTA primary | https://theiha.org | "About the Institute for Human Advancement" | noopener noreferrer + target=_blank |
| /products/sage 15% callout | https://theiha.org | "Institute for Human Advancement" | noopener noreferrer + target=_blank |
| /products/vellum 10% callout (added in Plan 12-05) | https://theiha.org | "Institute for Human Advancement" | noopener noreferrer + target=_blank |

For each, run:
```bash
curl -sI https://theiha.org | head -3
```
Expected: `HTTP/2 200`. Confirms the destination is live.

## Direction B: IHA → Perpetual Core (cross-repo, phase-2 follow-up — NOT in this plan's scope)

These edits live in `~/ORGANIZED/01_PROJECTS/ACTIVE/iha-website` and require a separate commit on that repo. Lorenzo (or a follow-up cross-repo plan) applies these. Direction B does NOT block STUDIO-LK-01 closure on the Perpetual Core side.

| File | Line | Edit |
|---|---|---|
| `iha-website/src/lib/constants.ts` | ~236 | SUSTAIN pillar's "Perpetual Core" reference becomes `<a href="https://perpetualcore.com/engine" target="_blank" rel="noopener noreferrer">Perpetual Core</a>` (or equivalent in JSX consumer if constants.ts holds plain strings — wrap in the consuming component). |
| `iha-website/src/lib/case-studies.ts` | ~110 | "Perpetual Core (AI Infrastructure)" reference becomes a hyperlink to perpetualcore.com/engine in the consumer markup. |

For each, after Lorenzo edits and deploys iha-website:
```bash
curl -s https://theiha.org/ | grep -c "perpetualcore.com/engine"
```
Expected: at least 2 (SUSTAIN block + case studies block).

## Pre-deploy verification (dev server)

Run from the perpetual-core dev server (`npm run dev`):
```bash
curl -s http://localhost:3000/about | grep -E '<a [^>]*theiha\.org[^>]*noopener'
curl -s http://localhost:3000/engine | grep -E '<a [^>]*theiha\.org[^>]*noopener'
curl -s http://localhost:3000/products/sage | grep -E '<a [^>]*theiha\.org[^>]*noopener'
```
Each command must return at least one match.

## Post-deploy verification (production)

After `feat/studio-repositioning` (with this plan applied) merges to main and deploys:
```bash
curl -s https://perpetualcore.com/about | grep -E '<a [^>]*theiha\.org[^>]*noopener'
curl -s https://perpetualcore.com/engine | grep -E '<a [^>]*theiha\.org[^>]*noopener'
curl -s https://perpetualcore.com/products/sage | grep -E '<a [^>]*theiha\.org[^>]*noopener'
```
Each must return at least one match.

## Status tracker

- [ ] Direction A pre-deploy verification (dev server)
- [ ] Direction A post-deploy verification (production)
- [ ] Direction B iha-website edits applied (Lorenzo, cross-repo) — phase-2 follow-up; "deferred" is a valid resolution
- [ ] Direction B post-deploy verification (theiha.org) — phase-2 follow-up

## Notes

The cross-repo Direction B is gated on Lorenzo running edits in the iha-website worktree. Per AGENT_LOG.md `0194213` on iha-website unified the SUSTAIN pillar definition and case-studies.ts:110 framing — but those edits did NOT add anchor tags. The hyperlink edits are pending and tracked under this plan's `phase_2_followup` frontmatter. Direction B does NOT block STUDIO-LK-01 closure on the Perpetual Core side.
```

Run all three pre-deploy verification curls against `npm run dev`. Capture output. If any returns no match, go back to Task 1 and add the missing link.
  </action>
  <verify>
1. `test -f .planning/phases/12-studio-repositioning-v1-1/12-03-LINK-AUDIT.md` returns 0.
2. The file contains the four-section structure (Direction A table, Direction B table, pre-deploy, post-deploy).
3. The dev-server curls described in "Pre-deploy verification" each return at least one match.
4. The status tracker has Direction A pre-deploy checkbox marked complete.
  </verify>
  <done>
The link-audit artifact exists with structured tables for both directions. All Direction A pre-deploy curls return matches. The post-deploy and Direction B sections are flagged as pending — those gate on deploy + Lorenzo's cross-repo work (or are formally deferred per phase_2_followup frontmatter).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Lorenzo applies cross-repo edits to iha-website (Direction B) OR formally defers</name>
  <action>
The iha-website repo lives at `~/ORGANIZED/01_PROJECTS/ACTIVE/iha-website` (per CLAUDE.md ecosystem map). Direction B link edits are out of scope for this perpetual-core phase plan because they require committing to a different repository. Per this plan's `phase_2_followup` frontmatter, Direction B does NOT block STUDIO-LK-01 closure on the Perpetual Core side — "deferred" is a valid resolution at this checkpoint.

**Lorenzo: please apply these edits manually (or spawn a separate Claude session in the iha-website repo). OR mark Direction B as deferred to v1.2 — both resolutions are acceptable.**

1. **`iha-website/src/lib/constants.ts:~236`** — SUSTAIN pillar block. Find the line where "Perpetual Core" appears in the SUSTAIN pillar text. If it's a plain-string export, this requires a JSX consumer change instead — find the React component that renders SUSTAIN content and wrap "Perpetual Core" with:
   ```tsx
   <a href="https://perpetualcore.com/engine" target="_blank" rel="noopener noreferrer" className="text-primary underline underline-offset-4">Perpetual Core</a>
   ```

2. **`iha-website/src/lib/case-studies.ts:~110`** — find "Perpetual Core (AI Infrastructure)" reference. Same wrapper pattern in the consuming component.

3. Commit on iha-website with:
   ```
   feat(links): hyperlink Perpetual Core references to perpetualcore.com/engine
   ```

4. Deploy iha-website (Vercel push to main or whatever the current deploy flow is).

5. Verify post-deploy:
   ```bash
   curl -s https://theiha.org/ | grep -c "perpetualcore.com/engine"
   curl -s https://theiha.org/case-studies | grep -c "perpetualcore.com/engine"
   ```
   Each command should return at least 1.

6. Update `.planning/phases/12-studio-repositioning-v1-1/12-03-LINK-AUDIT.md` Direction B status checkboxes.
  </action>
  <resume-signal>
Type "iha-links-applied" once Direction B is committed and deployed. OR type "deferred" to mark Direction B as v1.2 follow-up — both are passing resolutions because Direction-B truths are tracked under `phase_2_followup` (not `must_haves`) per this plan's frontmatter. STUDIO-LK-01 closure on the Perpetual Core side does NOT depend on Direction B.
  </resume-signal>
</task>

</tasks>

<verification>
Phase-level checks for STUDIO-LK-01:

1. Direction A (PC → IHA) — three perpetual-core pages each carry at least one anchor tag to https://theiha.org with target=_blank rel=noopener noreferrer.
2. Direction A pre-deploy curls return matches; post-deploy curls (to be run after merge) will return matches.
3. The link-audit artifact `.planning/phases/12-studio-repositioning-v1-1/12-03-LINK-AUDIT.md` exists and tracks both directions.
4. Direction B (IHA → PC) is either applied (Lorenzo committed iha-website edits) OR explicitly deferred to v1.2 with documentation. Both resolutions are acceptable per `phase_2_followup` frontmatter.
5. Vellum's IHA link (10% callout) is NOT modified by this plan — owned by Plan 12-05 (which adds it unconditionally).
</verification>

<success_criteria>
- [ ] /about, /engine, /products/sage all hyperlink IHA references with target=_blank rel=noopener noreferrer
- [ ] All anchors point to https://theiha.org (https://, not bare theiha.org)
- [ ] Pre-deploy curls find anchor tags on each page
- [ ] Link-audit artifact created and populated
- [ ] Lorenzo notified of Direction B cross-repo work via checkpoint (with explicit "deferred" path documented)
- [ ] Lint clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/12-studio-repositioning-v1-1/12-03-SUMMARY.md` documenting:
- All anchor tags added with file:line references
- Pre-deploy curl output captured
- Direction B disposition (applied / deferred-to-v1.2 / blocked-on-Lorenzo)
- Confirmation that Direction-B truths live in `phase_2_followup` (not blocking must_haves)
- Any deferred items for STUDIO-PL-01 mobile QA (e.g., link tap targets at 375px)
</output>
