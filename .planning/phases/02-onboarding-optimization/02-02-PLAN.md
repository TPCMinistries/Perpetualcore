---
phase: 02-onboarding-optimization
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/onboarding/OnboardingChecklist.tsx
  - lib/onboarding/actions.ts
  - components/dashboard/DashboardWithOnboarding.tsx
  - app/dashboard/layout.tsx
autonomous: true
requirements: [ONBD-02]

must_haves:
  truths:
    - "New user sees an onboarding checklist on the dashboard after completing the wizard"
    - "Checklist tracks three activation milestones: first chat, upload document, explore agents"
    - "Completed milestones are visually marked with a green checkmark and strikethrough"
    - "Checklist progress bar fills proportionally as milestones are completed"
    - "Checklist auto-hides once all three milestones are complete"
  artifacts:
    - path: "components/onboarding/OnboardingChecklist.tsx"
      provides: "Redesigned activation checklist with 3 milestones, auto-detection, visual progress"
      min_lines: 80
    - path: "lib/onboarding/actions.ts"
      provides: "Updated server actions with explore-agents milestone detection and improved progress logic"
      exports: ["getOnboardingProgress", "dismissOnboardingChecklist", "isChecklistDismissed", "completeOnboarding", "markStepComplete", "needsOnboarding"]
    - path: "app/dashboard/layout.tsx"
      provides: "Dashboard layout that renders OnboardingChecklist after OnboardingFlowV3 for post-onboarding users"
  key_links:
    - from: "components/onboarding/OnboardingChecklist.tsx"
      to: "lib/onboarding/actions.ts"
      via: "getOnboardingProgress server action"
      pattern: "getOnboardingProgress"
    - from: "app/dashboard/layout.tsx"
      to: "components/onboarding/OnboardingChecklist.tsx"
      via: "direct import and render"
      pattern: "OnboardingChecklist"
    - from: "lib/onboarding/actions.ts"
      to: "conversations, documents, agents tables"
      via: "Supabase queries checking if user has records"
      pattern: "from.*conversations|from.*documents|from.*agents"
---

<objective>
Onboarding activation checklist that tracks three key milestones (first chat, upload document, explore agents) with visual progress and auto-detection.

Purpose: After the aha-moment chat (Plan 01), users need clear guidance on what to do next. The checklist creates a visible path to full activation — users who complete all three milestones are significantly more likely to become paying customers. The current OnboardingChecklist exists but is disconnected from the active OnboardingFlowV3, uses a hacked completion detection for "search," and is not rendered in the dashboard layout.

Output: Redesigned OnboardingChecklist with proper milestone tracking, integrated into dashboard layout.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-optimization/02-01-SUMMARY.md
@components/onboarding/OnboardingChecklist.tsx
@lib/onboarding/actions.ts
@components/dashboard/DashboardWithOnboarding.tsx
@app/dashboard/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign OnboardingChecklist with 3 activation milestones and proper detection</name>
  <files>
    components/onboarding/OnboardingChecklist.tsx
    lib/onboarding/actions.ts
  </files>
  <action>
    Rewrite `lib/onboarding/actions.ts` — update the `getOnboardingProgress` function:
    - Keep the existing `completeOnboarding`, `markStepComplete`, `dismissOnboardingChecklist`, `isChecklistDismissed`, and `needsOnboarding` functions unchanged.
    - Rewrite `getOnboardingProgress` to check three milestones:
      1. `first_chat`: Query `conversations` table for `user_id` with at least 1 row (already exists).
      2. `first_document`: Query `documents` table for `user_id` with at least 1 row (already exists).
      3. `explore_agents`: Query the agents/assistants page visit. Since there is no easy "page visit" table, use a pragmatic approach: check if the user has visited the assistants browse page by checking if they have any record in `assistants` table where they are the owner OR they have navigated to /dashboard/assistants/browse. Since tracking page visits would require new infrastructure, use the simpler heuristic: check the `assistants` table for rows where `user_id` matches. If user has created or interacted with at least one assistant, mark as complete. If not, provide the link to `/dashboard/assistants/browse` so they can explore. Additionally, add a new `markStepComplete` call that the assistants browse page can call when visited — but for now, fall back to checking assistant records.
    - Return the progress array in the same shape: `{ step_key: string, completed: boolean }[]` with keys `first_chat`, `first_document`, `explore_agents`.

    Rewrite `components/onboarding/OnboardingChecklist.tsx`:
    - Change the three steps to:
      1. `first_chat` — "Have your first conversation" / "Chat with your AI brain" / icon: MessageSquare / href: `/dashboard/chat`
      2. `first_document` — "Upload a document" / "Build your knowledge base" / icon: Upload / href: `/dashboard/library`
      3. `explore_agents` — "Explore AI agents" / "Discover specialized AI assistants" / icon: Bot / href: `/dashboard/assistants/browse`
    - Keep the existing visual design pattern (it uses Card, Progress, emerald for completed, slate for incomplete) but improve it:
      - Add a subtle gradient background: `bg-gradient-to-br from-violet-50/50 to-blue-50/50 dark:from-violet-950/20 dark:to-blue-950/20` to match the app's purple/blue brand.
      - The progress bar should use the same purple-to-blue gradient as the onboarding wizard.
      - Each incomplete step should have a subtle hover animation (scale or shadow).
      - Each completed step shows green checkmark with emerald background and line-through text (keep existing pattern).
    - Keep the dismiss button (X) and the auto-hide when all steps complete.
    - Keep the loading state and the `isChecklistDismissed` check.
    - Export as named export `OnboardingChecklist` (matching existing pattern).
    - Import `Bot` from lucide-react for the agents step icon.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Verify `getOnboardingProgress` returns all three milestone keys. Verify OnboardingChecklist renders 3 steps.
  </verify>
  <done>
    OnboardingChecklist shows 3 activation milestones (first chat, upload doc, explore agents) with real completion detection from database tables. Completed items show green checkmarks. Progress bar fills proportionally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate OnboardingChecklist into dashboard layout for post-onboarding users</name>
  <files>
    app/dashboard/layout.tsx
    components/dashboard/DashboardWithOnboarding.tsx
  </files>
  <action>
    Modify `app/dashboard/layout.tsx`:
    - Import `OnboardingChecklist` from `@/components/onboarding/OnboardingChecklist`.
    - After the OnboardingFlowV3 component (which only shows for users who have NOT completed onboarding), add the OnboardingChecklist for users who HAVE completed onboarding. The checklist should render inside the `DashboardLayoutClient` children area as a floating/sticky element or at the top of the content area.
    - Since the layout wraps `{children}`, the checklist needs to be rendered in a way that appears at the top of every dashboard page. The cleanest approach: wrap `{children}` with a div that has the checklist above it. Something like:
      ```tsx
      <DashboardLayoutClient profile={profile}>
        {profile?.onboarding_completed && !profile?.onboarding_checklist_dismissed && (
          <div className="px-4 pt-4 pb-0 max-w-4xl mx-auto w-full">
            <OnboardingChecklist />
          </div>
        )}
        {children}
      </DashboardLayoutClient>
      ```
    - The `profile` object fetched by `getUserProfile()` already includes `onboarding_completed` and `onboarding_checklist_dismissed`. Verify this by checking what `getUserProfile` returns. If it does not include `onboarding_checklist_dismissed`, extend the query in `lib/auth/actions.ts` to include it. But do NOT change the auth file unless necessary — first check if the profile data is already available.
    - The OnboardingChecklist component itself already handles the dismissed state and auto-hide logic, so the server-side check is just an optimization to avoid rendering the component at all for dismissed users.

    Update `components/dashboard/DashboardWithOnboarding.tsx`:
    - This file uses the older WelcomeWizard flow and is likely no longer the active code path (OnboardingFlowV3 in layout.tsx is the active one). Leave it as-is to avoid breaking anything, but verify it is not imported anywhere critical. If it IS imported somewhere, update its OnboardingChecklist reference to use the new milestones. Based on the codebase analysis, DashboardWithOnboarding does NOT appear to be used in the active layout (layout.tsx uses OnboardingFlowV3 directly), so no changes needed here.

    After both tasks, run `npm run build` to verify the full production build succeeds.
  </action>
  <verify>
    Run `npm run build` to verify production build succeeds with zero errors. Verify that `app/dashboard/layout.tsx` renders OnboardingChecklist for users who have completed onboarding. Visually confirm (or verify via code) that the checklist appears at the top of the dashboard content area.
  </verify>
  <done>
    OnboardingChecklist appears at the top of the dashboard for users who completed the onboarding wizard but have not completed all milestones or dismissed the checklist. Auto-hides when all 3 milestones are done. Build passes.
  </done>
</task>

</tasks>

<verification>
1. After completing onboarding wizard (Plan 01 redirects to chat), navigate back to dashboard — checklist should appear
2. Checklist shows 3 items: first chat (should already be marked complete if user chatted in Plan 01), upload doc, explore agents
3. Upload a document — revisit dashboard, "Upload a document" should now be checked off
4. Visit /dashboard/assistants/browse — revisit dashboard, "Explore AI agents" should be checked off
5. Once all 3 are complete, checklist auto-hides
6. Dismiss button (X) hides checklist permanently
7. Run `npm run build` — zero errors
</verification>

<success_criteria>
- OnboardingChecklist renders on dashboard with 3 activation milestones
- Milestones auto-detect completion from database records (conversations, documents, assistants)
- Completed items show green checkmarks with visual distinction
- Progress bar fills proportionally (0%, 33%, 66%, 100%)
- Checklist auto-hides at 100% or when dismissed
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-optimization/02-02-SUMMARY.md`
</output>
