---
phase: 02-onboarding-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/onboarding/OnboardingFlowV3.tsx
  - components/chat/EmptyState.tsx
  - app/api/chat/route.ts
  - lib/onboarding/guided-chat.ts
autonomous: true
requirements: [ONBD-01]

must_haves:
  truths:
    - "New user completing onboarding is directed to a guided first chat experience (not a generic dashboard landing)"
    - "The first AI response references the user's name, role, and goals collected during onboarding (demonstrating persistent memory)"
    - "The chat EmptyState for first-time users shows a contextual welcome prompt pre-filled based on their onboarding answers"
  artifacts:
    - path: "lib/onboarding/guided-chat.ts"
      provides: "First-chat prompt builder that creates a personalized greeting prompt from onboarding profile data"
      exports: ["buildFirstChatPrompt", "getFirstChatSuggestions"]
    - path: "components/onboarding/OnboardingFlowV3.tsx"
      provides: "Modified completion flow that redirects to /dashboard/chat?guided=true instead of just closing"
    - path: "components/chat/EmptyState.tsx"
      provides: "Enhanced empty state that detects guided=true and shows personalized first-chat prompt"
    - path: "app/api/chat/route.ts"
      provides: "System prompt enhancement that injects onboarding context for first conversation"
  key_links:
    - from: "components/onboarding/OnboardingFlowV3.tsx"
      to: "/dashboard/chat?guided=true"
      via: "router.push on completeOnboarding"
      pattern: "router\\.push.*chat.*guided"
    - from: "components/chat/EmptyState.tsx"
      to: "lib/onboarding/guided-chat.ts"
      via: "import buildFirstChatPrompt"
      pattern: "buildFirstChatPrompt"
    - from: "app/api/chat/route.ts"
      to: "profiles table"
      via: "loads user_role, primary_goals, preferred_name for first conversation system prompt"
      pattern: "preferred_name|user_role|primary_goals"
---

<objective>
Guided first-chat aha moment: after onboarding, redirect users into a chat experience that immediately demonstrates persistent memory by referencing their name, role, and goals.

Purpose: The biggest conversion lever for an AI product is the "wow, it knows me" moment. Currently OnboardingFlowV3 ends with a generic button and drops users on the dashboard with no guidance. This plan creates a seamless onboarding-to-chat bridge that makes the AI's persistent memory feature undeniable from the very first interaction.

Output: Modified onboarding completion flow, guided chat utilities, enhanced chat empty state, and first-conversation context injection.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/onboarding/OnboardingFlowV3.tsx
@components/chat/EmptyState.tsx
@app/api/chat/route.ts
@app/api/profile/context/route.ts
@lib/intelligence/preference-loader.ts
@lib/ai/memory.ts
@app/dashboard/chat/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guided-chat utility and modify onboarding completion redirect</name>
  <files>
    lib/onboarding/guided-chat.ts
    components/onboarding/OnboardingFlowV3.tsx
  </files>
  <action>
    Create `lib/onboarding/guided-chat.ts` with two named exports:

    1. `buildFirstChatPrompt(profile: { preferredName: string; userRole: string; primaryGoals: string[]; industry?: string }): string` — Returns a natural-language first message for the user to send (or have auto-sent). The prompt should be personalized, e.g.: "Hi! I'm [name], a [role]. I'm looking to [first goal]. What can you help me with?" The function maps role IDs (professional, entrepreneur, creative, technical, student_educator, researcher) to human-readable labels and goal IDs (from the GoalsStep options in OnboardingFlowV3) to readable descriptions. Return a prompt that feels natural, not robotic.

    2. `getFirstChatSuggestions(userRole: string, primaryGoals: string[]): Array<{ icon: string; text: string; prompt: string }>` — Returns 3-4 personalized quick-action prompts based on the user's role and goals. These replace the generic QUICK_PROMPTS in EmptyState for first-time guided users. For example, an entrepreneur with "market_research" goal would get "Research my competitors", "Draft a business plan outline", "Analyze market trends".

    Then modify `components/onboarding/OnboardingFlowV3.tsx`:
    - In the `completeOnboarding` function, change the post-completion behavior. After the successful API calls, instead of just `setIsOpen(false)`, do `router.push('/dashboard/chat?guided=true')`.
    - In the `handleActionClick` function, append `?guided=true` to the URL if the action is `/dashboard/chat`.
    - In the `CompleteStep` component, change the main CTA button text from "Start Using My AI Brain" to "Start Your First Conversation" and have it call `router.push('/dashboard/chat?guided=true')` directly (bypassing completeOnboarding since it was already called in the flow). Wait — actually the CompleteStep's `onGetStarted` already calls `completeOnboarding`. So just ensure `completeOnboarding` redirects to chat with guided=true. The `setIsOpen(false)` should still happen to close the modal before the redirect.

    Do NOT change the onboarding wizard steps themselves (Welcome, PersonalInfo, Goals, Customize are working well). Only modify the exit flow.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Verify that `lib/onboarding/guided-chat.ts` exports both functions. Verify that OnboardingFlowV3's completeOnboarding now includes `router.push('/dashboard/chat?guided=true')`.
  </verify>
  <done>
    OnboardingFlowV3 completion redirects to `/dashboard/chat?guided=true`. `buildFirstChatPrompt` and `getFirstChatSuggestions` exist and return personalized content based on user profile data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance chat EmptyState for guided first-use and inject onboarding context into AI system prompt</name>
  <files>
    components/chat/EmptyState.tsx
    app/api/chat/route.ts
  </files>
  <action>
    Modify `components/chat/EmptyState.tsx`:
    - Import `buildFirstChatPrompt` and `getFirstChatSuggestions` from `@/lib/onboarding/guided-chat`.
    - Add a new prop `isGuidedFirstChat?: boolean` to `EmptyStateProps`.
    - When `isGuidedFirstChat` is true, show a different layout:
      - Heading: "Welcome to your AI brain, [name]!" (fetch name from the same profile fetch already in ChatPage)
      - Subheading: "I already know a bit about you from your setup. Try asking me something — I'll remember everything."
      - Show a prominent "Send your first message" card with the pre-built prompt from `buildFirstChatPrompt` that the user can click to auto-send (call `onSuggestionClick` with the built prompt).
      - Below it, show the personalized suggestions from `getFirstChatSuggestions` instead of the generic QUICK_PROMPTS.
      - Hide the "Recent conversations" section and the generic activity section for guided mode (the user has no prior conversations anyway).
    - To get the profile data for `buildFirstChatPrompt`, add a fetch to `/api/profile/context` in the `fetchActivity` useEffect (or a separate effect) when `isGuidedFirstChat` is true.

    Modify `app/dashboard/chat/page.tsx` (the parent):
    - Read `guided` from URL search params using `useSearchParams()` from `next/navigation`.
    - Pass `isGuidedFirstChat={searchParams.get('guided') === 'true' && messages.length === 0}` to the EmptyState component. The `messages.length === 0` check ensures the guided state goes away once the user sends their first message.

    Modify `app/api/chat/route.ts`:
    - In the POST handler, after the existing system prompt is built (around line 200+), add a check: if the user has `onboarding_completed === true` AND the conversation is new (no existing conversationId in the request body) AND this appears to be within a few minutes of onboarding completion (check `onboarding_completed_at` within last 10 minutes), inject additional context into the system prompt:
      ```
      FIRST CONVERSATION CONTEXT:
      This is the user's very first conversation. They just completed onboarding.
      Their name is [preferred_name]. They are a [user_role]. Their goals are [primary_goals].
      Make your FIRST response demonstrate that you remember them by name and reference their goals.
      This is the "aha moment" — show them you have persistent memory by greeting them personally
      and proactively suggesting how you can help with their specific goals.
      Do NOT be generic. Be specific to their role and goals.
      ```
    - Load the profile fields `preferred_name, user_role, primary_goals, onboarding_completed_at` from the profiles table. The existing code already fetches the user profile in the route — extend that query to include these fields if not already present.

    Do NOT modify the streaming logic or the tool system. Only add context injection for the first conversation.
  </action>
  <verify>
    Run `npx tsc --noEmit` to verify no type errors. Run `npm run build` to verify production build succeeds. Verify that EmptyState accepts and uses `isGuidedFirstChat` prop. Verify that chat/page.tsx reads the `guided` search param.
  </verify>
  <done>
    When a user completes onboarding and lands on `/dashboard/chat?guided=true`, the EmptyState shows personalized welcome with pre-built prompt. The AI's first response references the user by name and their specific goals — the persistent memory aha moment is delivered.
  </done>
</task>

</tasks>

<verification>
1. Complete the OnboardingFlowV3 wizard and verify redirect goes to `/dashboard/chat?guided=true`
2. On the guided chat page, verify the EmptyState shows personalized welcome, not generic prompts
3. Click the pre-built first message and verify the AI responds by referencing the user's name, role, and goals
4. After sending the first message, verify the guided state clears and normal chat resumes
5. Run `npm run build` — zero errors
</verification>

<success_criteria>
- OnboardingFlowV3 completion redirects to guided chat (not generic dashboard)
- First-time chat EmptyState shows personalized welcome with pre-built prompt
- AI's first response demonstrates persistent memory (references name, role, goals)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-optimization/02-01-SUMMARY.md`
</output>
