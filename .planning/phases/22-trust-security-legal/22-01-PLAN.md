---
phase: 22-trust-security-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/rls/rfp-tenant-isolation.test.ts
  - .github/workflows/ci.yml
autonomous: false
requirements: [TRUST-01]
user_setup:
  - service: github-actions
    why: "RLS CI gate runs against the live LDC Brain AI database and needs the service-role key as a repo secret"
    env_vars:
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard (project hgxxxmtfmvguotkowxbu) -> Settings -> API -> service_role key -> add to GitHub repo Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "A cross-tenant test authenticating as Org A returns 0 rows (not an error) when reading Org B's vault artifacts, proposals, AND entitlements"
    - "A dedicated test-rls CI job runs npx vitest run tests/rls/ on every push/PR and fails the pipeline if any isolation assertion fails"
    - "The test-rls job has the three Supabase secrets wired (URL, anon key, service-role key)"
  artifacts:
    - path: "tests/rls/rfp-tenant-isolation.test.ts"
      provides: "Cross-tenant isolation suite including a new rfp_entitlements assertion"
      contains: "rfp_entitlements"
    - path: ".github/workflows/ci.yml"
      provides: "Required test-rls CI job scoped to tests/rls/ with live-DB secrets"
      contains: "test-rls"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tests/rls/"
      via: "npx vitest run tests/rls/ with SUPABASE_SERVICE_ROLE_KEY env"
      pattern: "vitest run tests/rls"
    - from: "tests/rls/rfp-tenant-isolation.test.ts"
      to: "rfp_entitlements"
      via: "user-scoped client select returns [] for Org B's entitlement row"
      pattern: "rfp_entitlements"
---

<objective>
Make the cross-tenant RLS isolation test a REQUIRED CI gate, and extend the existing isolation suite to cover rfp_entitlements (built in Phase 14, not yet asserted).

Purpose: Satisfies TRUST-01 / phase success criterion 1 — a CI test authenticating as Org A attempting to read Org B's proposals, vault chunks, AND entitlements returns 0 rows, and is a required check in the pipeline. This is the hard security gate that lets a second org go live.
Output: An extended tests/rls/rfp-tenant-isolation.test.ts and a new required `test-rls` job in ci.yml. The test infrastructure (ephemeral users, orgs, RLS-scoped clients) already exists — this is wiring + one new assertion.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trust-security-legal/22-RESEARCH.md

# Existing test infrastructure to extend (DO NOT rewrite — add to it)
@tests/rls/rfp-tenant-isolation.test.ts
@.github/workflows/ci.yml
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rfp_entitlements cross-tenant assertion to the existing isolation suite</name>
  <files>tests/rls/rfp-tenant-isolation.test.ts</files>
  <action>
    Extend the EXISTING suite — do not create a new file and do not rewrite the existing assertions.

    1. In `beforeAll` (after the memberships insert, alongside the vault artifact seeding around line 152), seed one `rfp_entitlements` row per org via the `admin` (service-role) client:
       `await admin.from("rfp_entitlements").insert([{ org_id: orgA, coverage_level: "l1" }, { org_id: orgB, coverage_level: "l1" }])`.
       Check `coverage_level` is a valid CHECK value — Phase 14 stored it as a text CHECK constraint; `l1` is the baseline (confirm against supabase/migrations/20260606_rfp_entitlements.sql; if the column requires other NOT NULL fields, supply them). Throw on insert error like the other seeds do.
       Note: the Stripe webhook upsert only ever writes 3 fields (org_id, coverage_level, updated_at) per the Phase 14 decision log — match that minimal shape.

    2. Add a new `it("User A cannot read Org B entitlements", ...)` inside the existing `describe("RFP tenant isolation", ...)` block, mirroring the existing vault-artifact assertion pattern (lines 188-198):
       - build a user-scoped client via the existing `clientFor(userA.access_token)` helper
       - `const { data, error } = await c.from("rfp_entitlements").select("*").eq("org_id", orgB)`
       - `expect(error).toBeNull()` (RLS returns empty, NOT a 403 — entitlements SELECT policy is `rfp_my_org_ids()`)
       - `expect(data).toEqual([])`

    3. Add a positive-control `it("User A reads only their own Org A entitlements", ...)`:
       - `const { data } = await clientFor(userA.access_token).from("rfp_entitlements").select("*").eq("org_id", orgA)`
       - `expect(data?.length).toBeGreaterThanOrEqual(1)` and assert every returned row has `org_id === orgA`.

    The afterAll already deletes orgs with CASCADE (deletes rfp_user_orgs, rfp_vault_artifacts, etc.) — confirm rfp_entitlements has an ON DELETE CASCADE FK to rfp_orgs so the seeded entitlement rows are cleaned up. If it does NOT cascade, add an explicit `await admin.from("rfp_entitlements").delete().in("org_id", [orgA, orgB])` to afterAll before the org deletes.

    Do NOT add a schema migration — rfp_entitlements already exists from Phase 14.
  </action>
  <verify>
    With .env.local present, run `npx vitest run tests/rls/rfp-tenant-isolation.test.ts`. All assertions pass, including the two new entitlements assertions. Confirm afterAll leaves no `*@test.local` users or orphan entitlement rows (the run completes cleanly).
  </verify>
  <done>The isolation suite asserts vault artifacts, orgs, opportunities (existing) PLUS entitlements (new): Org A reads 0 rows for Org B's entitlements and ≥1 row for its own. Suite is green against the live DB.</done>
</task>

<task type="auto">
  <name>Task 2: Add a required test-rls CI job wired to the live DB secrets</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a NEW job named `test-rls` to ci.yml. Do NOT add the service-role secret to the existing `test` job — that would run prod-DB RLS tests on every unit-test run (research Pitfall 2).

    Append this job (keep existing jobs intact):
    ```yaml
      test-rls:
        name: Cross-Tenant RLS Gate
        runs-on: ubuntu-latest
        needs: lint-and-typecheck
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: 20
              cache: npm
          - name: Install dependencies
            run: npm ci
          - name: Cross-tenant isolation tests (REQUIRED GATE)
            run: npx vitest run tests/rls/
            env:
              NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
              NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
              SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    ```
    This scopes the run to `tests/rls/` (both isolation files) and injects all three Supabase secrets. The existing `build` job already references the two NEXT_PUBLIC_* secrets, so only SUPABASE_SERVICE_ROLE_KEY is net-new.

    IMPORTANT: the RLS test files read env from `.env.local` first, then fall back to `process.env`. The GitHub Actions `env:` block populates `process.env`, so no `.env.local` is needed in CI. Confirm the test's env-loading logic falls through to `process.env` (it does — see lines 52-61) and does not hard-require the `.env.local` file to exist.

    To make this a REQUIRED check: GitHub branch-protection "required status checks" is configured in the repo UI/API, not in ci.yml. Document in the SUMMARY that the operator must add `Cross-Tenant RLS Gate` to required status checks on main/develop branch protection (gh api command: `gh api -X PUT repos/{owner}/{repo}/branches/main/protection/required_status_checks/contexts -f 'contexts[]=Cross-Tenant RLS Gate'`).
  </action>
  <verify>
    Run `node -e "const yaml=require('yaml')||null"` is not needed — instead validate YAML by running `npx --yes yaml-lint .github/workflows/ci.yml` OR `node -e "require('js-yaml')"` if available; if no YAML linter is installed, verify structurally with `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('valid')"`. Confirm the `test-rls` job exists and references `SUPABASE_SERVICE_ROLE_KEY`.
  </verify>
  <done>ci.yml contains a `test-rls` job that runs `npx vitest run tests/rls/` with all three Supabase secrets in its env. YAML parses cleanly. Existing jobs (lint-and-typecheck, build, test) are unchanged.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Add SUPABASE_SERVICE_ROLE_KEY repo secret + mark RLS gate required</name>
  <action>Human action only (credential gate — cannot be automated from this environment). Add the SUPABASE_SERVICE_ROLE_KEY GitHub repo secret and mark the Cross-Tenant RLS Gate as a required status check, then confirm the job runs green. Steps are in how-to-verify below.</action>
  <what-built>The test-rls CI job and the extended isolation suite are committed. The job will run on the next push but will FAIL until the GitHub repo has the SUPABASE_SERVICE_ROLE_KEY secret, because that secret cannot be added programmatically from this environment (it is a credential gate).</what-built>
  <how-to-verify>
    1. Add the secret to the repo. Either:
       - Settings -> Secrets and variables -> Actions -> New repository secret -> name `SUPABASE_SERVICE_ROLE_KEY`, value = the service_role key from Supabase project hgxxxmtfmvguotkowxbu (Dashboard -> Settings -> API), OR
       - via gh CLI: `gh secret set SUPABASE_SERVICE_ROLE_KEY` (paste the key when prompted).
    2. Confirm `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY` secrets already exist (the build job uses them) — list with `gh secret list`.
    3. Mark the check required: add `Cross-Tenant RLS Gate` to required status checks on the `main` (and `develop`) branch protection rules (Settings -> Branches, or the gh api command in Task 2).
    4. Push a commit (or re-run the workflow) and confirm the `Cross-Tenant RLS Gate` job runs and passes green.
  </how-to-verify>
  <resume-signal>Type "approved" once the secret is set and the test-rls job has run green, or describe any failure (e.g., missing secret, wrong project key, CHECK-constraint error on the entitlements seed).</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run tests/rls/rfp-tenant-isolation.test.ts` passes locally with the new entitlements assertions
- `.github/workflows/ci.yml` parses as valid YAML and contains the `test-rls` job referencing SUPABASE_SERVICE_ROLE_KEY
- After the human-action checkpoint: the `Cross-Tenant RLS Gate` job runs green in GitHub Actions and is listed as a required status check on main
</verification>

<success_criteria>
Phase success criterion 1 is met: a CI test authenticating as Org A attempting to read Org B's proposals, vault chunks, AND entitlements returns 0 rows, and `Cross-Tenant RLS Gate` is a required pipeline check.
</success_criteria>

<output>
After completion, create `.planning/phases/22-trust-security-legal/22-01-SUMMARY.md`. Record: the entitlements assertion added, the CI job added, whether the GitHub secret was confirmed present, and the exact branch-protection step the operator must take (or took) to make the check required.
</output>
