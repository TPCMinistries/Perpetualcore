---
phase: 22-trust-security-legal
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-trust-security-legal/SECURITY-AUDIT.md
autonomous: true
requirements: [TRUST-02]

must_haves:
  truths:
    - "A documented, route-by-route audit records that no user-context API route reads tenant data via the service-role client without a prior RLS-enforced auth/membership check"
    - "The audit explicitly documents the intentional dual-client pattern (createClient auth gate, then createAdminClient write/read after the gate) and names the routes that use it"
    - "The audit confirms whether rfp_opportunity_enrichments holds per-org data, validating its globally-readable policy"
  artifacts:
    - path: ".planning/phases/22-trust-security-legal/SECURITY-AUDIT.md"
      provides: "Formal service-role hygiene audit with a route-by-route table"
      contains: "createAdminClient"
      min_lines: 60
  key_links:
    - from: ".planning/phases/22-trust-security-legal/SECURITY-AUDIT.md"
      to: "app/api/rfp/**"
      via: "route-by-route audit table citing each createAdminClient usage and its preceding auth check"
      pattern: "app/api/rfp"
---

<objective>
Produce a DOCUMENTED service-role hygiene audit confirming no user-context API route reads tenant data via the service-role client without first proving auth/membership via the RLS-enforced client.

Purpose: Satisfies TRUST-02 / phase success criterion 2 — a code audit confirms NO user-context API route reads tenant data via service-role; user-facing vault/proposal reads go through RLS-enforced paths. Research already established the pattern is CLEAN across all 17 user-facing routes; this plan formalizes that finding into a repo artifact and re-verifies it live (so the audit is evidence, not assertion). If the re-verification finds a genuine violation, fix it.
Output: .planning/phases/22-trust-security-legal/SECURITY-AUDIT.md — a route-by-route audit table with the verification method and findings.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-trust-security-legal/22-RESEARCH.md

# Audit source — the dual-client pattern and route inventory are documented in research; re-verify against live code
@lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the createClient/createAdminClient ordering across all user-facing RFP routes</name>
  <files>.planning/phases/22-trust-security-legal/SECURITY-AUDIT.md</files>
  <action>
    Re-run the audit empirically (do not trust research alone — produce evidence). Use grep across `app/api/rfp/**`:

    1. List every route that calls `createAdminClient`:
       `grep -rn "createAdminClient" app/api/rfp/`
    2. For EACH such route, confirm a `createClient()` + `getUser()` (or membership check via the RLS-scoped client) appears BEFORE the first `createAdminClient()` call. The audit rule (research Pitfall 4): any route returning rfp_proposals, rfp_vault_artifacts, or rfp_entitlements data to a user MUST call createClient()+getUser() first; admin client may only be used for writes, or for reads provably bounded by an earlier RLS membership check.
       Practical check per file: grep the line numbers of `createClient(`, `getUser(`, and `createAdminClient(` and confirm ordering (auth before admin). For ambiguous files, open and read the relevant block.
    3. Flag any route where `createAdminClient()` precedes the auth/membership check — that is a VIOLATION. Research found NONE; if your re-verification also finds none, document that. If you find one, STOP and note it for Task 2.
    4. Verify the cron/webhook/admin-console routes (app/api/cron/**, app/api/webhooks/rfp-stripe/**, app/api/admin/rfp/**) correctly use ONLY createAdminClient (no user session context) — these are correct as admin-only and should be listed as out-of-scope-but-correct.

    Then write SECURITY-AUDIT.md with:
    - Header: title, date, auditor (Claude, Phase 22), scope (app/api/rfp/** + cron/webhook/admin context), method (grep + manual read).
    - A "Verification command" section showing the exact grep used and the count of routes inspected.
    - A route-by-route table: | Route | Tables touched | createAdminClient used for | Auth check before admin? (Y/N + line evidence) | Verdict |
      Populate from the live grep, cross-checked against the research route list (research lines 410-445).
    - A subsection on the intentional dual-client pattern, quoting the canonical example (createClient auth gate -> membership proof via RLS -> createAdminClient for the read/write) and explaining why this is NOT misuse per CLAUDE.md ("Background/server operations: ALWAYS use createAdminClient()").
    - A subsection resolving Open Question 2: state whether rfp_opportunity_enrichments holds per-org data. Verify by reading the migration/schema for that table (grep `rfp_opportunity_enrichments` in supabase/migrations and lib/) — confirm columns are per-opportunity metadata, not org-scoped. Conclude whether the `auth.uid() IS NOT NULL` globally-readable policy is correct.
    - A "Findings" conclusion: PASS (no service-role misuse) or list of violations.
    - A note that this audit is scoped to tables/routes that exist NOW (Phase 14 foundation + Phases 04/05/13 routes); later-phase routes must be re-audited when added.
  </action>
  <verify>
    `grep -rn "createAdminClient" app/api/rfp/` output is fully accounted for in the audit table (every hit has a row). SECURITY-AUDIT.md exists, contains a route table, and states an explicit PASS/violations verdict. The enrichments question is answered with a schema citation.
  </verify>
  <done>SECURITY-AUDIT.md records a route-by-route audit proving (with line evidence) that every user-context RFP route authenticates via createClient before any createAdminClient call, documents the intentional dual-client pattern, and resolves the enrichments per-org question.</done>
</task>

<task type="auto">
  <name>Task 2: Fix any genuine violation found (no-op if audit is clean)</name>
  <files>.planning/phases/22-trust-security-legal/SECURITY-AUDIT.md</files>
  <action>
    If Task 1 found ZERO violations (the expected outcome per research), this task is a documented no-op: add a one-line "Remediation: none required — audit clean" entry to SECURITY-AUDIT.md and finish.

    If Task 1 found a route where createAdminClient reads tenant data (rfp_proposals/rfp_vault_artifacts/rfp_entitlements) BEFORE an auth+membership check, remediate surgically in that route file (add the route file to this plan's files_modified in the SUMMARY):
    - Insert the standard auth gate at the top: `const supabase = await createClient(); const { data: { user } } = await supabase.auth.getUser(); if (!user) return 401;`
    - Add the membership proof via the RLS-scoped client (rfp_user_orgs select scoped by org_id + user.id) before the admin read.
    - Re-run the relevant smoke/test for that route.
    - Keep the change surgical (Karpathy: touch only what's broken).
    Document the remediation in SECURITY-AUDIT.md with before/after line references.
  </action>
  <verify>
    If clean: SECURITY-AUDIT.md states "Remediation: none required". If a fix was made: the offending route now calls createClient()+getUser()+membership check before createAdminClient(), and `npm run type-check` passes for the changed file.
  </verify>
  <done>Either the audit is documented clean with no code change, or each found violation is remediated with the standard auth-gate pattern and the fix is recorded in the audit doc.</done>
</task>

</tasks>

<verification>
- `grep -rn "createAdminClient" app/api/rfp/` — every result appears as a row in SECURITY-AUDIT.md
- SECURITY-AUDIT.md states an explicit verdict (PASS or remediated)
- The rfp_opportunity_enrichments globally-readable policy is validated with a schema citation
- If any code was changed, `npm run type-check` passes
</verification>

<success_criteria>
Phase success criterion 2 is met: a documented code audit confirms no user-context API route reads tenant data via the service-role client without a prior RLS-enforced auth/membership check; user-facing vault/proposal reads go through RLS-enforced paths.
</success_criteria>

<output>
After completion, create `.planning/phases/22-trust-security-legal/22-02-SUMMARY.md`. Record: routes audited (count), verdict (clean/remediated), the enrichments finding, and any route files modified.
</output>
