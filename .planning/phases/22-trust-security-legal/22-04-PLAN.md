---
phase: 22-trust-security-legal
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/DATA-SOURCE-COMPLIANCE.md
autonomous: true
requirements: [TRUST-04]

must_haves:
  truths:
    - "A written compliance doc reviews every registered data source against its terms of use and records whether each is used lawfully"
    - "The doc formally records that Candid / Foundation Directory is excluded (license prohibits AI/LLM use) and that zero Candid API calls exist in the codebase"
    - "The doc records that ProPublica/IRS 990 are not yet ingested (planned, Phase 16) and flags the ProPublica commercial-use ToS question to resolve before that integration"
  artifacts:
    - path: ".planning/DATA-SOURCE-COMPLIANCE.md"
      provides: "Data-source ToS compliance review covering every registered source + the Candid exclusion"
      contains: "Candid"
      min_lines: 50
  key_links:
    - from: ".planning/DATA-SOURCE-COMPLIANCE.md"
      to: "lib/rfp/source-catalog.ts"
      via: "each catalog source reviewed against its ToS with a compliant/blocked verdict"
      pattern: "source-catalog"
---

<objective>
Write the data-source ToS compliance review that confirms every registered source is used lawfully, formally records the Candid exclusion, and verifies no Candid data is ingested.

Purpose: Satisfies TRUST-04 / phase success criterion 4 — a documented ToS-compliance review confirms ProPublica/IRS 990 used lawfully and no Candid data ingested. Research confirmed Candid is catalog-blocked with zero API calls, and ProPublica/IRS 990 are "planned" (not yet ingested). This plan produces the formal artifact.
Output: .planning/DATA-SOURCE-COMPLIANCE.md — a per-source compliance table + the Candid exclusion declaration + the ProPublica open question.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-trust-security-legal/22-RESEARCH.md

# The authoritative source registry — review each entry against its ToS
@lib/rfp/source-catalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the live source-catalog state and the Candid exclusion in code</name>
  <files>.planning/DATA-SOURCE-COMPLIANCE.md</files>
  <action>
    Gather evidence before writing (so the doc is grounded in current code, not just research):
    1. Read lib/rfp/source-catalog.ts and enumerate every registered source with its `status` and `ingestMode` (the doc must cover ALL registered sources, not only the ones in the research table).
    2. Confirm Candid is `status: "blocked"` / `ingestMode: "licensed"` and prove zero ingestion: run `grep -rin "candid" lib/ app/` and confirm the only references are the catalog entry + the canonicalize.ts comment/type (no candid.org API call). Capture the grep output for the doc.
    3. Confirm ProPublica / IRS 990 status: `grep -rin "propublica\|irs_990\|nonprofit" lib/rfp/` — confirm no API calls exist yet (status "planned").

    Begin writing .planning/DATA-SOURCE-COMPLIANCE.md with a header (title, date, reviewer, declaration line: "Data sources reviewed for ToS compliance as of 2026-06-06. Candid excluded; no license-restricted data redistributed."). Include a "Verification commands & output" section with the grep evidence for the Candid exclusion.
  </action>
  <verify>
    `grep -rin "candid" lib/ app/` output is captured in the doc and shows no candid.org API call. The doc's source list matches every entry in lib/rfp/source-catalog.ts (no registered source omitted).
  </verify>
  <done>DATA-SOURCE-COMPLIANCE.md exists with a dated declaration, enumerates every catalog source with live status, and includes grep evidence proving zero Candid ingestion.</done>
</task>

<task type="auto">
  <name>Task 2: Write the per-source compliance table and the ProPublica/Candid findings</name>
  <files>.planning/DATA-SOURCE-COMPLIANCE.md</files>
  <action>
    Complete the doc with a per-source compliance table covering each registered source (research lines 233-248 as the baseline, expanded to match the live catalog):
    | Source | Status (live/planned/blocked) | ToS URL | Key terms (public-domain? redistribution? attribution? commercial use?) | Compliant verdict |
    Cover at minimum: SAM.gov, Grants.gov, SBIR.gov, NIH Grants, NSF Grants, NYC Open Data (DYCD/HRA/DOE), NJ Treasury (OpenBid), ProPublica Nonprofit Explorer, IRS 990 direct, Candid Foundation Directory — plus any additional sources found in the live catalog in Task 1.

    Then add three subsections:
    1. "Candid exclusion (CONFIRMED)" — license prohibits AI/LLM use and redistribution; catalog status "blocked"; zero API calls (cite the grep). TRUST-04 satisfied for Candid by current code state.
    2. "ProPublica / IRS 990 (PLANNED — not yet ingested)" — IRS 990 is public-domain government data; ProPublica Nonprofit Explorer API is free for research with no explicit prohibition found, BUT the commercial-SaaS-redistribution clause is unresolved (research Open Question 4). Record the action: "ProPublica API terms (https://projects.propublica.org/nonprofits/api) to be read in full and the commercial-use finding documented here BEFORE Phase 16 ingests this data." Do not ingest anything in this phase.
    3. "Redistribution stance" — a short statement that the product surfaces opportunity metadata/links and does not resell or redistribute license-restricted bulk datasets.

    Close with a "Review cadence" note: re-review when a new source is added to the catalog or before any source moves from planned -> live.

    This is a documentation deliverable only — no code changes, no ingestion, no schema change.
  </action>
  <verify>
    The doc contains a per-source table with a verdict column covering every catalog source; Candid row shows "blocked / N/A"; ProPublica row shows "planned / verify commercial-use clause before Phase 16". The doc states an overall TRUST-04 conclusion.
  </verify>
  <done>DATA-SOURCE-COMPLIANCE.md reviews every registered source against its terms, formally excludes Candid with code evidence, flags the ProPublica commercial-use question for pre-Phase-16 resolution, and concludes TRUST-04 is satisfied for current ingestion.</done>
</task>

</tasks>

<verification>
- .planning/DATA-SOURCE-COMPLIANCE.md exists with a dated declaration
- Per-source table covers every entry in lib/rfp/source-catalog.ts (none omitted)
- Candid documented as excluded with grep evidence of zero API calls
- ProPublica/IRS 990 documented as planned with the commercial-use ToS action flagged before Phase 16
</verification>

<success_criteria>
Phase success criterion 4 is met: a documented ToS-compliance review confirms ProPublica/IRS 990 will be used lawfully (and is not yet ingested) and that no Candid data is ingested.
</success_criteria>

<output>
After completion, create `.planning/phases/22-trust-security-legal/22-04-SUMMARY.md`. Record: sources reviewed (count), the Candid exclusion evidence, and the ProPublica pre-Phase-16 action item.
</output>
