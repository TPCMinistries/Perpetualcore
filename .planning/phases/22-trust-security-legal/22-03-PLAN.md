---
phase: 22-trust-security-legal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/terms/page.tsx
  - app/privacy/page.tsx
  - app/ai-disclosure/page.tsx
  - middleware.ts
autonomous: false
requirements: [TRUST-03]

must_haves:
  truths:
    - "Terms of Service, Privacy Policy, and AI-use disclosure pages are publicly accessible (no login) at /terms, /privacy, and /ai-disclosure on the live domain"
    - "All three pages describe the RFP & Proposal Engine (government/grant data, vault uploads, LLM-generated proposals, no auto-submission) — NOT the generic Perpetual Core AI OS (no Slack/Zoom/WhatsApp/calendar references)"
    - "Each page carries a visible 'draft — pending counsel review / not legal advice' banner and a current (2026) last-updated date; entity named as Perpetual Core LLC"
  artifacts:
    - path: "app/ai-disclosure/page.tsx"
      provides: "New AI-use disclosure page (federal-contractor AI disclosure: GSA GSAR 552.239-7001, NIH; human-review requirement)"
      contains: "552.239-7001"
    - path: "app/terms/page.tsx"
      provides: "RFP-engine-specific Terms of Service"
      contains: "Perpetual Core LLC"
    - path: "app/privacy/page.tsx"
      provides: "RFP-engine-specific Privacy Policy"
      contains: "vault"
    - path: "middleware.ts"
      provides: "Public (unauthenticated) access for /ai-disclosure alongside /terms and /privacy"
      contains: "ai-disclosure"
  key_links:
    - from: "middleware.ts"
      to: "/ai-disclosure"
      via: "public-path allowlist entry so the page renders without auth"
      pattern: "ai-disclosure"
    - from: "app/terms/page.tsx"
      to: "/ai-disclosure"
      via: "in-page link to the AI disclosure page"
      pattern: "ai-disclosure"
---

<objective>
Rewrite /terms and /privacy for the RFP engine and create a new public /ai-disclosure page, then wire /ai-disclosure into the middleware public allowlist.

Purpose: Satisfies TRUST-03 / phase success criterion 3 — ToS, Privacy Policy, and AI-use disclosure pages publicly accessible at known URLs on the live domain. The existing /terms and /privacy describe the generic Perpetual Core AI OS (Slack/Zoom/WhatsApp/calendar — none in the RFP engine); they must be rewritten. /ai-disclosure does not exist and is the most critical page (federal-contractor AI-use disclosure).
Output: rewritten app/terms/page.tsx + app/privacy/page.tsx, new app/ai-disclosure/page.tsx, and middleware.ts updated so /ai-disclosure is public.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-trust-security-legal/22-RESEARCH.md

# Existing pages to rewrite (match their layout/styling — shadcn Button + prose container)
@app/terms/page.tsx
@app/privacy/page.tsx
@middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /terms and /privacy for the RFP engine</name>
  <files>app/terms/page.tsx, app/privacy/page.tsx</files>
  <action>
    Keep the existing page scaffold (Next metadata export, the gradient container, the shadcn `Button` back-to-home link, the `prose` content wrapper). Replace only the legal copy. Match the existing component structure so styling stays consistent.

    Entity/product facts to use (research lines 198-218):
    - Operator: Perpetual Core LLC (NOT "Perpetual Core, Inc.")
    - Parent mission org: Institute for Human Advancement (IHA), 501(c)(3)
    - Product: RFP & Proposal Engine at rfp.perpetualcore.com
    - Data processed: public government RFP data (SAM.gov, Grants.gov, SBIR, NIH, NSF); municipal open data (NYC, NJ); user-uploaded vault documents (past proposals, annual reports); LLM-generated proposal content (Anthropic Claude, OpenAI)
    - NOT processed: Candid Foundation Directory (blocked); NO Slack/Zoom/WhatsApp/calendar integrations
    - Final submission is ALWAYS human — the product gets users to submission-ready, never auto-submits

    /terms/page.tsx — replace content with RFP-specific sections:
    1. Acceptance of Terms
    2. Description of Service (the RFP & Proposal Engine: discovery, fit scoring, drafting, review, compliance, submission tracking — explicitly NO auto-submission)
    3. User Responsibilities (review and verify all AI-generated proposal content before submission; user is responsible for the accuracy and compliance of anything submitted)
    4. Government & Public Data (opportunities sourced from public government/municipal data; those sources' own terms govern that data; we do not redistribute license-restricted data)
    5. AI-Generated Content (proposals are AI-assisted; see /ai-disclosure) — link to /ai-disclosure
    6. Intellectual Property, 7. Limitation of Liability, 8. Termination, 9. Contact (legal@perpetualcore.com)
    Update the "Last updated" line to a 2026 date. Update metadata title/description to mention the RFP & Proposal Engine. Add a draft banner (see Task 2 for exact banner text/style; reuse the same banner element).

    /privacy/page.tsx — replace content with RFP-specific sections:
    1. Information We Collect (org profile data, vault documents the user uploads [PDF/DOCX], proposal content, usage data)
    2. How We Use Information (generate embeddings + proposal drafts + fit scores; vault docs are processed by AI providers)
    3. Information from Third Parties / Sub-processors (REMOVE Slack/Google Drive/Zoom; ADD: government data sources [SAM.gov, Grants.gov, etc.] and AI providers Anthropic & OpenAI used for embeddings and proposal generation)
    4. Data Retention, 5. Data Security (reference RLS tenant isolation), 6. Your Rights, 7. Contact
    Update "Last updated" to 2026. Update metadata. Add the same draft banner. Add a link to /ai-disclosure.

    Keep copy draft-grade and clear; do not invent specific legal guarantees. No `any` types; TypeScript strict.
  </action>
  <verify>
    `npm run type-check` passes. `grep -ni "slack\|zoom\|whatsapp\|calendar\|Perpetual Core, Inc" app/terms/page.tsx app/privacy/page.tsx` returns NOTHING. Both files contain "Perpetual Core LLC", a 2026 last-updated date, and a link to /ai-disclosure. `npm run dev` then load /terms and /privacy — both render the new RFP content.
  </verify>
  <done>/terms and /privacy describe the RFP engine (no generic AI-OS feature references), name Perpetual Core LLC, show a 2026 date and a draft banner, and link to /ai-disclosure.</done>
</task>

<task type="auto">
  <name>Task 2: Create the /ai-disclosure page and add it to the middleware public allowlist</name>
  <files>app/ai-disclosure/page.tsx, middleware.ts</files>
  <action>
    Create app/ai-disclosure/page.tsx mirroring the layout of app/terms/page.tsx (metadata export, gradient container, back-to-home Button, prose wrapper) for visual consistency. Statically rendered, no auth, no client-side data.

    Required sections (research lines 219-227, 470-482):
    1. How AI is used in this product — fit scoring, draft generation, adversarial review, compliance checking
    2. Which AI providers process your data — Anthropic (Claude) and OpenAI; vault documents and proposal context are sent to these providers to generate embeddings and content
    3. Human review requirement — AI generates drafts; the user reviews, verifies, and submits. The product never auto-submits.
    4. Federal & grant compliance context — federal contractors using AI in solicitation responses are subject to GSA GSAR 552.239-7001; NIH has separate AI-disclosure requirements for grant applications. Draft output includes an AI-use disclosure notice and the compliance gate includes an AI-disclosure acknowledgement (forward-reference to the Phase 19 compliance gate).
    5. Controlling AI use / opt-out — how a user can limit or control AI processing (best-effort description).
    6. Data retention for AI-processed content.
    Include the string "552.239-7001" verbatim in the federal-compliance section.

    Add a prominent draft banner at the top of ALL THREE pages (define it inline on each page, or as a tiny shared element if a components/ location already exists for legal — prefer inline to keep changes surgical): a visible callout box reading approximately: "Draft — pending counsel review. This content is provided for informational purposes only and does not constitute legal advice. Review with qualified counsel before relying on it." Style it as an amber/yellow bordered callout so it's clearly visible.

    Update metadata: title "AI-Use Disclosure | Perpetual Core RFP & Proposal Engine".

    middleware.ts — add /ai-disclosure to the public-path allowlist next to the existing `/privacy` and `/terms` checks (currently around lines 88-89: `pathname === '/privacy' || pathname === '/terms'`). Add `pathname === '/ai-disclosure'` so the page renders without authentication. Do not touch the getUser()-before-/api short-circuit logic — only extend the public-path list (surgical).
  </action>
  <verify>
    `npm run type-check` passes. `app/ai-disclosure/page.tsx` exists and contains "552.239-7001". `grep -n "ai-disclosure" middleware.ts` shows the new public-path entry. With `npm run dev`, load /ai-disclosure while logged out — it returns 200 and renders (no redirect to login). All three pages show the draft banner.
  </verify>
  <done>/ai-disclosure exists, is publicly accessible unauthenticated, covers AI usage + providers + human-review + GSA GSAR 552.239-7001/NIH, and all three legal pages carry the draft banner. middleware allowlists /ai-disclosure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all three legal pages load unauthenticated with correct RFP content</name>
  <action>Human verification only. Confirm /terms, /privacy, and /ai-disclosure load unauthenticated with the correct RFP-engine content and draft banner. Steps are in how-to-verify below.</action>
  <what-built>Three legal pages rewritten/created for the RFP engine and made publicly accessible: /terms, /privacy (rewritten), /ai-disclosure (new). Middleware allows /ai-disclosure without login.</what-built>
  <how-to-verify>
    1. Run `npm run dev`. While LOGGED OUT (use an incognito window), visit:
       - http://localhost:3000/terms
       - http://localhost:3000/privacy
       - http://localhost:3000/ai-disclosure
       Each must load (200, no redirect to login) and display the draft banner.
    2. Confirm /terms and /privacy describe the RFP engine (government/grant data, vault uploads, AI-assisted proposals, no auto-submission) and contain NO Slack/Zoom/WhatsApp/calendar references and say "Perpetual Core LLC".
    3. Confirm /ai-disclosure explains the AI providers (Claude, OpenAI), the human-review requirement, and the federal-compliance context (GSA GSAR 552.239-7001 / NIH).
    4. Optional: after deploy, confirm the same three URLs are publicly reachable on the live domain.
  </how-to-verify>
  <resume-signal>Type "approved" if all three pages load unauthenticated with correct RFP content + draft banner, or describe what's wrong (wrong content, page redirects to login, banner missing).</resume-signal>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- `grep -ni "slack\|zoom\|whatsapp" app/terms/page.tsx app/privacy/page.tsx` returns nothing
- `grep -n "ai-disclosure" middleware.ts` shows the public-path entry
- /terms, /privacy, /ai-disclosure all return 200 unauthenticated and show the draft banner
- /ai-disclosure contains "552.239-7001"
</verification>

<success_criteria>
Phase success criterion 3 is met: ToS, Privacy Policy, and AI-use disclosure pages are publicly accessible at /terms, /privacy, /ai-disclosure on the live domain, all RFP-specific, all carrying a visible draft/not-legal-advice banner.
</success_criteria>

<output>
After completion, create `.planning/phases/22-trust-security-legal/22-03-SUMMARY.md`. Record: the three pages' new content scope, the middleware allowlist change, and confirmation the pages load unauthenticated.
</output>
