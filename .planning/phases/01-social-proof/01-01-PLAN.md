---
phase: 01-social-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - components/landing/FounderStory.tsx
  - components/landing/ComparisonTable.tsx
  - components/landing/TrustBadges.tsx
  - components/landing/SocialProofBanner.tsx
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
  - PROOF-03
  - PROOF-04

must_haves:
  truths:
    - "Visitor sees a Built By section with Lorenzo's name, photo placeholder, IHA mission, and a brief founder story"
    - "Visitor sees a comparison table showing Perpetual Core vs ChatGPT vs generic competitors with rows for memory, models, RAG, agents, and pricing"
    - "Visitor sees trust badges for SOC 2 Ready, Enterprise SSO, and 99.9% Uptime SLA on the landing page"
    - "Visitor sees a Trusted by X organizations counter with placeholder partner/industry logos"
  artifacts:
    - path: "components/landing/FounderStory.tsx"
      provides: "Built By founder credibility section"
      min_lines: 40
    - path: "components/landing/ComparisonTable.tsx"
      provides: "Feature comparison table vs ChatGPT and competitors"
      min_lines: 60
    - path: "components/landing/TrustBadges.tsx"
      provides: "SOC 2, SSO, uptime trust badge strip"
      min_lines: 30
    - path: "components/landing/SocialProofBanner.tsx"
      provides: "Trusted by X organizations counter with logo placeholders"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "Landing page with all 4 social proof sections integrated"
      contains: "FounderStory"
  key_links:
    - from: "app/page.tsx"
      to: "components/landing/FounderStory.tsx"
      via: "import and render"
      pattern: "import.*FounderStory"
    - from: "app/page.tsx"
      to: "components/landing/ComparisonTable.tsx"
      via: "import and render"
      pattern: "import.*ComparisonTable"
    - from: "app/page.tsx"
      to: "components/landing/TrustBadges.tsx"
      via: "import and render"
      pattern: "import.*TrustBadges"
    - from: "app/page.tsx"
      to: "components/landing/SocialProofBanner.tsx"
      via: "import and render"
      pattern: "import.*SocialProofBanner"
---

<objective>
Add four social proof sections to the Perpetual Core landing page: a founder credibility story, a competitive comparison table, trust/compliance badges, and an organization counter with logo placeholders.

Purpose: Visitors currently see no credibility signals beyond the product itself. These sections give visitors reasons to trust the product before signing up -- founder legitimacy, competitive differentiation, security posture, and social proof via organization adoption.

Output: Four new component files in `components/landing/` and an updated `app/page.tsx` that renders all four sections in the landing page flow.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four social proof landing page components</name>
  <files>
    components/landing/FounderStory.tsx
    components/landing/ComparisonTable.tsx
    components/landing/TrustBadges.tsx
    components/landing/SocialProofBanner.tsx
  </files>
  <action>
Create a `components/landing/` directory and build four components. All components are client-side ("use client" not needed since they have no interactivity -- just static JSX). Use the same Tailwind styling conventions already in `app/page.tsx`: `backdrop-blur-2xl`, `bg-card/80`, `border-2 border-border`, gradient backgrounds, `shadow-xl`, `animate-on-scroll` classes, lucide-react icons, and `@/components/ui/card` from shadcn.

**1. FounderStory.tsx (PROOF-01)**
A "Built By" section with:
- Section header: "Built By a Builder, Not a Board Room" or similar
- A two-column layout (stacks on mobile): left side has a gray placeholder div (rounded, 200x200) for Lorenzo's photo with alt text "Lorenzo Daughtry-Chambers, Founder", right side has:
  - Name: "Lorenzo Daughtry-Chambers"
  - Title: "Founder & CEO"
  - 2-3 sentences about building Perpetual Core as a tool he uses daily to run multiple organizations, making it production-tested from day one
  - Brief mention of the Institute for Human Advancement (IHA) mission: "10% of every dollar funds workforce development and education through our nonprofit arm, the Institute for Human Advancement"
  - A subtle badge/pill: "Founder-Led & Mission-Driven"
- Use the `Card` component with the glassmorphic style matching existing cards
- Include appropriate lucide icons (Building2, Heart, or similar)

**2. ComparisonTable.tsx (PROOF-02)**
A feature comparison table:
- Section header: "How Perpetual Core Compares" with subtitle
- Responsive HTML table (horizontal scroll on mobile via `overflow-x-auto`) with 4 columns: Feature | Perpetual Core | ChatGPT Plus | Other AI Tools
- Rows (7 total):
  - Conversation Memory: "Infinite - never forgets" | "~200 messages then resets" | "Session-based"
  - AI Models: "GPT-4, Claude, Gemini, DeepSeek" | "GPT-4 only" | "Single model"
  - Document Search (RAG): "Full RAG with vector search" | "Limited file upload" | "No document search"
  - AI Agents: "30+ pre-built + custom" | "Basic GPTs" | "Limited or none"
  - Team Collaboration: "Shared knowledge bases" | "Individual only" | "Varies"
  - Enterprise Security: "RLS, SSO ready, audit logs" | "Basic" | "Varies"
  - Pricing: "From $49/mo (all models)" | "$20/mo (GPT-4 only)" | "$20-100/mo per model"
- Perpetual Core column gets green check icons and bold text. Competitor columns get neutral/dimmed styling.
- Use Tailwind table classes: `divide-y`, `border`, alternating row backgrounds

**3. TrustBadges.tsx (PROOF-03)**
A horizontal strip of trust badges:
- Three badges in a row (flexbox, wrap on mobile):
  - Shield icon + "SOC 2 Ready" + subtitle "Enterprise compliance"
  - Lock/Key icon + "Enterprise SSO" + subtitle "SAML & OAuth 2.0"
  - Clock/Zap icon + "99.9% Uptime SLA" + subtitle "Guaranteed availability"
- Each badge is a rounded card/pill with an icon, bold title, and muted subtitle
- Style: glassmorphic cards with subtle borders, similar to the trust indicators already in the hero section but larger and more prominent
- Use `Shield`, `Lock`, `Clock` from lucide-react (Lock is not currently imported -- add it. Check if it exists in lucide-react, if not use `ShieldCheck` for SSO instead)

**4. SocialProofBanner.tsx (PROOF-04)**
A "Trusted by" section:
- Large centered counter text: "Trusted by 50+ Organizations" (use a number that reads as aspirational but not unbelievable for an early-stage product)
- Subtitle: "From startups to enterprises across healthcare, legal, education, and finance"
- A row of 6 placeholder logo spots: gray rounded rectangles (w-24 h-10 or similar) with industry labels underneath (e.g., "Healthcare", "Legal", "Education", "Finance", "Technology", "Non-Profit"). These are placeholders -- use `bg-muted` boxes, NOT actual logos
- Style the counter number with the gradient text treatment used elsewhere (`bg-gradient-to-r from-primary via-purple-600 to-primary bg-clip-text text-transparent`)
- Add `animate-on-scroll` to the section wrapper

Do NOT add any external dependencies. Use only what is already in the project: lucide-react, shadcn/ui Card, Tailwind.
  </action>
  <verify>
Run `npx tsc --noEmit` (or `npm run build`) from the project root to confirm all four component files compile without TypeScript errors. Visually inspect that each file exports a default or named function component.
  </verify>
  <done>
Four component files exist in `components/landing/`, each exports a React component, and `npm run build` passes with no errors related to these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate social proof components into the landing page</name>
  <files>
    app/page.tsx
  </files>
  <action>
Import and render all four social proof components in `app/page.tsx`. The page is currently 1366 lines with this section order:

1. Navigation
2. Hero
3. Value Proposition Banner
4. Visitor Segmentation (Choose Your Path)
5. Interactive Chat Demo
6. Core Value Props
7. AI Executive Suite
8. Who It's For (Use Cases)
9. Security & Privacy
10. Features Grid
11. How It Works
12. Benefits
13. Pricing Teaser
14. Final CTA
15. Footer

**Insert the new sections at these locations:**

- **SocialProofBanner** (PROOF-04): Insert immediately AFTER the Hero section (after line ~377, before the Value Proposition Banner). This "Trusted by 50+ Organizations" counter creates immediate credibility right below the hero CTA. Wrap with `animate-on-scroll` if not already in the component.

- **ComparisonTable** (PROOF-02): Insert AFTER the Core Value Props section (after line ~714, before AI Executive Suite). The visitor has just learned what makes Perpetual Core different -- now show them a direct comparison. This is the logical "prove it" moment.

- **TrustBadges** (PROOF-03): Insert AFTER the Security & Privacy section (after line ~1037, before Features Grid). The visitor has just read about security -- now reinforce it with formal compliance badges.

- **FounderStory** (PROOF-01): Insert AFTER the Benefits section (after line ~1115, before Pricing Teaser). By this point the visitor knows the product and its advantages -- the founder story adds a personal, human touch right before the pricing ask.

**Imports to add at the top of page.tsx** (near line 43, after the existing imports):
```tsx
import { SocialProofBanner } from "@/components/landing/SocialProofBanner";
import { ComparisonTable } from "@/components/landing/ComparisonTable";
import { TrustBadges } from "@/components/landing/TrustBadges";
import { FounderStory } from "@/components/landing/FounderStory";
```

If any component uses a default export instead of named export, adjust the import accordingly. Add any new lucide-react icons needed by the components to the existing lucide-react import block at the top of page.tsx only if they are used directly in page.tsx (the components should import their own icons).

Do NOT restructure or refactor any existing sections. Only add imports and render the four new components at the specified insertion points.
  </action>
  <verify>
Run `npm run build` from the project root. The build must pass with zero errors. Then run `npm run dev` and visit http://localhost:3000 to confirm:
1. "Trusted by 50+ Organizations" banner appears below the hero
2. Comparison table appears after the "An AI Operating System, Not Just Chat" section
3. Trust badges (SOC 2, SSO, Uptime) appear after the Security & Privacy section
4. Founder story appears after the Benefits section, before Pricing
5. Page scrolls smoothly with no layout breaks
  </verify>
  <done>
All four social proof sections are visible on the landing page at their designated positions. `npm run build` passes. No existing sections are broken or displaced. The page renders correctly on both desktop and mobile viewports.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` passes with zero errors
2. Navigate to http://localhost:3000 and scroll the full page
3. Confirm all 4 social proof sections are present in the correct order:
   - Hero -> **Trusted by 50+ Orgs** -> Value Prop Banner -> ...
   - Core Value Props -> **Comparison Table** -> AI Executive Suite -> ...
   - Security & Privacy -> **Trust Badges** -> Features Grid -> ...
   - Benefits -> **Founder Story** -> Pricing Teaser -> ...
4. Each section is responsive (check at mobile, tablet, desktop breakpoints)
5. Existing sections are unmodified and still render correctly
</verification>

<success_criteria>
- Visitor scrolling the landing page sees Lorenzo's story with IHA mission (PROOF-01)
- Visitor sees a clear comparison table: Perpetual Core vs ChatGPT vs competitors across 7 feature rows (PROOF-02)
- Visitor sees SOC 2 Ready, Enterprise SSO, and 99.9% Uptime SLA badges (PROOF-03)
- Visitor sees "Trusted by 50+ Organizations" with industry logo placeholders (PROOF-04)
- `npm run build` passes with no errors
- No existing landing page functionality is broken
</success_criteria>

<output>
After completion, create `.planning/phases/01-social-proof/01-01-SUMMARY.md`
</output>
