---
phase: 14-canonical-data-foundation
plan: 04
type: execute
wave: 2
depends_on: ["14-01", "14-02", "14-03"]
files_modified:
  - lib/supabase/database.types.ts
  - tests/unit/rfp-dedup-persist.test.ts
  - scripts/verify-rfp-dedup.ts
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "Ingesting the same opportunity from two different sources results in ONE canonical row with TWO aliases, not two canonicals"
    - "A runnable verification proves dedup end-to-end (test or script), not just key-derivation"
    - "database.types.ts reflects all Phase 14 schema (7 new opportunity columns, rfp_entitlements, match_vault_docs RPC)"
  artifacts:
    - path: "tests/unit/rfp-dedup-persist.test.ts"
      provides: "Vitest proving persistCanonicalAliases collapses two-source duplicates to one canonical + two aliases"
      contains: "persistCanonicalAliases"
    - path: "scripts/verify-rfp-dedup.ts"
      provides: "Live DB script: seed two-source duplicate, run canonical persist, assert 1 canonical / 2 aliases, cleanup"
      contains: "rfp_opportunity_aliases"
    - path: "lib/supabase/database.types.ts"
      provides: "Regenerated types including all Phase 14 schema changes"
      contains: "rfp_entitlements"
  key_links:
    - from: "lib/rfp/ingest/run.ts"
      to: "persistCanonicalAliases"
      via: "already called after upsertBatch (line ~191) — this plan VERIFIES it works"
      pattern: "persistCanonicalAliases"
    - from: "scripts/verify-rfp-dedup.ts"
      to: "rfp_opportunity_canonicals / rfp_opportunity_aliases"
      via: "count assertion 1 canonical : 2 aliases for one logical opportunity"
      pattern: "rfp_opportunity_(canonicals|aliases)"
---

<objective>
Prove the dedup layer works end-to-end (FND-02) and reconcile `database.types.ts` against all Phase 14 schema changes from Plans 14-01/02/03.

Purpose: The canonical/alias tables (migration 20260604) and `persistCanonicalAliases()` exist, and `lib/rfp/ingest/run.ts` ALREADY calls it after `upsertBatch()` (line ~191) — the wiring is present in this branch. The genuine FND-02 gap is the absence of a VERIFIABLE dedup test/script proving two sources collapse to one opportunity. This plan supplies that proof and regenerates types once (single owner of database.types.ts to avoid a parallel-wave collision with 14-01/02/03).
Output: A unit test, a live-DB verify script, and a regenerated database.types.ts.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-canonical-data-foundation/14-RESEARCH.md
@lib/rfp/ingest/canonicalize.ts
@lib/rfp/ingest/run.ts
@tests/unit/rfp-canonicalize.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate database.types.ts for all Phase 14 schema</name>
  <files>lib/supabase/database.types.ts</files>
  <action>
This plan is the single owner of `database.types.ts` regen for Phase 14 (14-01/02/03 deliberately deferred it here to avoid three parallel writers colliding on one file).

Run the existing project script: `npm run types:supabase` (defined in package.json: `supabase gen types typescript --project-id hgxxxmtfmvguotkowxbu --schema public > lib/supabase/database.types.ts`).

This regenerates types from the live schema, which by Wave 2 already contains: the 7 new rfp_opportunities columns (14-01), the rfp_entitlements table (14-03), and the match_vault_docs RPC + HNSW (14-02). If the supabase CLI is not authenticated in this environment, surface an auth checkpoint rather than hand-editing the file.

After regen, confirm the file compiles project-wide and that the casts added in 14-02 (retrieve.ts rpc) and 14-03 (webhook upsert) are still valid or can be simplified now that types exist (optional cleanup — keep surgical).
  </action>
  <verify>
1. `grep -n "rfp_entitlements" lib/supabase/database.types.ts` returns matches (table now typed).
2. `grep -n "naics_codes\|cfda_numbers\|funder_name" lib/supabase/database.types.ts` returns matches (FND-01 columns typed).
3. `grep -n "match_vault_docs" lib/supabase/database.types.ts` returns a match under Functions (RPC typed).
4. `npx tsc --noEmit` passes across the repo.
  </verify>
  <done>
database.types.ts regenerated from live schema; rfp_entitlements, the 7 opportunity columns, and the match_vault_docs RPC are all present; repo type-checks clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a unit test proving persistCanonicalAliases collapses two-source duplicates</name>
  <files>tests/unit/rfp-dedup-persist.test.ts</files>
  <action>
Create `tests/unit/rfp-dedup-persist.test.ts` (vitest, mirror the style of the existing tests/unit/rfp-canonicalize.test.ts). This test proves the FND-02 behavior at the unit level by mocking `createAdminClient()` and asserting the persist logic groups two OpportunityRowWithId records that share a canonical_key into ONE canonical upsert and TWO alias upserts.

Build two rows that derive the SAME canonical_key (per canonicalize.ts: e.g., both resolve to `grants_gov:353936` — one with source 'grants_gov' source_id '353936', another cross-post with source 'nsf_grants' and raw_json.id 353936). Mock the supabase admin client so `.from('rfp_opportunity_canonicals').upsert(...).select(...)` returns a single canonical row, and capture the alias payload. Assert:
- `rfp_opportunity_canonicals` upsert receives exactly ONE unique canonical_key.
- `rfp_opportunity_aliases` upsert receives TWO alias rows pointing at that one canonical id.
- Result `canonicals_seen === 1`, alias count === 2.

Use vi.mock / vi.fn to intercept createAdminClient from '@/lib/supabase/server'. No real network. No `any` — type the mock returns.
  </action>
  <verify>
`npx vitest run tests/unit/rfp-dedup-persist.test.ts` passes. The test fails if persistCanonicalAliases is changed to create two canonicals (guards FND-02 regression).
  </verify>
  <done>
A passing unit test asserts two same-key, different-source records produce one canonical and two aliases via persistCanonicalAliases.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a live-DB dedup verification script</name>
  <files>scripts/verify-rfp-dedup.ts</files>
  <action>
Create `scripts/verify-rfp-dedup.ts` (tsx one-shot, mirror style of scripts/seed-rfp-demo-org.ts and scripts/smoke-test-rfp-apis.ts; shebang `#!/usr/bin/env tsx`). This is the FND-02 success-criterion proof against the real DB: ingesting the same opportunity twice from two sources yields ONE row, not two.

Steps:
1. Use `createAdminClient()`.
2. Insert two rfp_opportunities rows representing the SAME logical opportunity from two sources (e.g. source='grants_gov' source_id='VERIFY-353936' and source='nsf_grants' source_id='VERIFY-24-569' with raw_json={ id: <same grants id> }) so deriveOpportunityCanonicalKey collapses them to one key. Capture their ids.
3. Call `persistCanonicalAliases([rowA, rowB])` (import from '@/lib/rfp/ingest/canonicalize').
4. Query: count distinct canonicals for the shared canonical_key (assert === 1) and count aliases pointing to it (assert === 2).
5. Print PASS/FAIL with the counts. Exit code 0 on PASS, 1 on FAIL.
6. CLEANUP: delete the seeded aliases, canonical, and the two opportunities rows so the script is repeatable and leaves no test rows in the CORE brain DB.

/db-safe: this writes to the shared CORE DB. Use clearly-prefixed VERIFY- source_ids and guaranteed cleanup in a finally block so no residue remains. Add an npm script `"verify:rfp-dedup": "tsx scripts/verify-rfp-dedup.ts"` to package.json.
  </action>
  <verify>
`npm run verify:rfp-dedup` exits 0 and prints "1 canonical / 2 aliases — PASS". Re-running it a second time also passes (cleanup confirmed idempotent — no leftover VERIFY- rows: `SELECT count(*) FROM rfp_opportunities WHERE source_id LIKE 'VERIFY-%'` returns 0 after run).
  </verify>
  <done>
A runnable script seeds a two-source duplicate, proves it collapses to one canonical with two aliases against the live DB, exits 0 on PASS, and cleans up all seeded rows.
  </done>
</task>

</tasks>

<verification>
- Success Criterion 2 (FND-02): Ingesting the same opportunity twice from two sources results in one row, not two — proven by both the unit test (Task 2) and the live-DB script (Task 3).
- Phase-wide type integrity: database.types.ts reflects all four requirements' schema (Task 1).
- No residue in the CORE brain DB (cleanup verified).
</verification>

<success_criteria>
- database.types.ts regenerated; rfp_entitlements + 7 opportunity columns + match_vault_docs RPC all typed; repo type-checks
- Unit test proves persistCanonicalAliases collapses two-source duplicates to 1 canonical / 2 aliases
- Live-DB verify script proves the same end-to-end and self-cleans
- `npm run verify:rfp-dedup` and the new vitest both pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-canonical-data-foundation/14-04-SUMMARY.md`. Note that run.ts already wired persistCanonicalAliases (no code wiring needed — this plan was verification + types reconciliation), and record the dedup script command.
</output>
