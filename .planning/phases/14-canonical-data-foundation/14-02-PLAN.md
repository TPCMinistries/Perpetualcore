---
phase: 14-canonical-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606_rfp_vault_hnsw_match_rpc.sql
  - lib/rfp/vault/retrieve.ts
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "pgvector version is confirmed >= 0.5.0 before the HNSW index is built"
    - "rfp_vault_artifacts.embedding is indexed with HNSW (not ivfflat), under the single name rfp_vault_artifacts_embedding_idx"
    - "SELECT match_vault_docs(org_id, query_embedding, 50) returns top-K org-scoped chunks without scanning in Node"
    - "retrieve.ts calls the RPC first and keeps the in-Node cosine fallback for local dev"
  artifacts:
    - path: "supabase/migrations/20260606_rfp_vault_hnsw_match_rpc.sql"
      provides: "Preflight version check, ivfflat→HNSW swap, match_vault_docs SECURITY DEFINER RPC"
      contains: "USING hnsw (embedding vector_cosine_ops)"
    - path: "lib/rfp/vault/retrieve.ts"
      provides: "RPC-first retrieval with in-Node fallback"
      contains: "match_vault_docs"
  key_links:
    - from: "lib/rfp/vault/retrieve.ts"
      to: "match_vault_docs RPC"
      via: "admin.rpc('match_vault_docs', { org_id, query_embedding, match_count })"
      pattern: "rpc\\(['\"]match_vault_docs"
    - from: "match_vault_docs body"
      to: "rfp_vault_artifacts"
      via: "WHERE org_id = match_vault_docs.org_id (tenant isolation inside SECURITY DEFINER)"
      pattern: "WHERE.*org_id"
---

<objective>
Replace the ivfflat index on `rfp_vault_artifacts.embedding` with an HNSW index and create the `match_vault_docs()` SECURITY DEFINER RPC so vault retrieval runs in Postgres instead of fetching all chunks and computing cosine in Node (FND-03).

Purpose: `lib/rfp/vault/retrieve.ts` explicitly documents in-Node cosine as a Phase 14 target. At >50 docs/org the current path fetches MB-scale payloads per query. The HNSW RPC is the foundation Phase 18 scoring depends on.
Output: One migration (preflight + index swap + RPC) applied to `hgxxxmtfmvguotkowxbu`, plus retrieve.ts updated to call the RPC with the existing in-Node path as fallback.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-canonical-data-foundation/14-RESEARCH.md
@lib/rfp/vault/retrieve.ts
@lib/rfp/vault/embed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight pgvector version, swap ivfflat→HNSW, create match_vault_docs RPC</name>
  <files>supabase/migrations/20260606_rfp_vault_hnsw_match_rpc.sql</files>
  <action>
PREFLIGHT (run first via supabase MCP `execute_sql`, do NOT proceed if it fails):
`SELECT extversion FROM pg_extension WHERE extname = 'vector'` — confirm >= 0.5.0 (HNSW support). Supabase managed DB is almost certainly 0.6+. If < 0.5.0, abort and surface a checkpoint — do not build the HNSW index.

Also check for an existing `match_vault_docs` function (research open question #3):
`SELECT proname, pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname = 'match_vault_docs'` — if it exists with a different signature, use CREATE OR REPLACE (the SQL below already does).

Create `supabase/migrations/20260606_rfp_vault_hnsw_match_rpc.sql` with the standard "Applied via: supabase MCP apply_migration" header. Contents:

1. Drop old ivfflat, create HNSW (pitfall #1: must DROP first or you get two indexes). Use regular CREATE INDEX (not CONCURRENTLY) — table is small pre-dogfood; add a comment noting CONCURRENTLY when >10K rows:
```sql
DROP INDEX IF EXISTS rfp_vault_artifacts_embedding_idx;
CREATE INDEX rfp_vault_artifacts_embedding_idx
  ON rfp_vault_artifacts USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);
```

2. The SECURITY DEFINER RPC with the EXACT signature FND-03 requires — `match_vault_docs(org_id uuid, query_embedding vector(1024), match_count int)`. Embedding dim is 1024 (text-embedding-3-large, confirmed in embed.ts). MUST include `SET search_path = public` (pitfall #3 — search_path injection). Tenant isolation is the internal `WHERE org_id = match_vault_docs.org_id`:
```sql
CREATE OR REPLACE FUNCTION match_vault_docs(
  org_id          uuid,
  query_embedding vector(1024),
  match_count     int DEFAULT 8
)
RETURNS TABLE (
  id uuid, body text, title text, type text,
  source_metadata jsonb, similarity float
)
LANGUAGE sql STABLE SECURITY DEFINER SET search_path = public
AS $$
  SELECT id, body, title, type, source_metadata,
         1 - (embedding <=> query_embedding) AS similarity
  FROM rfp_vault_artifacts
  WHERE rfp_vault_artifacts.org_id = match_vault_docs.org_id
    AND embedding IS NOT NULL
  ORDER BY embedding <=> query_embedding
  LIMIT match_count;
$$;
```

Apply via supabase MCP `apply_migration`, name `rfp_vault_hnsw_match_rpc`, project `hgxxxmtfmvguotkowxbu`. /db-safe applies. The DROP INDEX is on a NON-DATA object (index only) — confirm no DROP TABLE/COLUMN before applying.
  </action>
  <verify>
Via supabase MCP `execute_sql`:
1. HNSW confirmed (FND-03 success criterion — note research correction: index is on rfp_vault_artifacts, NOT rfp_opportunities): `SELECT indexname, indexdef FROM pg_indexes WHERE indexname = 'rfp_vault_artifacts_embedding_idx'` — indexdef contains `hnsw` and there is exactly ONE index on the embedding column (pitfall #1 check).
2. RPC exists with correct signature: `SELECT pg_get_function_identity_arguments(oid) FROM pg_proc WHERE proname='match_vault_docs'` returns `org_id uuid, query_embedding vector, match_count integer`.
3. Callable: `SELECT count(*) FROM match_vault_docs('00000000-0000-0000-0000-000000000000'::uuid, array_fill(0, ARRAY[1024])::vector(1024), 50)` returns without error (0 rows is fine — proves the RPC executes the HNSW path in Postgres).
  </verify>
  <done>
pgvector >= 0.5.0 confirmed; the ivfflat index is gone and a single HNSW index named rfp_vault_artifacts_embedding_idx exists; match_vault_docs RPC exists with the exact (org_id uuid, query_embedding vector(1024), match_count int) signature, is SECURITY DEFINER with SET search_path=public, and is callable returning org-scoped results.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire retrieve.ts to call match_vault_docs with in-Node cosine fallback</name>
  <files>lib/rfp/vault/retrieve.ts</files>
  <action>
Update `lib/rfp/vault/retrieve.ts` so the primary retrieval path calls the new RPC instead of fetching all chunks and computing cosine in Node. Keep the existing in-Node cosine implementation as a documented fallback (for local dev / when the RPC is unavailable) — do NOT delete it.

Surgical change: after embedding the query (`qvec`), call:
```typescript
const { data, error } = await admin.rpc("match_vault_docs", {
  org_id: orgId,
  query_embedding: qvec,        // number[] length 1024
  match_count: k,
});
```
Map the returned rows (which include a `similarity` column) into the existing `RetrievedChunk` shape (the codebase maps similarity → similarity_score). If `error` is non-null, log it and fall through to the existing in-Node cosine block so behavior degrades gracefully rather than throwing. No `any` — type the RPC result rows explicitly (id, body, title, type, source_metadata, similarity). Match existing code style; touch only the retrieval function.
  </action>
  <verify>
1. `npm run lint` passes (no new `any`, no unused imports).
2. `npx tsc --noEmit` passes (note: database.types.ts won't yet include the RPC types until Plan 14-04 regen; if the typed `.rpc()` call errors only on the unknown RPC name, cast the rpc arg object as the existing codebase does for other untyped calls, and leave a comment that 14-04 regen will type it).
3. `grep -n "match_vault_docs" lib/rfp/vault/retrieve.ts` shows the RPC call; `grep -n "cosineSimilarity" lib/rfp/vault/retrieve.ts` confirms the fallback is retained.
  </verify>
  <done>
retrieve.ts calls match_vault_docs as the primary path, maps similarity into RetrievedChunk, falls back to in-Node cosine on RPC error, retains the cosine helper, lints and type-checks clean.
  </done>
</task>

</tasks>

<verification>
- Success Criterion 3 (FND-03): `SELECT match_vault_docs(org_id, query_embedding, 50)` returns results without scanning in Node; HNSW index confirmed via `pg_indexes` on `rfp_vault_artifacts_embedding_idx` (research-corrected name — the roadmap's `rfp_opportunities_embedding_idx` is a known naming error; document this in the SUMMARY).
- Preflight: pgvector >= 0.5.0 confirmed before index build.
- Tenant isolation: RPC enforces `WHERE org_id = $1` inside SECURITY DEFINER + SET search_path=public.
</verification>

<success_criteria>
- pgvector version verified >= 0.5.0
- Single HNSW index on rfp_vault_artifacts.embedding (no leftover ivfflat)
- match_vault_docs RPC live with exact required signature, SECURITY DEFINER, search_path pinned, callable
- retrieve.ts uses the RPC with in-Node fallback retained
- SUMMARY documents the roadmap index-name discrepancy (rfp_opportunities_embedding_idx → rfp_vault_artifacts_embedding_idx)
</success_criteria>

<output>
After completion, create `.planning/phases/14-canonical-data-foundation/14-02-SUMMARY.md`. Record the confirmed pgvector version and the index-name correction.
</output>
