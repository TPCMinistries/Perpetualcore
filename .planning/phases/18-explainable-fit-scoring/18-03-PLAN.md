---
phase: 18-explainable-fit-scoring
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - lib/rfp/scoring/summary.ts
  - lib/rfp/scoring/recompute.ts
  - app/api/rfp/opps/[id]/rescore/route.ts
  - lib/supabase/database.types.ts
autonomous: true
requirements: [SCORE-01, SCORE-02, SCORE-03]
must_haves:
  truths:
    - "Scoring retrieves up to 5 vault chunks for each (opp, org) pair and routes the embed+LLM block through guardedLLMCall"
    - "The AI summary cites vault artifacts by id/name and returns structured citations (graceful plain-text fallback on JSON parse failure)"
    - "score_breakdown is extended with dimensions + disqualifiers + vault_hit_count + scored_at_v2 sentinel"
    - "rfp_fit_evidence rows are written for the cited chunks per dimension"
    - "An on-demand POST /api/rfp/opps/[id]/rescore re-runs v2 scoring for a single (opp, org) pair"
    - "Over-budget orgs are silently skipped (BudgetExceededError) without failing the run"
  artifacts:
    - path: "lib/rfp/scoring/summary.ts"
      provides: "generateExplainedSummary returning text + cited artifacts + cost meta"
      contains: "cited_artifact"
    - path: "lib/rfp/scoring/recompute.ts"
      provides: "shared scoreOnePair path with vault retrieval + disqualifiers + evidence upsert"
      contains: "retrieveVaultChunks"
    - path: "app/api/rfp/opps/[id]/rescore/route.ts"
      provides: "on-demand single-pair v2 rescore endpoint"
      exports: ["POST"]
  key_links:
    - from: "lib/rfp/scoring/recompute.ts"
      to: "guardedLLMCall"
      via: "wraps retrieve+summarize block"
      pattern: "guardedLLMCall"
    - from: "lib/rfp/scoring/recompute.ts"
      to: "rfp_fit_evidence"
      via: "upsertFitEvidence from evidence-store"
      pattern: "upsertFitEvidence"
    - from: "app/api/rfp/opps/[id]/rescore/route.ts"
      to: "lib/rfp/scoring/recompute.ts"
      via: "calls shared scoreOnePair"
      pattern: "scoreOnePair|recompute"
---

<objective>
Wire the Phase 18 vault-grounding into the scoring pipeline: retrieve vault evidence, generate a cited summary, persist the extended breakdown + evidence rows, and expose an on-demand rescore endpoint for the demo/detail path. This is the integration plan — it consumes the table from 18-01 and the pure functions from 18-02.

Purpose: Delivers SCORE-01 (plain-English why), SCORE-02 (cited vault artifacts), and SCORE-03 (dimension breakdown persisted) end-to-end through the cron AND on-demand paths. All LLM/embed cost is captured via the Phase 17 guardrail.
Output: Extended summary.ts + recompute.ts, new rescore route, regenerated database.types.ts.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-explainable-fit-scoring/18-RESEARCH.md
@.planning/phases/18-explainable-fit-scoring/18-01-SUMMARY.md
@.planning/phases/18-explainable-fit-scoring/18-02-SUMMARY.md

# Files this plan extends — read current shape before editing:
@lib/rfp/scoring/summary.ts
@lib/rfp/scoring/recompute.ts
@lib/rfp/ai/guardrail.ts
@lib/rfp/vault/retrieve.ts
@app/api/rfp/opps/[id]/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend summary.ts with cited, structured output</name>
  <files>lib/rfp/scoring/summary.ts</files>
  <action>
Add a new exported function alongside the existing `generateFitSummary` (do NOT delete the old one — recompute may still call it on the empty-vault path):

```ts
export interface ExplainedSummaryResult extends FitSummaryResult {
  cited_artifact_ids: string[];
  cited_excerpts: Array<{ artifact_id: string; excerpt: string; artifact_title: string }>;
}
export async function generateExplainedSummary(
  opp, profile, dimensions, vaultChunks
): Promise<ExplainedSummaryResult>
```

Behavior (per 18-RESEARCH.md Pattern 4 + Pitfall 4):
- Reuse the SAME model selection already in this file (claude-sonnet-4-5 primary / claude-haiku-4-5 fallback) — do NOT introduce a new model (would log $0 cost; see Anti-Patterns).
- Build a prompt that includes the 5 dimension sub-scores + each vault chunk (doc_title, doc_id, short text). Instruct Claude to return JSON ONLY: { "text": "...", "cited_artifact_ids": [...], "cited_excerpts": [{artifact_id, excerpt(<=120 chars), artifact_title}] }.
- Parse with JSON.parse inside try/catch. On failure, fall back to: text = first text block, cited_artifact_ids = [], cited_excerpts = []. Log with console.warn (non-fatal) per Pitfall 4.
- When `vaultChunks.length === 0`, skip citation instructions and produce a plain summary with empty citation arrays (empty-vault degrade — Pitfall 1).
- Return tokensIn/tokensOut/costUsd computed via the existing `computeCostUsd` import already used by generateFitSummary, so the shape extends FitSummaryResult exactly.

Do NOT call guardedLLMCall inside this function — the caller wraps it (keeps the function reusable and testable).
  </action>
  <verify>`npx tsc --noEmit` clean. Grep confirms `generateExplainedSummary`, `cited_artifact_ids`, a `try`/`catch` around `JSON.parse`, and reuse of the existing model constant (no new model string). `npm run lint` passes.</verify>
  <done>generateExplainedSummary returns structured cited output, degrades to plain text on parse failure and on empty vault, and computes cost via the existing helper.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared scoreOnePair v2 path in recompute.ts (vault + disqualifiers + evidence)</name>
  <files>lib/rfp/scoring/recompute.ts</files>
  <action>
Extract/extend a shared helper so BOTH the cron path (`scoreNewOpportunitiesForAllActiveOrgs`) and the per-org path (`recomputeAllForOrg`) and the new rescore endpoint use ONE function (per 18-RESEARCH.md Pitfall 5):

```ts
export async function scoreOnePairV2(opp, org, profile): Promise<{ row, evidenceRows, skippedBudget: boolean }>
```

Steps inside (18-RESEARCH.md Extended Flow):
1. base = scoreOpportunity(opp, profile)  (unchanged deterministic call)
2. disqualifiers = checkDisqualifiers(opp, org)  (import from ./disqualifiers — Plan 02)
3. Wrap the embed+retrieve+summarize block in ONE guardedLLMCall(orgId, async () => {...}) so vault embed cost is captured (per 18-RESEARCH.md Note on embedding cost + Anti-Patterns). Inside:
   - chunks = await retrieveVaultChunks(orgId, `${opp.title} ${opp.agency ?? ''} ${(opp.brief ?? '').slice(0,300)}`, { k: 5 })
   - dims = mapToDimensions(base, chunks.length, disqualifiers)  (import from ./dimensions — Plan 02)
   - summary = await generateExplainedSummary(opp, profile, dims, chunks)
   - return an object extending LLMCallMeta: { agent: 'scoring_v2', model: <model used>, tokensIn, tokensOut, costUsd, _summary: summary, _dims: dims, _chunks: chunks }
   NOTE: 'scoring_v2' agent label — confirm RFP_MODEL_RATES already covers claude-sonnet-4-5/claude-haiku-4-5 (it does). At beachhead scale (<=10 orgs) per-pair embedding is acceptable; add a `// TODO: cache opp embedding at N>20 orgs` comment (18-RESEARCH.md Open Q1) — do NOT pre-optimize.
4. Catch BudgetExceededError exactly like recompute.ts:437 — set skippedBudget true, summary/dims still computed from base WITHOUT the LLM (use a deterministic fallback summary string and mapToDimensions with vaultHitCount 0), so chips/scores still upsert. Re-throw non-budget errors.
5. Build the extended score_breakdown (ScoreBreakdownV2 — 18-RESEARCH.md Pattern 6): spread base breakdown + { dimensions: dims, disqualifiers, vault_hit_count: chunks.length, scored_at_v2: new Date().toISOString() }.
6. Build evidenceRows: for each cited chunk, one FitEvidenceRow per relevant dimension (use cited_excerpts mapping to artifact_id/title; similarity from the chunk). Truncate excerpt to 200 chars (store helper also truncates — belt and suspenders).
7. Return { row (for upsertMatches: opp_id, org_id, fit_score, score_breakdown, chips, summary text, scored_version+1), evidenceRows, skippedBudget }.

8. **CRITICAL — extend the source `.select()` queries so disqualifier inputs are actually fetched** (without this, SCORE-04 passes unit tests but silently NEVER fires in production):
   - `rfp_opportunities` is selected in TWO places — the cron path (recompute.ts:135-137) and the per-org path (recompute.ts:542-544). Both currently select `'id, source, title, agency, amount_min, amount_max, deadline, brief, keywords, geo'`. Extend BOTH selects to also include `set_aside_code, eligibility_types, naics_codes`.
   - `rfp_orgs` is selected inside `loadLatestProfile` at recompute.ts:202-203 as `.select('id, naics')`. Extend it to `.select('id, naics, type')` so `org.type` is available to `checkDisqualifiers`. Thread `type` through whatever org/profile object is passed to `scoreOnePairV2`.
   - Update the corresponding TypeScript input types (`OpportunityForScoring` is in score.ts — do not modify it; instead define a local `OppForDisqualifier`/extend the row shape read here) so `set_aside_code`, `eligibility_types`, `naics_codes`, and `org.type` are typed and reach `checkDisqualifiers`.

Then: in `scoreNewOpportunitiesForAllActiveOrgs` AND `recomputeAllForOrg`, replace the inline summary call with `scoreOnePairV2`, collect rows, call existing `upsertMatches(rows)`, and after upsert call `upsertFitEvidence(allEvidenceRows)` (import from ./evidence-store — Plan 01). Keep the existing asyncPool concurrency pattern.

**CRITICAL — `recomputeAllForOrg` MUST also route through `scoreOnePairV2`.** Today it calls `generateFitSummary` WITHOUT `guardedLLMCall` (see the intentional comment near recompute.ts:590). After this task, BOTH paths use `scoreOnePairV2`, so the per-org path is budget-guarded too. Delete/replace the line-590 ungated call and its explanatory comment; do not leave a second ungated LLM path.
  </action>
  <verify>`npx tsc --noEmit` clean and `npm run lint` passes. Grep confirms `scoreOnePairV2`, `retrieveVaultChunks`, `checkDisqualifiers`, `mapToDimensions`, `upsertFitEvidence`, and a `BudgetExceededError` catch that sets a skip flag (not a throw). Confirm the select fields landed: `grep -n "set_aside_code, eligibility_types, naics_codes" lib/rfp/scoring/recompute.ts` returns TWO hits (cron + per-org), and `grep -n "id, naics, type" lib/rfp/scoring/recompute.ts` returns at least one. Confirm the per-org path is guarded with a function-scoped grep: `grep -A40 "export async function recomputeAllForOrg" lib/rfp/scoring/recompute.ts | grep -E "scoreOnePairV2|guardedLLMCall"` returns a match, and `grep -A40 "export async function recomputeAllForOrg" lib/rfp/scoring/recompute.ts | grep "generateFitSummary"` returns NOTHING (the old ungated call is gone).</verify>
  <done>One shared `scoreOnePairV2` produces the extended breakdown + evidence; BOTH the cron and per-org paths call it (per-org is now budget-guarded — no ungated `generateFitSummary` remains); the `rfp_opportunities` selects (both sites) fetch `set_aside_code, eligibility_types, naics_codes` and the `rfp_orgs` select fetches `type`, so `checkDisqualifiers` receives real data and SCORE-04 fires in production; budget-exceeded orgs skip the LLM but still upsert scores; evidence is persisted.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate Supabase types, then build the on-demand rescore endpoint</name>
  <files>lib/supabase/database.types.ts, app/api/rfp/opps/[id]/rescore/route.ts</files>
  <action>
Do these in order — regen the types FIRST so the new route is written against the typed `rfp_fit_evidence` columns.

STEP A — Regenerate Supabase types (this plan is the single owner of the Phase 18 types regen, matching the Phase 14-04 / 17 convention of one owner to avoid parallel-wave collision):
- The canonical types file is `lib/supabase/database.types.ts` (NOT a root `lib/database.types.ts` — that path does not exist; recompute.ts references confirm `lib/supabase/database.types.ts`).
- Run the existing package.json script `npm run types:supabase` (it runs `supabase gen types typescript --project-id hgxxxmtfmvguotkowxbu --schema public > lib/supabase/database.types.ts`). Do NOT hand-edit the file. Note: this targets the LDC Brain CORE project — /db-safe is read-only here (codegen only), so it is safe.
- This requires the 18-01 migration to be APPLIED to the project first (so `rfp_fit_evidence` exists in the schema the generator reads). If it has not been applied, apply the 18-01 migration via the normal supabase migration flow before running the generator.
- Remove any temporary `as unknown as` casts in `lib/rfp/scoring/evidence-store.ts` (Plan 01) that the regenerated types now make unnecessary.

STEP B — Create `app/api/rfp/opps/[id]/rescore/route.ts` exporting `POST` (18-RESEARCH.md Open Q2 — this is the demo path):
- Follow the dual-client pattern used across rfp routes (see app/api/rfp/opps/[id]/route.ts): createClient() to authenticate the user + resolve org membership via rfp_my_org_ids / existing helper; createAdminClient() for the actual scoring writes. NO service-role client in the user-auth gate (TRUST-02 convention; STATE.md 22-02).
- Validate input with Zod: body or query must include org_id (the org to score for); 400 on missing.
- Authorize: confirm the caller is a member of org_id (reuse the membership check already in the GET route). 403 otherwise.
- Load the opp + org + latest profile (reuse loadLatestProfile), call `scoreOnePairV2`, upsert the match row + evidence via the same helpers.
- On BudgetExceededError surfacing to the route, return `budgetExceededResponse(err)` (402) from guardrail.ts — actionable message, not a 500 (Phase 17 success criterion 3 pattern).
- Return 200 JSON with the new fit_score + dimensions + cited artifacts so the DetailPane (Plan 04) can refresh optimistically.
  </action>
  <verify>
STEP A: Confirm `rfp_fit_evidence` is present in `lib/supabase/database.types.ts` (`grep -c "rfp_fit_evidence" lib/supabase/database.types.ts` > 0). Confirm no leftover `as unknown as` cast remains in evidence-store.ts that the typed table replaces.
STEP B: Grep confirms `export async function POST` in the rescore route, the dual-client pattern (createClient auth gate + createAdminClient writes), Zod validation of org_id, and `budgetExceededResponse` on the 402 path.
Whole repo: `npx tsc --noEmit` clean and `npm run build` exits 0.
  </verify>
  <done>
(a) `npm run types:supabase` regenerates `lib/supabase/database.types.ts` cleanly and `rfp_fit_evidence` appears in it; temporary casts in evidence-store.ts are removed.
(b) `POST /api/rfp/opps/[id]/rescore` re-scores a single pair with full auth (Zod org_id, membership 403), budget handling (402 via budgetExceededResponse), and returns the fresh v2 score; repo type-checks and builds.
  </done>
</task>

</tasks>

<verification>
- Cron and per-org paths both call the shared scoreOnePairV2 (no divergent v1 summary path remains except the deliberate budget/empty-vault fallback).
- A successful run writes extended score_breakdown (with dimensions, disqualifiers, scored_at_v2) AND rfp_fit_evidence rows.
- POST /api/rfp/opps/[id]/rescore returns a fresh v2 score; over-budget returns 402 (not 500); embed+LLM cost captured via guardedLLMCall.
- `npm run build` exits 0; `lib/supabase/database.types.ts` typed for the new `rfp_fit_evidence` table.
</verification>

<success_criteria>
- SCORE-01: every scored opp carries a plain-English summary explaining fit.
- SCORE-02: the summary cites vault artifacts by name, persisted in rfp_fit_evidence.
- SCORE-03: score_breakdown carries the 5 labeled dimensions with sub-scores.
- All LLM/embed cost is guarded; budget-exceeded orgs degrade gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/18-explainable-fit-scoring/18-03-SUMMARY.md`.
</output>
