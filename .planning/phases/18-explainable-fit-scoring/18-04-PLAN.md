---
phase: 18-explainable-fit-scoring
plan: 04
type: execute
wave: 3
depends_on: ["18-03"]
files_modified:
  - app/api/rfp/opps/[id]/route.ts
  - app/(dashboard)/org/[orgId]/discovery/parts/DetailPane.tsx
  - app/(dashboard)/org/[orgId]/discovery/parts/FitReasoningPanel.tsx
  - app/(dashboard)/org/[orgId]/discovery/parts/VaultCitation.tsx
autonomous: false
requirements: [SCORE-01, SCORE-02, SCORE-03, SCORE-04]
must_haves:
  truths:
    - "GET /api/rfp/opps/[id] returns the v2 dimensions + disqualifiers + cited evidence alongside the existing score/summary"
    - "Clicking an opportunity shows a fit score with a plain-English why (SCORE-01)"
    - "The detail pane lists the 5 labeled dimensions each with a sub-score/rating (SCORE-03)"
    - "Cited vault artifacts render by name with an excerpt preview (SCORE-02)"
    - "When a disqualifier applies, a flagged warning is visible (SCORE-04)"
    - "Pre-v2 rows (no scored_at_v2) degrade gracefully and offer a Rescore action"
  artifacts:
    - path: "app/(dashboard)/org/[orgId]/discovery/parts/FitReasoningPanel.tsx"
      provides: "5-dimension breakdown + disqualifier flags + Rescore button"
      min_lines: 40
    - path: "app/(dashboard)/org/[orgId]/discovery/parts/VaultCitation.tsx"
      provides: "cited artifact name + excerpt chip"
      min_lines: 15
  key_links:
    - from: "app/(dashboard)/org/[orgId]/discovery/parts/DetailPane.tsx"
      to: "FitReasoningPanel.tsx"
      via: "renders panel above AI summary section"
      pattern: "FitReasoningPanel"
    - from: "app/(dashboard)/org/[orgId]/discovery/parts/FitReasoningPanel.tsx"
      to: "/api/rfp/opps/[id]/rescore"
      via: "POST on Rescore click"
      pattern: "rescore"
    - from: "app/api/rfp/opps/[id]/route.ts"
      to: "rfp_fit_evidence"
      via: "joins/selects evidence for the (opp, org)"
      pattern: "rfp_fit_evidence"
---

<objective>
Surface the explainable fit score in the opportunity detail UI: extend the GET endpoint to return v2 dimensions + disqualifiers + cited evidence, then render a Fit Reasoning panel with the 5 dimensions, disqualifier warnings, and named vault citations. Add a Rescore action for pre-v2 rows.

Purpose: This is the user-facing payoff for all four SCORE requirements. It depends on the data shape produced by Plan 03. Ends with a human-verify checkpoint because the success criteria are visual/functional.
Output: Extended GET route + DetailPane + two new presentational components.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-explainable-fit-scoring/18-RESEARCH.md
@.planning/phases/18-explainable-fit-scoring/18-03-SUMMARY.md

# Integration points — read current shape before editing:
@app/api/rfp/opps/[id]/route.ts
@app/(dashboard)/org/[orgId]/discovery/parts/DetailPane.tsx
@app/(dashboard)/org/[orgId]/discovery/parts/FitScoreChip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GET /api/rfp/opps/[id] to return v2 reasoning + evidence</name>
  <files>app/api/rfp/opps/[id]/route.ts</files>
  <action>
Extend the existing GET handler (do not change its auth/dual-client pattern). In addition to the current score_breakdown/chips/summary/enrichment payload:
- Read `dimensions`, `disqualifiers`, `vault_hit_count`, `scored_at_v2` out of the existing `score_breakdown` JSONB (no schema change — they were written by Plan 03).
- Fetch cited evidence: select from `rfp_fit_evidence` where opp_id = id and org_id = <caller org> and scored_version = the match row's scored_version, ordered by similarity desc. Use the existing admin/RLS read pattern already in this route. Group by dimension.
- Return a new top-level `fit_reasoning` object: { scored_v2: boolean (scored_at_v2 != null), dimensions, disqualifiers, evidence: groupedByDimension }.
- Keep backward compatibility: when scored_at_v2 is absent, return fit_reasoning: { scored_v2: false, dimensions: null, disqualifiers: [], evidence: [] } so the UI can show the Rescore CTA.
  </action>
  <verify>`npx tsc --noEmit` clean. Grep confirms the route selects from `rfp_fit_evidence` and returns a `fit_reasoning` key. Manually hit the route (or write a quick curl/script against a known opp+org) and confirm JSON includes fit_reasoning with dimensions when scored_at_v2 is set.</verify>
  <done>GET returns fit_reasoning (dimensions + disqualifiers + cited evidence) for v2 rows and a scored_v2:false stub for pre-v2 rows.</done>
</task>

<task type="auto">
  <name>Task 2: Build VaultCitation + FitReasoningPanel components</name>
  <files>app/(dashboard)/org/[orgId]/discovery/parts/VaultCitation.tsx, app/(dashboard)/org/[orgId]/discovery/parts/FitReasoningPanel.tsx</files>
  <action>
VaultCitation.tsx (presentational, no fetching):
- Props: { artifactTitle: string; artifactType: string; excerpt: string; similarity?: number }.
- Render the artifact name prominently, a small type badge, and the excerpt in a quoted/muted style. Match the existing Tailwind/mono-label aesthetic in DetailPane (font-mono uppercase tracking labels, zinc palette) — see DetailPane.tsx line ~727 section styling.

FitReasoningPanel.tsx:
- Props: { oppId: string; orgId: string; fitReasoning: <type from Task 1>; onRescored?: () => void }.
- If fitReasoning.scored_v2 is false: render a muted empty state with a "Score this opportunity" button that POSTs to `/api/rfp/opps/[oppId]/rescore?org_id=[orgId]`, shows a loading state, and calls onRescored (or re-fetches) on success. On 402 budget response, show the budget-exceeded message (read message from JSON) — do NOT show a generic error.
- If scored_v2 true: render the 5 labeled dimensions (mission fit, eligibility, track record, capacity, funder relationship) each with its sub_score as a bar or rating + the dimension label.
- Render disqualifier flags as warning chips (amber/red) with the flag label when disqualifiers.length > 0 (SCORE-04 visible warning).
- Under each dimension that has evidence, render VaultCitation for each cited artifact (SCORE-02). Handle the empty-evidence case (no vault docs) with a quiet "No vault evidence cited" line — never crash.
  </action>
  <verify>`npx tsc --noEmit` clean. `npm run lint` passes. Grep confirms FitReasoningPanel renders all 5 dimension keys, maps `disqualifiers`, renders `VaultCitation`, and POSTs to the rescore route with a 402-aware branch.</verify>
  <done>Two components exist: dimensions + disqualifier warnings + named vault citations render; pre-v2 shows a Rescore CTA with budget-aware handling.</done>
</task>

<task type="auto">
  <name>Task 3: Wire FitReasoningPanel into DetailPane</name>
  <files>app/(dashboard)/org/[orgId]/discovery/parts/DetailPane.tsx</files>
  <action>
In DetailPane.tsx, consume the new `fit_reasoning` field from the existing GET /api/rfp/opps/[id] fetch (the pane already fetches this — see research line 401). Render `<FitReasoningPanel .../>` immediately BEFORE the existing "AI summary" section (~line 727), passing oppId, orgId, and fit_reasoning. Keep the existing fit score + chips + AI summary rendering intact (the plain-English summary already satisfies SCORE-01; the panel adds SCORE-02/03/04). Wire onRescored to re-trigger the pane's existing fetch so the panel refreshes after an on-demand rescore. Make only surgical additions — do not restructure unrelated parts of the pane.
  </action>
  <verify>`npx tsc --noEmit` clean. `npm run build` exits 0. Grep confirms `FitReasoningPanel` is imported and rendered in DetailPane and that the existing AI summary section still renders after it.</verify>
  <done>DetailPane renders the Fit Reasoning panel above the AI summary, refreshing after rescore; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of explainable fit scoring UI</name>
  <what-built>
End-to-end explainable fit scoring in the discovery detail pane: a fit score + plain-English summary (SCORE-01), the 5-dimension breakdown with sub-scores (SCORE-03), named vault citations with excerpts (SCORE-02), and disqualifier warning chips (SCORE-04) — plus an on-demand Rescore button for pre-v2 rows.
  </what-built>
  <how-to-verify>
1. `npm run dev`, log in, open the discovery feed for an org that has at least one vault artifact uploaded.
2. Click an opportunity. CONFIRM: a fit score (0-100) shows with a plain-English summary of why the org fits (SCORE-01).
3. CONFIRM: the Fit Reasoning panel shows 5 labeled dimensions (mission fit, eligibility, track record, capacity, funder relationship), each with a sub-score/rating (SCORE-03).
4. CONFIRM: at least one cited vault artifact appears by name with an excerpt preview (SCORE-02). (If the org has no vault docs, instead confirm the panel shows "No vault evidence cited" gracefully and does not crash.)
5. SCORE-04 ineligible test (REQUIRED — deterministic seed, do not skip):
   a. Pick a known opportunity currently visible for a NONPROFIT org and copy its id. Capture the ORIGINAL value first so it can be restored:
      `SELECT id, set_aside_code FROM rfp_opportunities WHERE id = '<opp-id>';`  (note the returned set_aside_code — it may be NULL).
   b. Seed the disqualifier against the RFP Supabase project (LDC Brain CORE, project-id hgxxxmtfmvguotkowxbu — /db-safe applies; this is a single-row targeted UPDATE on rfp_opportunities, which is internal/no-user-data):
      `UPDATE rfp_opportunities SET set_aside_code = 'SBA' WHERE id = '<opp-id>' RETURNING id, set_aside_code;`
   c. Rescore that exact opp for the nonprofit org via the 18-03 endpoint: `POST /api/rfp/opps/<opp-id>/rescore?org_id=<nonprofit-org-id>` (or click the in-UI Rescore button on that opp).
   d. CONFIRM: a disqualifier warning chip renders on that opp's Fit Reasoning panel flagging entity-type ineligibility (set-aside restricts to small businesses; nonprofit ineligible).
   e. ROLLBACK (REQUIRED): restore the original value captured in (a):
      `UPDATE rfp_opportunities SET set_aside_code = <original-value-or-NULL> WHERE id = '<opp-id>';`  — use `NULL` literal if the original was NULL.
6. If the opp shows a "Score this opportunity" button (pre-v2 row), click it. CONFIRM: it scores and the panel populates; if the org is over AI budget, CONFIRM a clear budget message appears (not a generic 500).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (e.g. dimension missing, citation not rendering, disqualifier not flagged).</resume-signal>
  <action>Manual human verification only — Claude performs no automated work in this task; it presents the running UI for the user to confirm the four SCORE criteria render correctly. Follow the how-to-verify steps above.</action>
  <verify>User confirms all six how-to-verify checks pass. Step 5 is mandatory: the operator MUST run the seed UPDATE (set_aside_code='SBA' on a nonprofit org's opp), rescore, see the disqualifier chip render, then run the rollback UPDATE to restore the original value. SCORE-04 is not considered verified unless the chip rendered against the seeded ineligible opp.</verify>
  <done>User types "approved" after confirming: fit score + why (SCORE-01), 5 dimensions (SCORE-03), a named vault citation (SCORE-02), AND — via the required seed/rescore/rollback in step 5 — a disqualifier warning chip rendered on the seeded ineligible opp (SCORE-04). The seeded set_aside_code value has been rolled back to its original.</done>
</task>

</tasks>

<verification>
- GET /api/rfp/opps/[id] returns fit_reasoning with dimensions, disqualifiers, and cited evidence.
- DetailPane shows score + summary (SCORE-01), 5 dimensions (SCORE-03), named citations (SCORE-02), and disqualifier warnings (SCORE-04).
- The ineligible-opp test produces a visible disqualifier flag.
- Rescore button works and handles the 402 budget case with an actionable message.
- `npm run build` exits 0.
</verification>

<success_criteria>
All four Phase 18 success criteria are demonstrable in the UI: fit score + why, cited vault artifact, 5-dimension breakdown, and a disqualifier flag on an ineligible opportunity.
</success_criteria>

<output>
After completion, create `.planning/phases/18-explainable-fit-scoring/18-04-SUMMARY.md`.
</output>
