---
phase: 13-pre-work-stabilization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-pre-work-stabilization/13-02-DRIFT-TRIAGE.md
requirements: [LAUNCH-04]
autonomous: true

must_haves:
  truths:
    - "Every open row in rfp_source_drift has a classification (stale-URL / parser-break / upstream-empty / transient) and a disposition (resolved or triaged with owner + ETA)"
    - "Drift events that are confirmed stale/duplicate/already-fixed are marked resolved (resolved_at set) via a service-role write"
    - "Remaining unresolved drift events have a recorded owner and ETA in a triage register"
    - "The open_drift_events count surfaced by /api/health/rfp reflects the post-triage state"
  artifacts:
    - path: .planning/phases/13-pre-work-stabilization/13-02-DRIFT-TRIAGE.md
      provides: "A triage register: one row per drift event with source, reason, classification, disposition, owner, ETA"
      min_lines: 20
  key_links:
    - from: rfp_source_drift (resolved_at)
      to: app/api/health/rfp/route.ts open_drift_events count
      via: "service-role UPDATE setting resolved_at on triaged events lowers the live open-drift count"
      pattern: "rfp_source_drift"
---

<objective>
Triage all 18 open `rfp_source_drift` events: query the live table (read-only first, per /db-safe), classify each event (stale URL vs parser break vs upstream-empty vs transient fetch error), resolve the quick/confirmed-stale ones with a service-role write that sets `resolved_at`, and record owner + ETA for any that need a real parser fix. Produce a triage register so nothing is left as an undiagnosed open event.

Purpose: 18 open drift events at ~211 indexed opportunities is the live symptom of silent parser drift (PITFALLS Pitfall 1). The codebase cannot be called "safe to build on" with unexplained drift accumulating. We resolve or assign every one before discovery expansion (Phase 15).
Output: rfp_source_drift open-count reduced to only genuinely-actionable events, each with an owner + ETA, recorded in a register.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@supabase/migrations/20260510_rfp_state_city_drift.sql
@app/api/health/rfp/route.ts
@app/api/cron/rfp-discovery-state-city/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query and classify all open drift events (READ-ONLY first)</name>
  <files>.planning/phases/13-pre-work-stabilization/13-02-DRIFT-TRIAGE.md</files>
  <action>
    Activate /db-safe. Target database: LDC Brain AI (project hgxxxmtfmvguotkowxbu) — this is the `supabase` MCP per CLAUDE.md, where the RFP schema lives. VERIFY the project before writing: run `mcp__supabase__list_tables` (or a read query) and confirm `rfp_source_drift` exists in this project. If it does not, STOP and report which project actually holds the RFP schema — do not guess.
    rfp_source_drift has service-only RLS (USING(false)); the MCP service-role connection bypasses RLS, so queries work via the MCP.
    Run READ-ONLY:
      SELECT source, reason, details, created_at, resolved_at
      FROM rfp_source_drift
      WHERE resolved_at IS NULL
      ORDER BY source, created_at DESC;
    For each open event, classify by `reason` (the schema defines: 'zero_nodes' | 'http_status' | 'shape_mismatch' | 'fetch_error' | 'count_anomaly') plus the `details` jsonb:
      - stale-URL / dead source  → source endpoint moved or 404 (http_status 404/410)
      - parser-break             → shape_mismatch / zero_nodes on a source that should return data
      - upstream-empty           → zero_nodes where the source legitimately had no new postings
      - transient                → fetch_error / 5xx http_status, likely already self-recovered (check if a later successful baseline row exists for that source in rfp_source_baseline)
    Write the triage register to 13-02-DRIFT-TRIAGE.md as a table: | id | source | reason | classification | likely cause | disposition (resolve/keep-open) | owner | ETA |.
  </action>
  <verify>13-02-DRIFT-TRIAGE.md exists and contains one row per open drift event (expected ~18) each with a classification and disposition; the SELECT result is pasted into the summary</verify>
  <done>All open drift events are classified; each has a disposition of either "resolve now" or "keep-open with owner + ETA".</done>
</task>

<task type="auto">
  <name>Task 2: Resolve confirmed-stale events and record owner+ETA for the rest</name>
  <files>.planning/phases/13-pre-work-stabilization/13-02-DRIFT-TRIAGE.md</files>
  <action>
    For events classified as transient (a later successful rfp_source_baseline row exists for that source), upstream-empty, or duplicates of an already-fixed drift, mark them resolved via a service-role write through the `supabase` MCP:
      UPDATE rfp_source_drift
      SET resolved_at = now(),
          details = details || jsonb_build_object('triage', '<classification>', 'triaged_by', 'phase-13-stabilization', 'note', '<reason resolved>')
      WHERE id = '<id>';
    Do this per-id (do NOT blanket-resolve all rows). Keep the `details` jsonb append so the resolution reason is auditable.
    For events classified as parser-break or stale-URL that need a real code fix (a per-source parser change in lib/rfp or app/api/cron/rfp-discovery-state-city), DO NOT attempt the fix here — that is feature work for Phase 15. Instead record in 13-02-DRIFT-TRIAGE.md: the affected source, the specific failure, an owner (default: Lorenzo / Phase 15 ingest work), and an ETA tied to Phase 15. This satisfies "triaged with a clear owner and ETA" from the success criteria.
    Re-run the open-count query afterward and record the before/after open count in the register.
  </action>
  <verify>SELECT count(*) FROM rfp_source_drift WHERE resolved_at IS NULL returns a number lower than the starting 18 (or, if all 18 require parser fixes, equal to 18 but each row has an owner+ETA in the register); /api/health/rfp open_drift_events reflects the new count</verify>
  <done>Every one of the 18 open drift events is either resolved (resolved_at set, reason in details) or carries an explicit owner + ETA in the triage register. No drift event is left undiagnosed.</done>
</task>

</tasks>

<verification>
1. `SELECT source, reason, resolved_at FROM rfp_source_drift ORDER BY created_at DESC` — every formerly-open event is either resolved or appears in the triage register with owner+ETA.
2. 13-02-DRIFT-TRIAGE.md lists all 18 events with classification + disposition.
3. /api/health/rfp `open_drift_events` matches the post-triage open count.
</verification>

<success_criteria>
All 18 open source-drift events are resolved or triaged with a clear owner and ETA. (Phase Success Criterion 4)
</success_criteria>

<output>
After completion, create `.planning/phases/13-pre-work-stabilization/13-02-SUMMARY.md` with before/after open-drift counts and a link to the triage register.
</output>
