---
phase: 13-pre-work-stabilization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260606_rfp_api_key_expiry.sql
  - app/api/cron/rfp-key-expiry-check/route.ts
  - lib/rfp/key-expiry.ts
  - vercel.json
  - .env.example
requirements: [LAUNCH-03]
autonomous: false
user_setup:
  - service: sam.gov / fsd.gov
    why: "Federal contract (SAM.gov) ingestion requires a SYSTEM account API key (not a personal key). The system account is registered via fsd.gov and has a multi-day approval wait. Only Lorenzo, with TPC/org credentials, can submit it."
    env_vars:
      - name: SAM_GOV_API_KEY
        source: "SAM.gov system account → after fsd.gov approval, retrieve the system-account API key"
    dashboard_config:
      - task: "Register a SAM.gov SYSTEM account at fsd.gov using TPC/org credentials and submit the API key request"
        location: "https://fsd.gov (System Account registration)"
      - task: "Create a recurring 90-day calendar alert (fire at 21 days before expiry) for SAM.gov key rotation"
        location: "Google Calendar (recurring event)"

must_haves:
  truths:
    - "A key-expiry record stores the SAM.gov API key expiry date and is queryable"
    - "A scheduled job checks key expiry and fires an operator alert when any key is within 21 days of expiry"
    - "The SAM.gov SYSTEM account registration is submitted (human action) and tracked"
    - "A recurring calendar alert exists for the 90-day key-expiry cycle"
  artifacts:
    - path: app/api/cron/rfp-key-expiry-check/route.ts
      provides: "Cron route that reads key expiry dates and alerts when within 21 days"
      exports: ["GET"]
    - path: supabase/migrations/20260606_rfp_api_key_expiry.sql
      provides: "Storage for API key name + expiry date + last-alerted timestamp"
      contains: "expires_at"
    - path: lib/rfp/key-expiry.ts
      provides: "Helper to read/compute days-until-expiry and decide whether to alert"
  key_links:
    - from: app/api/cron/rfp-key-expiry-check/route.ts
      to: rfp_api_key_health (expires_at)
      via: "cron queries expiry, compares to now()+21d, sends alert"
      pattern: "rfp_api_key"
    - from: vercel.json
      to: app/api/cron/rfp-key-expiry-check
      via: "cron schedule entry"
      pattern: "rfp-key-expiry-check"
---

<objective>
Build the SAM.gov API-key-expiry alerting (code + env wiring — autonomous) AND drive the human-only SAM.gov SYSTEM account registration + 90-day calendar alert (checkpoint). SAM.gov system-account registration via fsd.gov has a multi-day wait and needs Lorenzo's org credentials, so it is a handoff that runs in parallel and must not block the phase — but the alerting infrastructure that makes a future expiry impossible to miss is built now.

Purpose: SAM.gov keys silently expire on a 90-day cycle; a missed renewal silently kills federal discovery (PITFALLS Pitfall 3). We store the expiry date, alert at 21 days (more slack than the 15-day renewal window), and register a non-expiring system account.
Output: A key-expiry health record + cron alert wired in Vercel, plus a submitted system-account registration and a recurring calendar alert.

NOTE: Grants.gov / SBIR ingest are NOT gated by this — they proceed in parallel and are out of scope here.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md
@.planning/research/rfp-engine/DAY-1-ACTIONS.md
@lib/rfp/sources.ts
@app/api/cron/rfp-discovery-state-city/route.ts
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API-key-expiry health storage + alerting helper</name>
  <files>supabase/migrations/20260606_rfp_api_key_expiry.sql, lib/rfp/key-expiry.ts</files>
  <action>
    Activate /db-safe. Target: LDC Brain AI (hgxxxmtfmvguotkowxbu, `supabase` MCP) — verify the RFP schema is here before applying.
    Create an ADDITIVE migration supabase/migrations/20260606_rfp_api_key_expiry.sql:
      CREATE TABLE IF NOT EXISTS rfp_api_key_health (
        key_name      text PRIMARY KEY,       -- e.g. 'sam_gov'
        expires_at    timestamptz,            -- null until set after registration
        account_type  text,                   -- 'system' | 'individual'
        last_alerted_at timestamptz,
        updated_at    timestamptz NOT NULL DEFAULT now()
      );
      Enable RLS, add a service-only policy (USING(false) WITH CHECK(false)) mirroring the existing rfp_source_drift pattern in 20260510_rfp_state_city_drift.sql. Seed one row: INSERT ... ('sam_gov', NULL, 'system', NULL) ON CONFLICT DO NOTHING.
    Apply via `mcp__supabase__apply_migration` (name: rfp_api_key_expiry).
    Create lib/rfp/key-expiry.ts exporting:
      - daysUntilExpiry(expiresAt: string | null): number | null
      - needsAlert(expiresAt, lastAlertedAt, thresholdDays = 21): boolean  // true when within threshold AND not alerted in the last 7 days
    TypeScript strict, no `any`. Use createAdminClient() for any DB read (server/background op per CLAUDE.md).
  </action>
  <verify>`SELECT * FROM rfp_api_key_health` returns the seeded sam_gov row; `npx tsc --noEmit` clean on lib/rfp/key-expiry.ts</verify>
  <done>rfp_api_key_health table exists with a sam_gov row; key-expiry helper compiles and computes days-until-expiry + alert decision.</done>
</task>

<task type="auto">
  <name>Task 2: Add the key-expiry cron route + Vercel schedule + env doc</name>
  <files>app/api/cron/rfp-key-expiry-check/route.ts, vercel.json, .env.example</files>
  <action>
    Create app/api/cron/rfp-key-expiry-check/route.ts (GET). Reuse the existing shared cron auth (lib/cron — confirmed in repo via `Extract shared cron auth into lib/cron`) so the route validates the CRON_SECRET exactly like the other rfp cron routes. The handler:
      1. createAdminClient(); read all rfp_api_key_health rows.
      2. For each, if needsAlert(expires_at, last_alerted_at) → send an operator alert. Reuse the existing alert/email mechanism the rfp-weekly-report cron uses (check app/api/cron/rfp-weekly-report/route.ts for the Resend/email helper and reuse it — do NOT introduce a new mailer). Subject like "SAM.gov API key expires in N days — rotate now".
      3. Set last_alerted_at = now() on alerted rows to avoid daily re-spam.
      4. Return JSON { checked, alerted }.
    Add a schedule entry to vercel.json for the new route (daily is fine, e.g. "0 13 * * *"). Match the existing cron entry format in vercel.json exactly.
    Add to .env.example (commented, NO secret value): `# SAM_GOV_API_KEY=  # system-account key from fsd.gov; set rfp_api_key_health.expires_at to (issued + 90d) after registration`.
  </action>
  <verify>`curl -H "Authorization: Bearer $CRON_SECRET" localhost:3000/api/cron/rfp-key-expiry-check` (or the project's cron auth header) returns 200 with { checked, alerted }; vercel.json parses (node -e "require('./vercel.json')"); `npx tsc --noEmit` clean</verify>
  <done>The cron route authenticates, reads key health, would alert at the 21-day threshold, records last_alerted_at, and is scheduled in vercel.json.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3 (checkpoint): Register SAM.gov system account + set 90-day calendar alert</name>
  <action>Human-only actions with no CLI/API: register the SAM.gov SYSTEM account at fsd.gov, set SAM_GOV_API_KEY + rfp_api_key_health.expires_at once issued, and create a recurring 90-day calendar alert. See how-to-verify for exact steps.</action>
  <what-built>The key-expiry alerting infrastructure (rfp_api_key_health table, lib/rfp/key-expiry.ts, the rfp-key-expiry-check cron, Vercel schedule, and the .env.example note) is built and ready. What remains is the two human-only actions that have no CLI/API: registering the SAM.gov SYSTEM account and creating the calendar alert.</what-built>
  <how-to-verify>
    1. Go to https://fsd.gov and register a SAM.gov SYSTEM account (not an individual account — system-account keys avoid the 90-day individual-key churn) using TPC / org credentials. Submit the API key request. Expect a multi-day approval wait — this is fine, it runs in parallel and does not block later Phase 13 work.
    2. When the system-account API key is issued: set it as `SAM_GOV_API_KEY` in Vercel (Production env) and update the DB row so alerting is armed:
         UPDATE rfp_api_key_health SET expires_at = '<issued_date + 90 days>', updated_at = now() WHERE key_name = 'sam_gov';
       (If a system-account key truly does not expire, set account_type accordingly and leave expires_at NULL — the alerting simply won't fire, which is correct.)
    3. Create a RECURRING Google Calendar event titled "Rotate SAM.gov API key" on a 90-day cadence, with a reminder 21 days before the key expiry date. This is the human backstop independent of the cron.
    4. Confirm: registration submitted (note the confirmation/reference), and the recurring calendar alert exists.
  </how-to-verify>
  <resume-signal>Reply "registration submitted + calendar alert set" (and paste the expiry date if the key is already issued), or describe any blocker (e.g. fsd.gov credential issue).</resume-signal>
</task>

</tasks>

<verification>
1. `SELECT * FROM rfp_api_key_health WHERE key_name = 'sam_gov'` returns a row; once the key is issued, expires_at is populated.
2. The rfp-key-expiry-check cron returns 200 and is listed in vercel.json.
3. SAM.gov system-account registration is submitted (human-confirmed) and a recurring 90-day calendar alert exists (human-confirmed).
</verification>

<success_criteria>
SAM.gov system account registration is submitted and a calendar alert exists for the 90-day key expiry cycle; key-expiry alerting is in place in code. (Phase Success Criterion 3 / LAUNCH-03)
</success_criteria>

<output>
After completion, create `.planning/phases/13-pre-work-stabilization/13-03-SUMMARY.md` noting the cron wiring and the human-action status (registration submitted, awaiting approval).
</output>
