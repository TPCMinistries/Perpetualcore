---
phase: 13-pre-work-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/rfp/page.tsx
  - lib/rfp/source-readiness.ts
requirements: [LAUNCH-04]
autonomous: true

must_haves:
  truths:
    - "No UI or marketing surface displays a static/inflated opportunity count (80k+ or any hardcoded number)"
    - "Any opportunity count shown is computed live from rfp_opportunities or absent entirely"
    - "A repo-wide grep for the literal 80000 / 80k / '80,000' in user-facing or admin RFP code returns zero inflated-count claims"
  artifacts:
    - path: app/admin/rfp/page.tsx
      provides: "Admin RFP readiness view with no hardcoded 80k threshold or '80k+' positioning copy"
      contains: "indexed"
  key_links:
    - from: app/admin/rfp/page.tsx
      to: rfp_opportunities (verified indexed count)
      via: "summary.indexed sourced from a live count query, not a hardcoded target"
      pattern: "summary\\.indexed"
---

<objective>
Remove every static or inflated opportunity-count claim ("80k+", "80,000", the hardcoded 80000 threshold, and any aspirational-positioning copy that references an unverified inventory number) from the codebase. Replace with a live-computed count from `rfp_opportunities` or remove the claim entirely.

This is the lowest-risk item in the phase and runs first/parallel. It does NOT touch the merge or any external account.

Purpose: A paying grant professional who searches and sees 211 results must never have been told "80k+". Inflated counts are the fastest trust-destroyer in this market (PITFALLS Pitfall 10).
Output: An RFP admin/marketing surface where counts are either dynamically sourced or absent — no hardcoded inventory numbers.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@app/admin/rfp/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate every inflated/static count claim in RFP surfaces</name>
  <files>app/admin/rfp/page.tsx</files>
  <action>
    Run a repo-wide audit (exclude node_modules, .next, .git, .planning) for inflated opportunity-count claims:
      grep -rniE "80[,]?000|80k\+?" app components lib
    Known hits to fix (confirmed this session):
      - app/admin/rfp/page.tsx line ~314: `tone={summary.indexed >= 80000 ? "emerald" : "amber"}` — a hardcoded 80000 threshold treated as the "good" target.
      - app/admin/rfp/page.tsx line ~490-495: `if (sourceReadinessSummary.indexed < 80000)` plus the copy "Public positioning wants 80k+ opportunities. Keep copy tied to verified indexed count until ingestion reaches that number."
    Confirm these are the ONLY inflated-count references in user-facing or admin RFP code. NOTE explicitly: app/(rfp-marketing)/rfp/page.tsx line ~240 ("$90K–$180K human role") and app/roi-calculator (avgSalary: 80000) are SALARY figures, NOT opportunity counts — leave them untouched. Record the full grep result in the summary so the verification step is auditable.
  </action>
  <verify>grep -rniE "80[,]?000|80k\+?" app components lib | grep -viE "node_modules|\.next" produces a list where every remaining hit is a salary/dollar figure, not an opportunity count</verify>
  <done>A complete, documented list of inflated opportunity-count claims exists; salary figures are explicitly excluded from scope.</done>
</task>

<task type="auto">
  <name>Task 2: Purge the hardcoded 80k threshold and aspirational copy from the admin RFP view</name>
  <files>app/admin/rfp/page.tsx, lib/rfp/source-readiness.ts</files>
  <action>
    In app/admin/rfp/page.tsx:
      1. Remove the hardcoded `>= 80000` tone comparison on the Indexed tile (line ~314). Tone the Indexed tile off a dynamic signal instead: use the existing `summary.indexedCoveragePercent` (>= a sensible coverage threshold like 80 → "emerald", else "amber") OR simply default to a neutral tone. Do NOT introduce a new hardcoded inventory number.
      2. Remove the entire `if (sourceReadinessSummary.indexed < 80000) { actions.push({ id: "inventory-scale", ... "Public positioning wants 80k+ opportunities..." }) }` block (line ~490-495). The "80k+" positioning copy is exactly the inflated claim LAUNCH-04 forbids. If a readiness nudge is still desired, replace it with copy tied to verified coverage that names NO absolute target (e.g. "Indexed inventory is below catalog coverage target — keep all public copy tied to the live verified count"), driven by `indexedCoveragePercent`, not a raw number.
    Check lib/rfp/source-readiness.ts (the module that computes `SourceReadinessSummary`): if `targetIndexedEstimate` or any field hardcodes 80000/80k, ensure it is either derived from the source catalog estimate already in code or removed. Do NOT invent a new number. If the file does not hardcode 80k, leave it unchanged and note that in the summary.
    Keep changes surgical — touch only the lines tied to the inflated number. Match existing TypeScript strict style.
  </action>
  <verify>grep -n "80000\|80k" app/admin/rfp/page.tsx lib/rfp/source-readiness.ts returns nothing; `npm run build` (or `npx tsc --noEmit`) succeeds with no new type errors in these files</verify>
  <done>The admin RFP page compiles, shows no "80k+" copy and no hardcoded 80000 threshold; the Indexed tile tone is driven by a dynamic/coverage signal or is neutral.</done>
</task>

</tasks>

<verification>
1. `grep -rniE "80[,]?000|80k\+?" app components lib | grep -viE "node_modules|\.next"` — every remaining hit is a salary/dollar figure, zero are opportunity counts.
2. `npm run build` succeeds (or `npx tsc --noEmit` clean for the touched files).
3. The admin RFP readiness view no longer references "80k+ opportunities" anywhere.
</verification>

<success_criteria>
No page or marketing surface displays a static or inflated opportunity count ("80k+" or any hardcoded number); counts are dynamic or absent. (Phase Success Criterion 2 / LAUNCH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/13-pre-work-stabilization/13-01-SUMMARY.md` including the full grep audit output and the exact lines changed.
</output>
