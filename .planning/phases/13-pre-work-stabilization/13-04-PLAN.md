---
phase: 13-pre-work-stabilization
plan: 04
type: execute
wave: 2
depends_on: ["13-01", "13-02", "13-03"]
files_modified:
  - .planning/phases/13-pre-work-stabilization/13-04-MERGE-LOG.md
requirements: [LAUNCH-01]
autonomous: false

must_haves:
  truths:
    - "feat/rfp-orgs-invites-cont (PR #4) is merged into main with BOTH sides' commits preserved (HEAD's 245 ahead AND main's 114 ahead)"
    - "The merged tree builds cleanly (npm run build) and key RFP routes resolve"
    - "A backup ref of pre-merge main exists so the merge can be rolled back"
    - "Production runs from main without regression to the live RFP surface (rfp.perpetualcore.com)"
    - "The merge fixes from 13-01 (count purge), 13-02 (drift triage), 13-03 (key-expiry) are included in main"
  artifacts:
    - path: .planning/phases/13-pre-work-stabilization/13-04-MERGE-LOG.md
      provides: "Conflict inventory, resolution decisions, backup ref name, build/route verification, rollback procedure"
      min_lines: 30
  key_links:
    - from: main (post-merge)
      to: rfp.perpetualcore.com (production)
      via: "Vercel deploy from main; middleware host-rewrite still serves the RFP surface"
      pattern: "middleware"
---

<objective>
Safely merge PR #4 (`feat/rfp-orgs-invites-cont`, 245 commits ahead of origin/main) into `main`. This is a TRUE TWO-WAY merge: origin/main is also 114 commits ahead (an account/lead/handoff workflow line of work that HEAD does not have) — both sides MUST be preserved. Production currently serves rfp.perpetualcore.com directly from the feat branch via a middleware host-rewrite, so the merge must NOT regress the live RFP surface. Resolve conflicts deliberately, verify build + key routes, and treat the actual `git push origin main` as a human-go-ahead checkpoint with an explicit rollback path.

Purpose: PR #4 at 245 commits is the largest divergence risk in the milestone; every day it stays open the merge gets harder, and a botched merge can drop RLS policies or skip migrations (PITFALLS Pitfall 11). A stable `main` is the precondition for all of v2.0.
Output: main = feat work ⊕ account/lead/handoff work, building cleanly, with a backup ref and a documented rollback.

This runs AFTER 13-01/02/03 so the stabilization fixes (count purge, drift triage, key-expiry) land on the branch and merge into main together.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/repositioning/MERGE_PLAN.md
@middleware.ts
@next.config.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot, fetch, and pre-analyze the conflict surface on a throwaway branch</name>
  <files>.planning/phases/13-pre-work-stabilization/13-04-MERGE-LOG.md</files>
  <action>
    First commit any pending stabilization work on feat/rfp-orgs-invites-cont so the branch is clean (13-01/02/03 outputs). Confirm `git status` is clean.
    `git fetch origin`. Re-confirm divergence: `git rev-list --count origin/main..HEAD` (~245) and `git rev-list --count HEAD..origin/main` (~114). Record both in the merge log — this proves it is a two-way merge, not a fast-forward.
    Create BACKUP REFS (rollback safety):
      git branch backup/main-pre-pr4-$(date +%Y%m%d) origin/main
      git tag backup/pr4-feat-$(date +%Y%m%d) HEAD
    Create a THROWAWAY merge-analysis branch from origin/main and dry-run the merge WITHOUT committing:
      git switch -c merge-analysis/pr4 origin/main
      git merge --no-commit --no-ff feat/rfp-orgs-invites-cont
    Capture `git diff --name-only --diff-filter=U` (the actual conflicted files). From this session's pre-analysis, the expected conflict surface is config + studio-repositioning overlap: .env.example, .github/workflows/ci.yml, .gitignore, next.config.mjs, package.json, package-lock.json, vercel.json, middleware.ts, app/layout.tsx, vitest.config.ts, playwright.config.ts, plus the homepage/Navbar/Footer/solicitations pages noted in .planning/repositioning/MERGE_PLAN.md. RFP code in lib/rfp/, app/(rfp-marketing)/, components/rfp/, app/api/cron/ is separate territory and should be low/no conflict.
    Write 13-04-MERGE-LOG.md: a table of every conflicted file with a planned resolution strategy (ours / theirs / hand-merge) and a one-line rationale. For migrations specifically: list any conflicting/duplicate migration filenames — migrations must NEVER be renamed or edited after applying; if two migrations share a timestamp, keep both with distinct names.
    Then `git merge --abort` and `git switch feat/rfp-orgs-invites-cont`; delete the analysis branch only after the log is written (keep it if useful). Do NOT push anything in this task.
  </action>
  <verify>13-04-MERGE-LOG.md lists the exact conflicted files with a resolution strategy each; backup branch `backup/main-pre-pr4-*` and tag `backup/pr4-feat-*` both exist (`git branch --list 'backup/*'`, `git tag --list 'backup/*'`); working tree is back on feat branch, clean</verify>
  <done>A backup ref of pre-merge main exists, the full conflict inventory is documented with a deliberate per-file resolution plan, and nothing has been pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the real merge into a local main, resolve conflicts, and verify build + routes</name>
  <files>.planning/phases/13-pre-work-stabilization/13-04-MERGE-LOG.md</files>
  <action>
    Update local main to match remote, then merge feat in (preserving both sides):
      git switch main && git reset --hard origin/main
      git merge --no-ff feat/rfp-orgs-invites-cont
    Resolve each conflict per the strategy recorded in the merge log:
      - Config files (next.config.mjs, package.json, vercel.json, ci.yml, .gitignore, .env.example): hand-merge — KEEP BOTH sides' additions (do not drop main's cron entries or env keys, do not drop feat's RFP cron/env). For package.json/package-lock.json take the union of deps then re-run `npm install` to regenerate a consistent lock.
      - middleware.ts: CRITICAL — preserve the host-rewrite that serves rfp.perpetualcore.com. If both sides changed middleware, hand-merge so the RFP host-rewrite AND any main-side auth/middleware changes both survive. (Recall the @supabase/ssr middleware ordering rule: getUser() refresh must run before the /api/* short-circuit.)
      - Homepage/Navbar/Footer/solicitations pages (studio-repositioning overlap): hand-merge using .planning/repositioning/MERGE_PLAN.md as the guide for which version wins per file.
      - Migrations: keep ALL migration files from both sides; never edit/rename an already-applied one.
    After resolving: `git add` resolved files, `git commit` the merge. DO NOT push yet.
    Verify the merged tree:
      1. `npm install` then `npm run build` — MUST succeed. Fix any merge-induced type/import breakage.
      2. Smoke-check key RFP routes resolve in the build output / dev server: app/(rfp-marketing)/rfp/page.tsx, app/admin/rfp/page.tsx, app/api/health/rfp/route.ts, and one org discovery route (app/(dashboard)/org/[orgId]/discovery). Confirm the home/Navbar/Footer render without import errors.
      3. Confirm the 13-01 count purge, 13-02 triage register, and 13-03 key-expiry route are all present in the merged tree.
    Record build result + route checks + the final merge commit SHA in 13-04-MERGE-LOG.md.
  </action>
  <verify>`git log --oneline -1` on local main shows the merge commit; `npm run build` exits 0; the four key RFP routes are present and import-clean; merge log records SHA + build PASS</verify>
  <done>Local main contains both lines of work, builds cleanly, key RFP routes resolve, and stabilization fixes are included — ready for a gated push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Human review then gated push of merged main to production</name>
  <action>Pause for human review of the local merged main BEFORE the irreversible push. On go-ahead, run `git push origin main`, confirm the Vercel deploy and that rfp.perpetualcore.com still serves the live RFP surface, and invoke the documented rollback if it regresses. See how-to-verify for exact steps.</action>
  <what-built>Local `main` now contains the merged result: PR #4's 245 commits + main's 114 commits, with conflicts resolved (config files union-merged, middleware host-rewrite preserved, studio-repositioning pages hand-merged per MERGE_PLAN.md, all migrations kept). `npm run build` passes and key RFP routes resolve. A backup branch/tag of pre-merge main exists. NOTHING has been pushed yet.</what-built>
  <how-to-verify>
    Review before the irreversible push to production-tracked main:
    1. Inspect the merge: `git log --oneline origin/main..main | head -30` and `git diff --stat origin/main..main` — confirm both feat work and main's account/handoff commits are present.
    2. Confirm the build artifact / a local `npm run dev` renders the RFP marketing page and admin RFP page without errors, and the live-serving host-rewrite is intact in middleware.ts.
    3. Review 13-04-MERGE-LOG.md for the conflict resolutions and the rollback procedure.
    When satisfied, give go-ahead to push. On go-ahead, the executor runs:
       git push origin main
    and then confirms Vercel deploys main and rfp.perpetualcore.com still serves the live RFP surface without regression.
    ROLLBACK (if the deploy regresses): `git push origin backup/main-pre-pr4-<date>:main --force-with-lease` (or revert the merge commit) to restore pre-merge main, then redeploy.
  </how-to-verify>
  <resume-signal>Reply "push approved" to push main and trigger the production deploy, or "hold" / describe concerns. After push, confirm rfp.perpetualcore.com is healthy or invoke the rollback.</resume-signal>
</task>

</tasks>

<verification>
1. `git log origin/main` (after push) shows the PR #4 branch commits AND main's prior account/handoff commits — both lines preserved.
2. `npm run build` passes on the merged main.
3. Backup ref `backup/main-pre-pr4-*` exists for rollback; rollback procedure documented in the merge log.
4. Production runs from main; rfp.perpetualcore.com serves the live RFP surface without regression (human-confirmed post-push).
</verification>

<success_criteria>
`git log origin/main` shows the PR #4 branch commits and production is running from main without regression. (Phase Success Criterion 1 / LAUNCH-01)
</success_criteria>

<output>
After completion, create `.planning/phases/13-pre-work-stabilization/13-04-SUMMARY.md` with the final merged SHA, the backup ref name, conflict-resolution summary, build/route verification, and post-push production health status.
</output>
