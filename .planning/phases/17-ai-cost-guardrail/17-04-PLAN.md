---
phase: 17-ai-cost-guardrail
plan: 04
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - lib/rfp/scoring/summary.ts
  - lib/rfp/scoring/recompute.ts
autonomous: true
requirements: [BILL-04]
must_haves:
  truths:
    - "generateFitSummary returns token + cost metadata (not just a string) so the cron can ledger per-org cost"
    - "The cron scoring loop routes each per-(opp,org) summary call through guardedLLMCall with that org's id"
    - "An over-budget org is SILENTLY SKIPPED in the cron (logged + continue); the cron does NOT abort for other orgs"
    - "Successful cron summary calls record cost in rfp_agent_sessions for the correct org"
  artifacts:
    - path: "lib/rfp/scoring/summary.ts"
      provides: "generateFitSummary returning { text, tokensIn, tokensOut, costUsd }"
      contains: "costUsd"
    - path: "lib/rfp/scoring/recompute.ts"
      provides: "Per-org guarded summary call in the scoring loop with budget-exceed skip"
      contains: "guardedLLMCall"
  key_links:
    - from: "lib/rfp/scoring/recompute.ts"
      to: "lib/rfp/ai/guardrail.ts"
      via: "guardedLLMCall(orgId, () => generateFitSummary(...)) inside the per-pair loop, catching BudgetExceededError to skip"
      pattern: "guardedLLMCall"
    - from: "lib/rfp/scoring/recompute.ts"
      to: "lib/rfp/scoring/summary.ts"
      via: "consumes the new { text, tokensIn, tokensOut, costUsd } return shape"
      pattern: "generateFitSummary"
---

<objective>
Make the background/cron scoring path budget-aware. Change `generateFitSummary` to return cost metadata instead of a bare string, then route each per-(opp,org) summary call in the cron loop through `guardedLLMCall` — silently skipping over-budget orgs (Open Question 1's agreed default) so one org's limit never aborts the whole cron.

Purpose: The cron scoring path is the only non-user LLM call site in scope. Without it, an over-budget org's background scoring would still spend money. This closes the last in-scope call site for BILL-04. The summary.ts return-type change is a dependency of the recompute caller, so both files live in this single plan to avoid a cross-plan ordering hazard.
Output: Updated summary.ts (new return shape) and recompute.ts (guarded, skip-on-budget loop).
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-cost-guardrail/17-RESEARCH.md
@lib/rfp/ai/guardrail.ts
@lib/rfp/scoring/summary.ts
@lib/rfp/scoring/recompute.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change generateFitSummary to return cost metadata</name>
  <files>lib/rfp/scoring/summary.ts</files>
  <action>
Careful refactor — `generateFitSummary` currently returns `Promise<string | null>` and discards the Anthropic `usage` response. Change it to return a structured object that preserves the existing text semantics AND exposes cost.

New return type: `Promise<{ text: string | null; tokensIn: number; tokensOut: number; costUsd: number }>`.

Implementation:
1. Profile-pending case (breakdown.profile_pending || profile === null): return `{ text: PROFILE_PENDING_SUMMARY, tokensIn: 0, tokensOut: 0, costUsd: 0 }` — no AI call, no cost.
2. No ANTHROPIC_API_KEY (getAnthropic() returns null): return `{ text: null, tokensIn: 0, tokensOut: 0, costUsd: 0 }`.
3. On a successful messages.create: extract `resp.usage.input_tokens` and `resp.usage.output_tokens` (Anthropic SDK shape). Compute costUsd using the canonical rate map from the guardrail: import `{ computeCostUsd }` from `@/lib/rfp/ai/guardrail` and call `computeCostUsd(model, input_tokens, output_tokens)` (model is whichever of claude-sonnet-4-5 / claude-haiku-4-5 actually answered — track it in the loop). Return `{ text, tokensIn: input_tokens, tokensOut: output_tokens, costUsd }`.
4. Both models failed (the existing fall-through): return `{ text: null, tokensIn: 0, tokensOut: 0, costUsd: 0 }`.

Keep the never-throw contract: the function still must not throw on model errors. Keep MODEL_PRIMARY/MODEL_FALLBACK logic; just capture which model succeeded and its usage.

TypeScript strict, no `any`. Anthropic `resp.usage` is `{ input_tokens: number; output_tokens: number }`.
  </action>
  <verify>Lint + typecheck green for summary.ts. The return type is the 4-field object in all branches. No throw paths added. `grep computeCostUsd lib/rfp/scoring/summary.ts` confirms the canonical rate map is used.</verify>
  <done>generateFitSummary returns { text, tokensIn, tokensOut, costUsd } in every branch; cost computed from RFP_MODEL_RATES via computeCostUsd; never throws.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the cron scoring loop per org with budget-exceed skip</name>
  <files>lib/rfp/scoring/recompute.ts</files>
  <action>
Read recompute.ts around the asyncPool over `pairs` (each pair = { opp, orgId, profile }) where `generateFitSummary(opp, profile, breakdown)` is called (~line 418). Update the call site for the new return shape AND wrap it with the budget guard.

1. Import `{ guardedLLMCall, BudgetExceededError }` from `@/lib/rfp/ai/guardrail`.
2. Inside the per-pair worker, replace the direct `const summary = await generateFitSummary(...)` (which returned a string) with a guarded call that:
   - Calls `guardedLLMCall(orgId, async () => { const r = await generateFitSummary(opp, profile, breakdown); return { agent: "scoring_summary_v1", model: <model used> , tokensIn: r.tokensIn, tokensOut: r.tokensOut, costUsd: r.costUsd, _text: r.text }; })`. Since LLMCallMeta has no text field, carry the text either by widening the returned object (it extends LLMCallMeta with an extra `text` field) or by destructuring r outside — simplest: return an object that satisfies LLMCallMeta PLUS a `text` field, then read `.text` off the result. The wrapper passes the object through and records cost.
   - Use the summary text from the guarded result for the existing downstream upsert (`summary` field on the match row). The current code uses the string `summary`; map it to `result.text`.
3. SKIP-ON-BUDGET (Open Question 1 default = silent skip + log): wrap the guarded call in try/catch. `if (err instanceof BudgetExceededError) { console.warn("[scoring/recompute] org over AI budget, skipping summary:", orgId); /* proceed with summary text = null so scoring chips still upsert */ }` then continue this pair with `text = null` (the structured chips/score still upsert; only the narrative summary is omitted). Re-throw any non-BudgetExceededError so real failures surface. Do NOT abort the whole cron — the asyncPool continues to other pairs/orgs.
4. IMPORTANT — note that the profile-pending and no-key branches now return text via the guarded wrapper too; that is fine (cost 0, no budget impact, no skip). The budget check still runs (cheap read) but a $0 cost won't trip anything for those branches because checkBudget compares cumulative spend, not this call's cost.
   - Minor optimization (optional): for the profile-pending sentinel you may bypass guardedLLMCall entirely since it makes no AI call — but keeping it guarded is also acceptable. Pick one and note it.
5. Update any type usage where `summary` was treated as `string | null` — it still is (result.text).

Surgical: do not change the scoring math (score.ts), the upsert/onConflict logic, the alert dispatch, or asyncPool concurrency. Only the summary call + cost guard + skip behavior.
  </action>
  <verify>Lint + typecheck green for recompute.ts. `grep guardedLLMCall lib/rfp/scoring/recompute.ts` present. Trace: an org with monthly_ai_budget_usd exceeded → its summary call throws BudgetExceededError → caught → warn logged → that pair upserts with summary=null → loop continues to other orgs (no cron abort). A within-budget org's summary call records a rfp_agent_sessions row with agent scoring_summary_v1.</verify>
  <done>Cron summary calls are per-org budget-guarded; over-budget orgs are skipped with a log (cron continues); within-budget calls ledger cost; scoring chips/scores still upsert regardless.</done>
</task>

</tasks>

<verification>
- generateFitSummary returns { text, tokensIn, tokensOut, costUsd } everywhere; never throws.
- recompute.ts guards each per-org summary call; BudgetExceededError → skip + log + continue (no cron abort).
- Within-budget cron calls write a scoring_summary_v1 rfp_agent_sessions row for the correct org.
- Lint + typecheck green; scoring math/upsert/alerts unchanged.
</verification>

<success_criteria>
- The background scoring path no longer spends AI budget for over-budget orgs (silent skip per Open Question 1).
- Cron cost is ledgered per org for within-budget runs.
- summary.ts return-type change is consumed correctly by recompute.ts (no broken scoring display).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-cost-guardrail/17-04-SUMMARY.md` noting: the model-tracking approach in summary.ts, whether profile-pending bypasses the guard, and confirmation the cron continues past an over-budget org.
</output>
