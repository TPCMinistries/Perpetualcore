---
phase: 17-ai-cost-guardrail
plan: 05
type: execute
wave: 3
depends_on: [17-01, 17-02, 17-03, 17-04]
files_modified:
  - tests/unit/rfp-ai-guardrail.test.ts
  - scripts/verify-rfp-ai-budget.ts
autonomous: true
requirements: [BILL-04]
must_haves:
  truths:
    - "A unit test proves a $0 budget org throws BudgetExceededError and the LLM fn is NEVER called (zero token usage)"
    - "A unit test proves a successful guarded call inserts one rfp_agent_sessions row with the call's cost"
    - "A unit test proves cumulative spend crossing the limit mid-session blocks the next call (BudgetExceededError, fn not called)"
    - "A live-DB script proves the same three behaviors against the real rfp_entitlements + rfp_agent_sessions schema, then cleans up"
  artifacts:
    - path: "tests/unit/rfp-ai-guardrail.test.ts"
      provides: "Vitest coverage of the 3 success criteria with mocked admin client"
      contains: "BudgetExceededError"
      min_lines: 60
    - path: "scripts/verify-rfp-ai-budget.ts"
      provides: "Live-DB end-to-end verification of the guardrail against CORE DB with VERIFY- prefix + cleanup"
      contains: "guardedLLMCall"
  key_links:
    - from: "tests/unit/rfp-ai-guardrail.test.ts"
      to: "lib/rfp/ai/guardrail.ts"
      via: "imports guardedLLMCall + BudgetExceededError, mocks createAdminClient"
      pattern: "guardedLLMCall"
    - from: "scripts/verify-rfp-ai-budget.ts"
      to: "rfp_agent_sessions / rfp_entitlements"
      via: "createAdminClient seeds a test org budget, runs guardedLLMCall, asserts ledger + block, cleans up"
      pattern: "rfp_agent_sessions"
---

<objective>
Prove all three Phase 17 success criteria with automated tests: (1) a $0 budget blocks the call with zero token usage, (2) a successful call writes a ledger row in the same lifecycle, (3) mid-session limit crossing blocks the next call with a budget error (mapped to 402). Cover both a fast mocked unit test and a live-DB script against the real schema.

Purpose: This is the goal-backward verification for BILL-04. Without it we cannot confirm the guardrail actually enforces the budget against the real tables.
Output: `tests/unit/rfp-ai-guardrail.test.ts` (vitest) and `scripts/verify-rfp-ai-budget.ts` (live-DB, CORE-safe).
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-cost-guardrail/17-RESEARCH.md
@lib/rfp/ai/guardrail.ts
@lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest unit tests for the 3 success criteria</name>
  <files>tests/unit/rfp-ai-guardrail.test.ts</files>
  <action>
Create vitest unit tests for guardedLLMCall (mock createAdminClient — no real DB). Mirror the patterns in research's Code Examples section. Use `vi.mock("@/lib/supabase/server", () => ({ createAdminClient: vi.fn() }))` and a `makeMockAdminWithBudget({ limitUsd, spentUsd })` helper that returns a chainable mock whose entitlements read yields `{ monthly_ai_budget_usd: limitUsd }` and whose rfp_agent_sessions select yields rows summing to spentUsd; capture the insert call so you can assert on the recorded row.

Three required test cases (each maps to a success criterion):
1. SUCCESS CRITERION 1 — "$0 budget blocks, zero token usage": limitUsd=0, spentUsd=0. fn = vi.fn() returning valid meta. Assert `guardedLLMCall("org-123", fn)` rejects with `BudgetExceededError` AND `expect(fn).not.toHaveBeenCalled()` (the LLM call NEVER fires → zero tokens).
2. SUCCESS CRITERION 2 — "successful call records cost in same lifecycle": limitUsd=10.00, spentUsd=0.5. fn returns meta `{ agent:"drafter_v1", model:"gpt-4o", tokensIn:500, tokensOut:1000, costUsd:0.012, proposalId:"prop-abc" }`. Await guardedLLMCall, assert the captured insert was called with `expect.objectContaining({ org_id:"org-123", cost_usd:0.012, agent:"drafter_v1" })`.
3. SUCCESS CRITERION 3 — "mid-session limit crossing blocks next call": limitUsd=10.00, spentUsd=10.00 (cumulative already at limit). fn = vi.fn(). Assert rejects with BudgetExceededError AND fn not called.

Add two guard tests:
4. NULL limit = unlimited: limitUsd=null, spentUsd=999. Assert guardedLLMCall RESOLVES (fn called) and cost recorded — NULL must NOT be treated as $0.
5. budgetExceededResponse maps to 402: construct a BudgetExceededError and assert `budgetExceededResponse(err).status === 402` and the JSON body contains error:"budget_exceeded", limit_usd, spent_usd.

TypeScript strict, no `any` (cast mocks via `as never` where needed, per research example).
  </action>
  <verify>`npm run test:run -- tests/unit/rfp-ai-guardrail.test.ts` passes all 5 cases. Each of the 3 success criteria has a green assertion. The $0 and mid-session cases both assert fn was NOT called.</verify>
  <done>All 5 vitest cases pass; the 3 success criteria are each proven; NULL-unlimited and 402-mapping covered.</done>
</task>

<task type="auto">
  <name>Task 2: Live-DB verification script (CORE-safe, VERIFY- prefix + cleanup)</name>
  <files>scripts/verify-rfp-ai-budget.ts</files>
  <action>
Create a tsx script (runnable via `npx tsx scripts/verify-rfp-ai-budget.ts`) that proves the guardrail against the REAL rfp_entitlements + rfp_agent_sessions schema on the CORE DB (hgxxxmtfmvguotkowxbu), following the Phase 14-04 live-verify pattern: VERIFY- prefix on any created identifiers + a `finally` block that cleans up everything it created. Use `createAdminClient()` from `@/lib/supabase/server`.

The script must:
1. Create (or reuse) a throwaway test org row in the minimal tables needed for an rfp_entitlements row keyed by org_id — if creating an org requires rfp_orgs, insert a row with name like "VERIFY-ai-budget-<timestamp>" and capture its id. (Read the rfp_orgs/rfp_entitlements schema to use correct NOT NULL columns.) Record the orgId.
2. CRITERION 1 ($0 blocks, zero tokens): set the org's `monthly_ai_budget_usd = 0`. Call `guardedLLMCall(orgId, fn)` with an fn that sets a `called = true` flag and returns dummy meta. Assert it throws BudgetExceededError AND `called === false`. Print PASS/FAIL.
3. CRITERION 2 (success records ledger row): set `monthly_ai_budget_usd = 10.00`. Count this-month rfp_agent_sessions rows for orgId, call guardedLLMCall with fn returning meta { agent:"verify_v1", model:"gpt-4o", tokensIn:100, tokensOut:200, costUsd:0.01 }, then re-count and assert exactly one new row with cost_usd ≈ 0.01. Print PASS/FAIL.
4. CRITERION 3 (mid-session crossing blocks): with limit still 10.00, directly insert ledger rows (via admin) totalling >= 10.00 this month for orgId, then call guardedLLMCall with a fn that flags called; assert it throws BudgetExceededError and called === false. Print PASS/FAIL.
5. NULL = unlimited sanity: set monthly_ai_budget_usd = null, call guardedLLMCall, assert it RESOLVES. Print PASS/FAIL.
6. `finally`: delete ALL rfp_agent_sessions rows created for orgId during the run, delete the rfp_entitlements row, and delete the VERIFY- org. Leave the DB exactly as found. Never delete non-VERIFY rows.

Exit non-zero if any criterion fails so it can gate CI later. Print a final summary line "ALL CRITERIA PASS" / "FAILURES: n".

/db-safe: this only touches VERIFY- rows it creates; all writes are scoped by the throwaway orgId; cleanup is guaranteed in finally.
  </action>
  <verify>`npx tsx scripts/verify-rfp-ai-budget.ts` prints PASS for all four checks and "ALL CRITERIA PASS", exits 0. A follow-up query confirms zero VERIFY- rows remain in rfp_orgs / rfp_entitlements / rfp_agent_sessions (cleanup verified).</verify>
  <done>Live-DB script proves the 3 criteria + NULL-unlimited against the real schema and removes every row it created (verified zero residue).</done>
</task>

</tasks>

<verification>
- Vitest: all 5 cases green; 3 success criteria each asserted; $0 and mid-session both assert fn NOT called (zero token usage).
- Live-DB script: all checks PASS, exit 0, full cleanup (no VERIFY- residue).
- BILL-04 success criteria 1/2/3 each have a concrete passing verification.
</verification>

<success_criteria>
- Setting a test org budget to $0 and triggering guardedLLMCall throws BudgetExceededError with the fn never invoked (criterion 1) — proven in both unit + live-DB.
- A successful guarded call writes one rfp_agent_sessions row with the call's cost in the same lifecycle (criterion 2).
- Cumulative spend crossing the limit blocks the next call with a budget error → 402 (criterion 3).
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-cost-guardrail/17-05-SUMMARY.md` with the test output summary (all green), the live-DB cleanup confirmation, and an explicit checklist mapping each of the 3 BILL-04 success criteria to its passing test. Also document the known deferred gap: quick-import/extract.ts is NOT guarded (no org_id at extract time) — flagged for a future phase.
</output>
