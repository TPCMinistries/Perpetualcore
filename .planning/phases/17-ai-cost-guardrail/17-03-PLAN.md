---
phase: 17-ai-cost-guardrail
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - app/api/rfp/orgs/[orgId]/voice/train/route.ts
  - app/api/rfp/orgs/[orgId]/voice/from-description/route.ts
  - app/api/rfp/orgs/[orgId]/vault/upload/route.ts
  - app/api/rfp/orgs/[orgId]/vault/from-description/route.ts
  - app/api/orgs/naics-suggest/route.ts
autonomous: true
requirements: [BILL-04]
must_haves:
  truths:
    - "Voice train/from-description and vault upload/from-description routes route their LLM call(s) through guardedLLMCall with the route's orgId"
    - "vault/from-description wraps BOTH the expand (gpt-4o) and embed (text-embedding-3-large) calls as one guarded operation with summed cost"
    - "naics-suggest accepts an optional org_id; when present and the user is a member it is budget-guarded, when absent the guard is skipped"
    - "Each route records exactly one rfp_agent_sessions row per call (no double-counting)"
    - "Over-budget orgs receive HTTP 402, not 500"
  artifacts:
    - path: "app/api/rfp/orgs/[orgId]/vault/from-description/route.ts"
      provides: "Expand+embed wrapped in a single guardedLLMCall with combined LLMCallMeta"
      contains: "guardedLLMCall"
    - path: "app/api/orgs/naics-suggest/route.ts"
      provides: "Optional org_id in body; conditional budget guard"
      contains: "guardedLLMCall"
  key_links:
    - from: "app/api/rfp/orgs/[orgId]/vault/from-description/route.ts"
      to: "lib/rfp/ai/guardrail.ts"
      via: "single guardedLLMCall wrapping expand then embed, returning summed cost meta"
      pattern: "guardedLLMCall"
    - from: "app/api/orgs/naics-suggest/route.ts"
      to: "lib/rfp/ai/guardrail.ts"
      via: "conditional guardedLLMCall when org_id provided"
      pattern: "guardedLLMCall"
---

<objective>
Route the remaining five user-facing LLM call sites — voice train, voice from-description, vault upload, vault from-description (two-call expand+embed), and naics-suggest — through `guardedLLMCall`, removing inline `rfp_agent_sessions` inserts. Resolve two of the three open questions here: naics-suggest gets an optional `org_id` for attribution; the two-call vault/from-description is wrapped as one guarded operation.

Purpose: Completes the user-facing call-site coverage for BILL-04 so no user-triggered RFP LLM call bypasses the budget gate.
Output: Five updated routes, each budget-gated and single-counting.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ai-cost-guardrail/17-RESEARCH.md
@lib/rfp/ai/guardrail.ts
@app/api/rfp/orgs/[orgId]/voice/train/route.ts
@app/api/rfp/orgs/[orgId]/vault/from-description/route.ts
@app/api/orgs/naics-suggest/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate voice train + voice from-description routes</name>
  <files>app/api/rfp/orgs/[orgId]/voice/train/route.ts, app/api/rfp/orgs/[orgId]/voice/from-description/route.ts</files>
  <action>
Read both files to confirm the LLM call (voice_fingerprint_extractor_v1, gpt-4o, lib/rfp/voice/extract.ts / from-description.ts) and the existing inline rfp_agent_sessions insert.

For each route:
1. Import `{ guardedLLMCall, budgetExceededResponse, BudgetExceededError }` from `@/lib/rfp/ai/guardrail`.
2. The orgId comes from the route param `[orgId]` (already validated against membership in the route). Use it as the guardedLLMCall tenant id.
3. Wrap the voice-extraction LLM call inside `guardedLLMCall(orgId, async () => { ...; return meta })` where meta = `{ agent: "voice_fingerprint_extractor_v1", model, tokensIn, tokensOut, costUsd, proposalId?: undefined }` built from the extractor result.
4. Remove the inline rfp_agent_sessions insert — the wrapper records it.
5. try/catch → return `budgetExceededResponse(err)` on BudgetExceededError; preserve all other error handling and the voice_fingerprint persistence to rfp_orgs.
6. Ensure the model call is the only model-invoking statement inside fn() so the budget preflight gates it.

Surgical: do not change the voice_fingerprint write, validation, or response shape.
  </action>
  <verify>Lint + typecheck green for both. `grep guardedLLMCall` present in both. No inline rfp_agent_sessions insert remains. Over-budget org → 402.</verify>
  <done>Both voice routes gate the extractor via guardedLLMCall, return 402 on budget exceed, and record exactly one session row via the wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Gate vault upload + vault from-description (two-call) routes</name>
  <files>app/api/rfp/orgs/[orgId]/vault/upload/route.ts, app/api/rfp/orgs/[orgId]/vault/from-description/route.ts</files>
  <action>
Read both files. vault/upload uses vault_indexer_v1 with text-embedding-3-large (embed.ts). vault/from-description makes TWO calls: expandOrgDescription (gpt-4o, expand.ts) THEN embedChunks (text-embedding-3-large, embed.ts), and currently does ONE combined inline session insert.

vault/upload:
1. Import the guardrail helpers.
2. orgId from `[orgId]` param.
3. Wrap the embeddings call in `guardedLLMCall(orgId, async () => { ...embed...; return meta })`, meta = `{ agent: "vault_indexer_v1", model: "text-embedding-3-large", tokensIn: <total_tokens>, tokensOut: 0, costUsd }` (embeddings have no output tokens — use 0, and cost = input × 0.13/M, which the route/embed already computes).
4. Remove the inline session insert. try/catch → budgetExceededResponse.

vault/from-description (Pitfall 5 — wrap BOTH calls as one guarded operation):
1. Import the guardrail helpers.
2. orgId from `[orgId]` param.
3. Wrap the ENTIRE "expand then embed" sequence inside a SINGLE `guardedLLMCall(orgId, async () => { const exp = await expandOrgDescription(...); const emb = await embedChunks(...); return meta })`. The budget is checked ONCE before expand fires; cost is recorded ONCE after embed completes. meta = `{ agent: "vault_indexer_v1", model: "gpt-4o+text-embedding-3-large" (or "gpt-4o" — pick the dominant; document choice), tokensIn: <expand.tokens_in + embed.total_tokens>, tokensOut: <expand.tokens_out>, costUsd: <expand cost + embed cost> }`. This matches the existing single-insert pattern.
4. Remove the existing combined inline session insert. try/catch → budgetExceededResponse.

Surgical: do not change chunking, vault artifact persistence, or response shapes.
  </action>
  <verify>Lint + typecheck green for both. vault/from-description has exactly ONE guardedLLMCall wrapping both model calls and ZERO inline session inserts. vault/upload likewise single-counts. Over-budget org → 402 (and the model is never invoked).</verify>
  <done>Both vault routes are budget-gated; from-description wraps expand+embed as one guarded op with summed cost; each records exactly one session row via the wrapper.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional org_id to naics-suggest + conditional guard (Open Question 2)</name>
  <files>app/api/orgs/naics-suggest/route.ts</files>
  <action>
Read the file. This route (naics_suggest_v1, gpt-4o, lib/rfp/naics/suggest.ts) is auth-gated but NOT org-scoped and currently does NO rfp_agent_sessions insert.

Resolve Open Question 2 with the agreed default — ADD optional org_id with graceful absence:
1. Add `org_id: z.string().uuid().optional()` to the request body schema (do not make it required — onboarding callers may not have an org yet).
2. Import `{ guardedLLMCall, budgetExceededResponse, BudgetExceededError }`.
3. If `body.org_id` IS present: verify the authenticated user is a member of that org (use the same membership-check pattern other RFP routes use — RLS-scoped read of rfp_user_orgs via createClient, or admin read + check). If the user is NOT a member, ignore the org_id (treat as absent) — do not leak which orgs exist; do not 403 a valid suggest call over an optional attribution field. If the user IS a member, wrap the suggest LLM call in `guardedLLMCall(body.org_id, async () => { ...; return { agent: "naics_suggest_v1", model: "gpt-4o", tokensIn, tokensOut, costUsd } })`. On BudgetExceededError return budgetExceededResponse(err).
4. If `body.org_id` is ABSENT (or membership not confirmed): call suggest WITHOUT the guard (skip the budget check) — acceptable for a ~$0.003 onboarding call per research. Add a brief code comment noting the gap. Do NOT insert a session row in the unguarded path (no org to attribute to).
5. When guarded, the wrapper records the session row — so do not add an inline insert.

Surgical: keep the existing suggestion logic and response shape unchanged; only add the optional attribution + conditional guard.
  </action>
  <verify>Lint + typecheck green. Body schema accepts requests with and without org_id. With a member's org_id over budget → 402, model never called. Without org_id → suggestion still returns 200 (guard skipped). No session insert in the unguarded path.</verify>
  <done>naics-suggest accepts optional org_id; guarded when a valid member org_id is supplied (records one session row, 402 when over budget); gracefully unguarded when absent.</done>
</task>

</tasks>

<verification>
- All five routes use guardedLLMCall (naics-suggest conditionally) + budgetExceededResponse.
- vault/from-description wraps expand+embed in ONE guarded call; one session row.
- Zero inline rfp_agent_sessions inserts remain across these files (no double-count).
- naics-suggest body schema has optional org_id; unguarded path returns 200 with no session row.
- Lint + typecheck green; no business-logic regressions.
</verification>

<success_criteria>
- All five user-facing call sites are budget-gated (naics conditionally); over-budget orgs get a 402 and the model never fires.
- Each guarded successful call records exactly one rfp_agent_sessions row.
- Open Question 2 (naics org_id) resolved per the agreed default.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ai-cost-guardrail/17-03-SUMMARY.md` noting: the model label chosen for vault/from-description, the naics-suggest membership-check approach, and confirmation all inline inserts were removed.
</output>
