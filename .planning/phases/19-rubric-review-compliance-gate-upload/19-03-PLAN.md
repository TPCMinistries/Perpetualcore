---
phase: 19-rubric-review-compliance-gate-upload
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260609_rfp_ai_disclosure_ack.sql
  - lib/rfp/compliance/generate.ts
  - lib/rfp/compliance/types.ts
  - app/api/rfp/proposals/[proposalId]/compliance/route.ts
  - app/api/rfp/proposals/[proposalId]/compliance-ack/route.ts
  - components/rfp/AiDisclosureBanner.tsx
  - app/(dashboard)/org/[orgId]/proposals/[proposalId]/page.tsx
  - app/ai-disclosure/page.tsx
autonomous: true
requirements: [REVIEW-04, REVIEW-05]
must_haves:
  truths:
    - "The compliance checklist explicitly checks page limits (numeric enforcement), required attachments, budget math markers, eligibility, and deadline+timezone — visible as pass/fail items before submitting"
    - "A null deadline timezone (when a deadline exists) is a FAIL blocker, not a warn"
    - "The checklist contains an ai-disclosure line item citing GSA GSAR 552.239-7001 / NIH that stays needs_review until the user explicitly acknowledges it — it can never auto-advance to met"
    - "Draft output in the proposal workspace carries a visible AI-use disclosure notice with an acknowledge action"
    - "Acknowledgment persists on the proposal (ai_disclosure_acknowledged) and flips the checklist item to met on the next compliance run"
  artifacts:
    - path: "supabase/migrations/20260609_rfp_ai_disclosure_ack.sql"
      provides: "additive ai_disclosure_acknowledged columns on rfp_proposals"
      contains: "ai_disclosure_acknowledged"
    - path: "app/api/rfp/proposals/[proposalId]/compliance-ack/route.ts"
      provides: "PATCH endpoint persisting the acknowledgment"
      exports: ["PATCH"]
    - path: "components/rfp/AiDisclosureBanner.tsx"
      provides: "AI-use disclosure notice + acknowledge action"
      min_lines: 30
  key_links:
    - from: "lib/rfp/compliance/generate.ts"
      to: "ai-disclosure checklist item"
      via: "buildPacketChecklist emits item id 'ai-disclosure' driven only by proposal.ai_disclosure_acknowledged"
      pattern: "ai-disclosure"
    - from: "components/rfp/AiDisclosureBanner.tsx"
      to: "/api/rfp/proposals/[proposalId]/compliance-ack"
      via: "PATCH on acknowledge click"
      pattern: "compliance-ack"
    - from: "app/api/rfp/proposals/[proposalId]/compliance/route.ts"
      to: "ai_disclosure_acknowledged"
      via: "selects the new column and passes it into CaptureReadinessInput"
      pattern: "ai_disclosure_acknowledged"
---

<objective>
Harden the deterministic compliance gate (page limits, deadline+timezone, budget math, attachments, eligibility — pass/fail before submit) and ship the AI-use disclosure: a persistent acknowledgment column, a PATCH ack endpoint, a checklist line item that can never auto-pass, and a visible disclosure banner on draft output.

Purpose: REVIEW-04 (compliance gate v1 hardening) and REVIEW-05 (AI-use disclosure per GSA GSAR 552.239-7001 / NIH). Fully independent of the rubric work — runs in Wave 1 parallel to 19-01.
Output: Applied additive migration + hardened compliance generator + ack endpoint + disclosure banner wired into the workspace.
</objective>

<execution_context>
@/Users/herald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rubric-review-compliance-gate-upload/19-RESEARCH.md

# Files to extend — read current shape before editing:
@lib/rfp/compliance/generate.ts
@lib/rfp/compliance/types.ts
@app/api/rfp/proposals/[proposalId]/compliance/route.ts
@lib/rfp/package/extract.ts
@components/rfp/SubmissionReadinessPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive migration — ai_disclosure_acknowledged on rfp_proposals</name>
  <files>supabase/migrations/20260609_rfp_ai_disclosure_ack.sql</files>
  <action>
Write the migration (additive only — /db-safe):

```sql
ALTER TABLE public.rfp_proposals
  ADD COLUMN IF NOT EXISTS ai_disclosure_acknowledged boolean NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS ai_disclosure_acknowledged_at timestamptz;
```

No new table → no new RLS needed; rfp_proposals' existing org-scoped policies cover the columns. Apply to LDC Brain AI (hgxxxmtfmvguotkowxbu) via `mcp__supabase__apply_migration` with name `rfp_ai_disclosure_ack`, showing the SQL in the log before applying. Do NOT regenerate database.types.ts (single-owner regen in 19-04) — use `as unknown as` casts where the columns are read/written.
  </action>
  <verify>mcp__supabase__execute_sql: `select ai_disclosure_acknowledged from rfp_proposals limit 1;` succeeds (column exists, default false).</verify>
  <done>Both columns live on rfp_proposals with safe defaults; existing rows unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Harden buildPacketChecklist — page limits, timezone fail, budget math, ai-disclosure item</name>
  <files>lib/rfp/compliance/generate.ts, lib/rfp/compliance/types.ts, app/api/rfp/proposals/[proposalId]/compliance/route.ts</files>
  <action>
**types.ts:** Add `ai_disclosure_acknowledged?: boolean` to nothing here — `ProposalInput` lives in generate.ts. No type changes needed in types.ts unless you add new exported helpers; keep it untouched if possible.

**generate.ts** — extend `ProposalInput` with `ai_disclosure_acknowledged?: boolean`, then add four hardened items inside `buildPacketChecklist` (the existing eligibility/attachment/budget coverage flows through the compliance matrix + `requirements-matrix` item — keep that, these are additive):

1. **Page limit enforcement** (per 19-RESEARCH.md Pattern 4.1): add `parsePageLimit(limits: string[]): number | null` using the regex `/(?:not exceed|maximum of?|limit[^\d]*)\s*(\d+)\s*page/i` over each extracted `page_limits` string (collect them from `input.packageExtractions`, same way matching_funds is collected). Estimate `current_pages = Math.ceil(totalWords / 250)` where totalWords sums `wordsFor(content)` across the canonical sections. Emit item `id: "page-limit"`:
   - no limit extracted → status `needs_review`, note "No page limit extracted; confirm against the solicitation."
   - limit found, current_pages <= limit → `met` with "≈X pages of Y allowed (250 words/page estimate)."
   - limit found, current_pages > limit → `missing` (BLOCKER) with the overage in the note.
2. **Deadline timezone hardening** (Pitfall 5): emit item `id: "deadline-timezone"`. When a due date exists (`input.proposal.due_date ?? input.opportunity?.deadline`) and `packageSummary.deadline_timezone` is null → status `missing` (fail blocker) with the "a missed timezone can cause a missed deadline" note from the research. When timezone present → `needs_review` with "Timezone: X. Confirm against the original posting." When no deadline at all → `needs_review`, "No deadline on file."
3. **Budget math check**: emit item `id: "budget-math"`. Count unresolved `[BUDGET` markers in the budget_narrative section specifically (reuse the marker-counting approach of countVerifyMarkers but scoped to budget markers): 0 markers AND a non-empty budget_narrative → `met`; markers present → `needs_review` with the count; empty budget narrative → covered by the existing section item, set `needs_review` with "Budget narrative missing — cannot verify budget math."
4. **AI disclosure** (Pitfall 4 — NO auto-status logic): emit item `id: "ai-disclosure"`, label "AI-use disclosure acknowledged", status = `input.proposal.ai_disclosure_acknowledged === true ? "met" : "needs_review"`, notes citing: "This proposal was drafted with AI assistance. Federal agencies (GSA GSAR 552.239-7001) and some funders (e.g. NIH) require or restrict AI-use disclosure. Review /ai-disclosure and the solicitation's AI policy, then acknowledge." The ONLY input that can make this `met` is the persisted boolean — never content scanning.

The existing `overall` derivation (any missing → fail, any needs_review/partial → warn) automatically makes timezone-null and page-overage hard FAIL blockers and keeps the gate at warn until AI disclosure is acknowledged. Do not change it.

**compliance/route.ts** (surgical): add `ai_disclosure_acknowledged` to the proposal SELECT (cast `as unknown as` if the generated types lack it) and pass it through on the `proposal` field of `CaptureReadinessInput`.
  </action>
  <verify>Grep confirms items `"page-limit"`, `"deadline-timezone"`, `"budget-math"`, `"ai-disclosure"` in generate.ts; `parsePageLimit` exists; ai-disclosure status derives only from `ai_disclosure_acknowledged` (no regex/content scan near it); route selects the new column. Existing unit-style behavior unchanged: no existing item ids renamed or removed.</verify>
  <done>Compliance gate explicitly checks page limits, attachments, budget math, eligibility, deadline+timezone; timezone-null and page-overage are fail blockers; ai-disclosure item exists and cannot auto-pass.</done>
</task>

<task type="auto">
  <name>Task 3: compliance-ack endpoint + AiDisclosureBanner + workspace and /ai-disclosure wiring</name>
  <files>app/api/rfp/proposals/[proposalId]/compliance-ack/route.ts, components/rfp/AiDisclosureBanner.tsx, app/(dashboard)/org/[orgId]/proposals/[proposalId]/page.tsx, app/ai-disclosure/page.tsx</files>
  <action>
**compliance-ack/route.ts** (new): `PATCH /api/rfp/proposals/[proposalId]/compliance-ack`. Follow the review route's auth pattern exactly: `createClient()` → `getUser()` → RLS-enforced proposal read to verify org membership; role gate via rfp_user_orgs with ALLOWED_ROLES = owner/writer/reviewer; then `createAdminClient()` to update `rfp_proposals` set `ai_disclosure_acknowledged = true, ai_disclosure_acknowledged_at = now()`. Body: `{ acknowledged: true }` validated with zod (only `true` accepted — un-acknowledging is not supported in v1; reject `false` with 400). Return `{ ok: true, acknowledged_at }`. Input validation + error handling per project rules. `export const runtime = "nodejs"; export const dynamic = "force-dynamic";`

**AiDisclosureBanner.tsx** (new, "use client"): props `{ proposalId: string; initialAcknowledged: boolean; acknowledgedAt: string | null }`. Renders a visible disclosure notice on draft output: "Drafted with AI assistance" headline, one-line GSAR 552.239-7001 / NIH mention, link to `/ai-disclosure` (on its own line per house style is for chat — in UI a normal Link is fine). If not acknowledged: an "Acknowledge AI-use disclosure" button that PATCHes the compliance-ack route, with loading/error states; on success switch to the acknowledged state ("Acknowledged on {date}" with a check). If acknowledged: render the confirmation state. Style: amber/info tone consistent with the zinc workspace palette.

**proposals/[proposalId]/page.tsx** (surgical): select the two new columns in the proposal fetch (`as unknown as` cast as needed) and render `<AiDisclosureBanner ...>` directly ABOVE the canonical sections block (`{/* Sections in canonical order */}` ~line 543) so every draft view carries the notice. Only render when at least one drafted section exists (no banner on empty proposals).

**app/ai-disclosure/page.tsx** (surgical, per research): add "Rubric extraction — AI extracts evaluation criteria from uploaded solicitation documents" to the existing list of AI uses. One list item, nothing else.
  </action>
  <verify>One `npx tsc --noEmit` (the single allowed typecheck for this plan) passes. Grep confirms: PATCH export in compliance-ack route with role gate + zod body validation; AiDisclosureBanner fetches `compliance-ack`; proposals page renders the banner above the sections loop; /ai-disclosure mentions rubric extraction.</verify>
  <done>User sees the AI-use disclosure on draft output, can acknowledge it (persisted), and the next compliance run flips the ai-disclosure checklist item to met.</done>
</task>

</tasks>

<verification>
- Migration applied to hgxxxmtfmvguotkowxbu (additive only, SQL shown).
- Checklist item ids stable and additive; SubmissionReadinessPanel/PursuitActionSummary render new items without changes (they iterate `items` generically — confirm by reading, not by modifying).
- Ack flow: PATCH → column update → next POST /compliance emits ai-disclosure: met.
- No local `next build`; deploy + end-to-end check happen in 19-04.
</verification>

<success_criteria>
REVIEW-04 and REVIEW-05 complete: the compliance gate surfaces explicit pass/fail items for page limits, attachments, budget math, eligibility, and deadline+timezone (timezone-null = fail); draft output carries an AI-use disclosure with an acknowledgment that gates the checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rubric-review-compliance-gate-upload/19-03-SUMMARY.md`
</output>
