---
phase: 19-rubric-review-compliance-gate-upload
plan: 04
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-03"]
files_modified:
  - components/rfp/PackageIntakePanel.tsx
  - lib/database.types.ts
  - scripts/VERIFY-phase19-seed.md
autonomous: false
requirements: [REVIEW-06]
must_haves:
  truths:
    - "A user can upload a 20-page PDF or DOCX solicitation on Vercel serverless and have it parsed without timeout or truncation"
    - "The package intake UI offers a solicitation mode that triggers rubric extraction and reports how many criteria were extracted"
    - "Uploading a solicitation makes Section L/M criteria (or funder priorities) with weights visible in the proposal workspace"
    - "All five roadmap success criteria are verified end-to-end on the deployed app by a human"
  artifacts:
    - path: "components/rfp/PackageIntakePanel.tsx"
      provides: "solicitation-mode toggle + extracted-criteria count in the result state"
      contains: "solicitation_mode"
    - path: "lib/database.types.ts"
      provides: "regenerated types incl. rfp_rubric_criteria + ai_disclosure_acknowledged"
      contains: "rfp_rubric_criteria"
  key_links:
    - from: "components/rfp/PackageIntakePanel.tsx"
      to: "/api/rfp/proposals/[proposalId]/package"
      via: "formData field solicitation_mode=true when toggle active"
      pattern: "solicitation_mode"
---

<objective>
Ship the user-facing solicitation upload flow (solicitation-mode toggle in the existing package intake panel), regenerate database.types.ts as the single owner for Phase 19's two migrations, deploy to production via `vercel --prod`, and run the human-verify checkpoint covering all five Phase 19 roadmap success criteria with deterministic seed/verify/rollback steps.

Purpose: REVIEW-06 (reliable PDF/DOCX solicitation upload on Vercel serverless) plus end-to-end verification of REVIEW-01..05 on prod.
Output: Solicitation upload UI live on rfp.perpetualcore.com; phase verified by human.
</objective>

<execution_context>
@/Users/herald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rubric-review-compliance-gate-upload/19-RESEARCH.md
@.planning/phases/19-rubric-review-compliance-gate-upload/19-01-SUMMARY.md
@.planning/phases/19-rubric-review-compliance-gate-upload/19-02-SUMMARY.md
@.planning/phases/19-rubric-review-compliance-gate-upload/19-03-SUMMARY.md

# Files to extend — read current shape before editing:
@components/rfp/PackageIntakePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Solicitation-mode toggle in PackageIntakePanel + types regen</name>
  <files>components/rfp/PackageIntakePanel.tsx, lib/database.types.ts</files>
  <action>
**PackageIntakePanel.tsx** (surgical — keep the existing upload/url/paste mode tabs, the post-import compliance + submission-tasks refresh chain, and all current styling):
1. Add a checkbox/toggle labeled "This is the solicitation — extract evaluation rubric" (default ON for the upload mode, since the panel's own empty-state copy already says "Add the solicitation PDF, DOCX, ..."; persists across mode switches). When active, append `form.set("solicitation_mode", "true")` to the existing formData build.
2. Consume the new `rubric_criteria` and `rubric_skipped_reason` keys from the package route response (shipped in 19-01). In the post-import result state, render one line: "N evaluation criteria extracted" (link/hint: "view them in the proposal workspace"), or when `rubric_skipped_reason` is set, a quiet note mapping: `no_opportunity_linked` → "No linked opportunity — rubric extraction skipped", `budget_exceeded` → "AI budget reached — rubric extraction skipped", `extraction_failed` → "Rubric extraction failed — re-upload with 'extract rubric' to retry".
3. Add a small "Re-extract rubric" affordance in the result state that re-submits with `force_re_extract=true` (reuses the same submit path).

**database.types.ts regen (single owner — established Phase 14-04/17 pattern):** regenerate via the supabase CLI against hgxxxmtfmvguotkowxbu (same command used in Phase 17; CLI is already authenticated per prior summaries). Confirm the diff is additive: `rfp_rubric_criteria` table types + `ai_disclosure_acknowledged`/`ai_disclosure_acknowledged_at` on rfp_proposals, with all Phase 14/17/18 types preserved (grep for `rfp_entitlements`, `match_vault_docs`, `rfp_fit_evidence` post-regen). Then remove the now-unnecessary `as unknown as` casts introduced by 19-01/19-02/19-03 ONLY where the regenerated types make them redundant and the change is mechanical — do not refactor anything else.
  </action>
  <verify>One `npx tsc --noEmit` (the single allowed typecheck for this plan, run AFTER the regen) passes. Grep confirms `solicitation_mode` and `force_re_extract` in PackageIntakePanel and `rfp_rubric_criteria` in lib/database.types.ts.</verify>
  <done>Solicitation mode is selectable in the intake UI with criteria-count feedback and a re-extract path; types are regenerated with all prior-phase types intact.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to production + write the deterministic seed/verify/rollback script</name>
  <files>scripts/VERIFY-phase19-seed.md</files>
  <action>
1. Commit all Phase 19 work (the executor's per-task commits should already exist; ensure the tree is clean).
2. Deploy: `vercel --prod` from this worktree (machine policy: Vercel build is the gate — no local `next build`). If the Vercel build fails, fix forward and redeploy; do not mark this task done until the deployment is live.
3. Confirm deployment: fetch `https://rfp.perpetualcore.com/ai-disclosure` (must mention rubric extraction) and confirm the deployment id via `vercel ls` or the Vercel MCP.
4. Generate a 20-page test solicitation: write a throwaway Node script (run with `node`, no new deps — use the already-installed `pdf-lib` if present, otherwise build a ~20-page DOCX-free plain approach: simplest reliable path is a 20-page PDF via `pdf-lib`; if `pdf-lib` is NOT in package.json, instead produce a 20-page DOCX using the installed `docx` package if present; if neither exists, create a long .txt master and print instructions to use any real federal RFP PDF the user has). The document MUST contain a realistic "SECTION M — EVALUATION FACTORS FOR AWARD" block with 4 weighted criteria (e.g. Technical Approach 40 points, Past Performance 25 points, Management Plan 20 points, Price 15 points), a "SECTION L" instructions block including "Proposals shall not exceed 15 pages", and a deadline line WITHOUT a timezone (to trigger the timezone fail blocker). Save the fixture to /tmp (not the repo).
5. Write `scripts/VERIFY-phase19-seed.md` (VERIFY- prefix per the Phase 14-04 live-DB safety pattern) documenting, with exact copy-paste SQL/commands:
   - SEED: which fixture file to upload and to which proposal (instruct using a dedicated test proposal in Lorenzo's canonical org — login lorenzo.d.chambers@gmail.com per memory).
   - VERIFY: SQL to confirm criteria rows (`select section_ref, max_points, weight, is_inferred from rfp_rubric_criteria where opp_id = '<opp>' order by section_ref;`), the ack column flip, and the reviewer findings row containing `criterion_id`.
   - ROLLBACK: SQL to delete the seeded `rfp_rubric_criteria` rows, the test package document row, the reviewer findings row, and reset `ai_disclosure_acknowledged = false` on the test proposal — scoped by the exact ids captured during verify (no broad deletes).
  </action>
  <verify>`vercel --prod` deployment is live (200 from rfp.perpetualcore.com). Fixture PDF/DOCX exists in /tmp with Section L/M content. scripts/VERIFY-phase19-seed.md exists with SEED/VERIFY/ROLLBACK sections and id-scoped rollback SQL.</verify>
  <done>Phase 19 code is live in production; a 20-page Section L/M test fixture and a deterministic seed/verify/rollback runbook exist for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all five Phase 19 success criteria on prod</name>
  <what-built>
Structured rubric extraction from uploaded solicitations (REVIEW-01), a rubric-anchored Anthropic-chain reviewer with criterion/severity/fix anchoring (REVIEW-02/03), a hardened compliance gate with page-limit/timezone/budget/ai-disclosure blockers (REVIEW-04/05), and a solicitation upload flow on Vercel serverless (REVIEW-06) — deployed to rfp.perpetualcore.com.
  </what-built>
  <how-to-verify>
Use the canonical login (lorenzo.d.chambers@gmail.com) on https://rfp.perpetualcore.com and the fixture + runbook from Task 2 (scripts/VERIFY-phase19-seed.md).

1. Open a test proposal linked to an opportunity in your org's pursuits view. In the Package Intake panel, keep "extract evaluation rubric" ON and upload the 20-page fixture from /tmp. CONFIRM: upload completes without timeout, and the result shows "4 evaluation criteria extracted" (REVIEW-06 + REVIEW-01 extraction).
2. Open the proposal workspace. CONFIRM: an "Evaluation Rubric" panel lists the 4 Section M criteria with their point values (Technical Approach 40 pts, etc.); any inferred weights carry an "inferred" badge (REVIEW-01 visible-in-workspace).
3. Click Review. CONFIRM: findings are anchored to specific criteria (criterion chips like "Section M.1") and draft sections, each with a severity label and a concrete suggested fix — not generic grammar feedback (REVIEW-02/03). Check the model on the session/cost line is a claude-* model.
4. Run Capture Readiness. CONFIRM the checklist shows: "Deadline timezone verified" as a FAIL/missing blocker (fixture has no timezone), a "Page limit" item comparing ≈pages vs the 15-page limit, a "Budget math" item, and "AI-use disclosure acknowledged" as needs_review (REVIEW-04).
5. CONFIRM: the draft output area shows the AI-use disclosure banner (GSAR 552.239-7001 / NIH). Click Acknowledge, re-run Capture Readiness, and CONFIRM the ai-disclosure item flips to met (REVIEW-05).
6. Re-upload the same fixture with rubric mode ON. CONFIRM: it reports the cached criteria (no duplicate rows — verify with the runbook SQL if in doubt).
7. ROLLBACK: run the id-scoped rollback SQL from scripts/VERIFY-phase19-seed.md to remove the seeded criteria, package doc, findings row, and reset the ack flag.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 19, or describe which step failed.</resume-signal>
  <action>Manual human verification only — Claude performs no automated work in this task; it presents the deployed app, the /tmp fixture, and scripts/VERIFY-phase19-seed.md so the user can confirm the five Phase 19 success criteria. Follow the how-to-verify steps above.</action>
  <verify>User confirms all seven how-to-verify checks pass. Steps 1-2 (20-page upload → 4 criteria visible), 3 (criterion-anchored findings), 4-5 (compliance blockers + ai-disclosure ack flip), and 7 (id-scoped rollback executed) are all mandatory — Phase 19 is not verified if any are skipped.</verify>
  <done>User types "approved" after confirming: 20-page solicitation parsed on prod without timeout (REVIEW-06), 4 weighted Section M criteria visible in the workspace (REVIEW-01), criterion-anchored findings with severity + fix on a claude-* model (REVIEW-02/03), timezone/page-limit/budget/ai-disclosure checklist behavior incl. the ack flip (REVIEW-04/05), cached re-upload with no duplicate rows, and the seeded data rolled back.</done>
</task>

</tasks>

<verification>
- All five ROADMAP Phase 19 success criteria confirmed by human on production.
- database.types.ts regen preserved all prior-phase types (single-owner pattern held).
- Seeded verification data fully rolled back (CORE DB hygiene).
</verification>

<success_criteria>
REVIEW-06 complete and Phase 19 verified end-to-end: 20-page solicitation uploads parse reliably on Vercel serverless, rubric criteria are extracted and visible, reviews are criteria-anchored, the compliance gate blocks on timezone/page/disclosure, and the human has approved on prod.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rubric-review-compliance-gate-upload/19-04-SUMMARY.md`
</output>
