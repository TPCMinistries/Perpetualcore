---
phase: 19-rubric-review-compliance-gate-upload
plan: 02
type: execute
wave: 2
depends_on: ["19-01", "19-03"]
files_modified:
  - lib/rfp/review/rubric.ts
  - lib/rfp/review/generate.ts
  - app/api/rfp/proposals/[proposalId]/review/route.ts
  - components/rfp/RubricCriteriaPanel.tsx
  - components/rfp/ReviewerFindingsPanel.tsx
  - app/(dashboard)/org/[orgId]/proposals/[proposalId]/page.tsx
autonomous: true
requirements: [REVIEW-02, REVIEW-03]
must_haves:
  truths:
    - "Triggering a review on an opp that has rubric criteria produces findings anchored to specific criteria (criterion_id) — not generic writing-quality feedback"
    - "Every reviewer finding carries a severity label, a section_type anchor, and a concrete suggested fix"
    - "The reviewer runs on the Anthropic-first model chain (sonnet → haiku → gpt-4o) instead of GPT-4o only"
    - "Extracted rubric criteria are visible in the proposal workspace with weights/points and inferred badges"
    - "Reviews on opps with no rubric criteria degrade gracefully to v1 generic-rubric behavior"
  artifacts:
    - path: "components/rfp/RubricCriteriaPanel.tsx"
      provides: "workspace panel listing section_ref + criterion_text + weight/points + is_inferred badge"
      min_lines: 40
    - path: "lib/rfp/review/rubric.ts"
      provides: "RubricCriterion type, rubric_criteria on ReviewerInput, criterion_id on ReviewerFinding"
      contains: "criterion_id"
    - path: "lib/rfp/review/generate.ts"
      provides: "Anthropic-first model chain reviewer"
      contains: "claude-sonnet-4-5"
  key_links:
    - from: "app/api/rfp/proposals/[proposalId]/review/route.ts"
      to: "rfp_rubric_criteria"
      via: "loads criteria for proposal.opp_id before calling generateReview"
      pattern: "rfp_rubric_criteria"
    - from: "lib/rfp/review/generate.ts"
      to: "lib/rfp/review/rubric.ts"
      via: "criteria-augmented system prompt when rubric_criteria present"
      pattern: "rubric_criteria"
    - from: "app/(dashboard)/org/[orgId]/proposals/[proposalId]/page.tsx"
      to: "RubricCriteriaPanel"
      via: "server component fetches criteria rows and renders panel"
      pattern: "RubricCriteriaPanel"
---

<objective>
Upgrade the Reviewer Agent from a generic GPT-4o pass to a rubric-anchored panel on the Anthropic-first model chain, and surface the extracted rubric criteria in the proposal workspace.

Purpose: REVIEW-02 (score against extracted criteria, not generic writing quality) and REVIEW-03 (findings anchored to draft sections with severity + suggested fix, now also anchored to criteria via criterion_id). Completes the REVIEW-01 success criterion's "visible in the proposal workspace" clause.
Output: Extended reviewer types/prompt, model-chain generate.ts, criteria-loading review route, RubricCriteriaPanel wired into the workspace.
</objective>

<execution_context>
@/Users/herald/.claude/get-shit-done/workflows/execute-plan.md
@/Users/herald/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-rubric-review-compliance-gate-upload/19-RESEARCH.md
@.planning/phases/19-rubric-review-compliance-gate-upload/19-01-SUMMARY.md

# Files to extend — read current shape before editing:
@lib/rfp/review/rubric.ts
@lib/rfp/review/generate.ts
@app/api/rfp/proposals/[proposalId]/review/route.ts
@lib/rfp/scoring/summary.ts
@components/rfp/ReviewerFindingsPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend reviewer types + prompt for rubric anchoring; upgrade generate.ts to the Anthropic chain</name>
  <files>lib/rfp/review/rubric.ts, lib/rfp/review/generate.ts</files>
  <action>
**rubric.ts** (backward-compatible extensions only):
1. Add exported `RubricCriterion` interface per 19-RESEARCH.md Pattern 1: `{ id: string; section_ref: string; criterion_text: string; max_points: number | null; weight: number | null }`.
2. Add `rubric_criteria?: RubricCriterion[]` to `ReviewerInput`.
3. Add `criterion_id?: string | null` to `ReviewerFinding` and `ReviewerFindingSchema` (`z.string().nullable().optional()` — do NOT require uuid format; the model echoes IDs and must not hard-fail Zod on a malformed echo. Drop findings whose criterion_id doesn't match a provided criterion at post-parse time instead — set it to null, keep the finding).
4. Add exported `buildRubricPromptAddendum(criteria: RubricCriterion[]): string` implementing Pattern 6: enumerate criteria as `[${c.id}] ${c.section_ref}: ${c.criterion_text} (${c.max_points ?? "?"} pts, weight ${c.weight ?? "?"})`, and instruct: "Score the draft against THESE criteria, not generic writing quality. For each criterion the draft fails to address, produce at least one finding with that criterion's id in criterion_id (category: alignment_with_rubric, severity at least high if the criterion is heavily weighted). Findings not tied to a specific criterion use criterion_id: null." Also document the new `criterion_id` field in the JSON shape block.
5. In `buildReviewerUserPrompt`, when `input.rubric_criteria?.length`, append a `# Solicitation evaluation rubric` block with the addendum.

**generate.ts** (model chain upgrade — Pitfall: keep the throw-on-malformed-JSON contract, it's what the route expects):
1. Replace `const MODEL = "gpt-4o"` + hardcoded prices with the chain from summary.ts: `MODEL_CHAIN = ["claude-sonnet-4-5", "claude-haiku-4-5", "gpt-4o"]`. Lazy `getAnthropic()`/`getOpenAI()` clients (copy the summary.ts lazy-init pattern); dispatch on `model.startsWith("gpt-")`.
2. Anthropic call: `messages.create({ model, max_tokens: 4096, system: REVIEWER_SYSTEM_PROMPT (+ rubric addendum context comes via user prompt), messages: [{ role: "user", content: userPrompt }] })`. OpenAI branch keeps the existing `response_format: { type: "json_object" }` call.
3. Per-model failure (API error, empty content, JSON/Zod parse failure) falls through to the next model in the chain; only after the FULL chain fails, throw (route turns it into a 502 — preserve this contract).
4. Compute cost via `computeCostUsd(model, tokens_in, tokens_out)` from `lib/rfp/ai/guardrail` instead of local price constants. `model` in the returned ReviewerResult must be the model that actually succeeded.
5. Post-parse: sanitize `criterion_id` values against `input.rubric_criteria` ids (unknown id → null). Keep excerpt truncation.
  </action>
  <verify>Grep confirms: MODEL_CHAIN with three ids in generate.ts, `computeCostUsd` imported, `criterion_id` in ReviewerFindingSchema, `buildRubricPromptAddendum` exported and used by buildReviewerUserPrompt. No remaining reference to PRICE_PER_M_INPUT.</verify>
  <done>Reviewer accepts optional rubric criteria, prompts for per-criterion findings with criterion_id, runs Anthropic-first with graceful chain fallback, and throws only on full-chain failure.</done>
</task>

<task type="auto">
  <name>Task 2: Load rubric criteria in the review route</name>
  <files>app/api/rfp/proposals/[proposalId]/review/route.ts</files>
  <action>
Surgical extension — do not change the auth, role-gate, delete-then-insert persistence, or guardedLLMCall structure:
1. After the proposal + opp are loaded and before `generateReview`, when `proposal.opp_id` is non-null, fetch criteria via createAdminClient: `select id, section_ref, criterion_text, max_points, weight from rfp_rubric_criteria where opp_id = proposal.opp_id order by created_at` (use `as unknown as` cast — types regen happens in 19-04). Map to `RubricCriterion[]`.
2. Pass `rubric_criteria: criteria.length > 0 ? criteria : undefined` into the `generateReview` input alongside the existing opportunity + sections. Zero criteria = v1 generic behavior (backward compatible).
3. Do NOT call rubric extraction here (Pitfall 6 — extraction happens only at package upload). If no criteria exist, the review proceeds generically.
4. Include `rubric_criteria_count: criteria.length` in the route's JSON response so the UI can distinguish rubric-anchored reviews.
  </action>
  <verify>Grep confirms the route selects from `rfp_rubric_criteria`, passes `rubric_criteria` into generateReview, and contains no import of extract.ts (no extraction on review trigger). guardedLLMCall block unchanged apart from the input.</verify>
  <done>Review runs are rubric-anchored whenever criteria exist for the opp and remain v1-generic when they don't; extraction is never re-triggered from the review path.</done>
</task>

<task type="auto">
  <name>Task 3: RubricCriteriaPanel + criterion labels in findings + workspace wiring</name>
  <files>components/rfp/RubricCriteriaPanel.tsx, components/rfp/ReviewerFindingsPanel.tsx, app/(dashboard)/org/[orgId]/proposals/[proposalId]/page.tsx</files>
  <action>
**RubricCriteriaPanel.tsx** (new, presentational, server-component-friendly — no fetching):
- Props: `{ criteria: Array<{ id: string; section_ref: string; criterion_text: string; max_points: number | null; weight: number | null; is_inferred: boolean }> }`.
- Render a titled panel ("Evaluation Rubric") listing each criterion: section_ref as a mono uppercase label, criterion_text as body, weight as a percentage (weight != null ? `${Math.round(weight*100)}%` : null) and/or max_points as `N pts`. When `is_inferred` is true, render an amber "inferred — verify against source" badge (Pitfall 2 UI guard). Match the existing zinc-palette / font-mono-label aesthetic used by SubmissionReadinessPanel and ReviewerFindingsPanel.
- Empty state: if criteria.length === 0 render nothing (return null) — the workspace only shows the panel when criteria exist.

**ReviewerFindingsPanel.tsx** (surgical): findings may now carry `criterion_id`. Accept an optional `criteria` prop (same shape, default []) and build an id→section_ref map; when a finding has a matching criterion_id, render a small chip with the criterion's section_ref next to the severity badge. Unknown/absent criterion_id renders exactly as today.

**proposals/[proposalId]/page.tsx** (surgical): in the server component's data-loading section, when the proposal has an opp_id, fetch `rfp_rubric_criteria` rows for that opp via the existing server supabase client (RLS allows the read; `as unknown as` cast). Render `<RubricCriteriaPanel criteria={criteria} />` directly above the `{reviewerResultGlobalOnly ? <ReviewerFindingsPanel .../> : null}` block (~line 540), and pass `criteria` into both ReviewerFindingsPanel usages (lines ~540 and the per-section findings flow only if trivially threadable — if SectionFindings would need prop drilling through multiple layers, label only in the top-level panel and note it in the summary). Keep all existing panels and ordering intact.
  </action>
  <verify>One `npx tsc --noEmit` (the single allowed typecheck for this plan) passes. Grep confirms: RubricCriteriaPanel imported + rendered in the proposals page, `is_inferred` badge logic present, ReviewerFindingsPanel maps criterion_id → section_ref chip.</verify>
  <done>Extracted criteria with weights and inferred badges are visible in the proposal workspace, and rubric-anchored findings display which criterion they address.</done>
</task>

</tasks>

<verification>
- Reviewer model chain matches summary.ts pattern; costs computed from RFP_MODEL_RATES via computeCostUsd.
- criterion_id flows: extraction (19-01) → rfp_rubric_criteria → review route → prompt → finding → UI chip.
- Graceful degradation verified by code paths: no criteria → v1 behavior; chain failure → 502 retry (unchanged contract).
- No local `next build`; Vercel build is the gate (deploy happens in 19-04).
</verification>

<success_criteria>
REVIEW-02 and REVIEW-03 complete: a review run on a criteria-bearing opp produces per-criterion findings (severity + section anchor + suggested fix + criterion_id) on the Anthropic-first chain, and the workspace shows the extracted rubric.
</success_criteria>

<output>
After completion, create `.planning/phases/19-rubric-review-compliance-gate-upload/19-02-SUMMARY.md`
</output>
