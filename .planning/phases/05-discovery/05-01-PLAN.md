---
phase: 05-discovery
plan: "01"
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - supabase/migrations/20260510_rfp_opportunities_extensions.sql
  - lib/rfp/sources.ts
  - lib/rfp/ingest/normalize.ts
  - lib/rfp/ingest/sam-gov.ts
  - lib/rfp/ingest/grants-gov.ts
  - lib/rfp/ingest/simpler-grants.ts
  - lib/rfp/ingest/sbir.ts
  - lib/rfp/ingest/run.ts
  - app/api/cron/rfp-discovery-federal/route.ts
  - vercel.json
  - .planning/phases/05-discovery/deferred-items.md
autonomous: true
requirements: ["DISC-01"]

must_haves:
  truths:
    - "Vercel Cron `/api/cron/rfp-discovery-federal` runs every 6 hours with bearer-secret auth"
    - "A single cron run fetches recent opportunities from SAM.gov, Grants.gov, Simpler.Grants.gov, and SBIR.gov in parallel and writes normalized rows to rfp_opportunities"
    - "Re-running the cron does not create duplicates — upsert keys on (source, source_id)"
    - "When an API key is missing or a source returns 401/5xx, the cron logs the failure but completes the other sources successfully"
    - "Each ingested row has source, source_id, title, agency, type, amount_min/max (when available), deadline, posted_at, and raw_json populated"
    - "SBIR.gov endpoint research is concluded — either a working endpoint is wired in, or a documented fallback (CSV download / scrape) is implemented, with the deferred note closed out"
  artifacts:
    - path: "supabase/migrations/20260510_rfp_opportunities_extensions.sql"
      provides: "Adds brief text, keywords text[], geo text columns to rfp_opportunities; adds source enum value if missing for any new source key"
      contains: "ALTER TABLE rfp_opportunities ADD COLUMN IF NOT EXISTS"
    - path: "lib/rfp/ingest/normalize.ts"
      provides: "Single normalizer that maps any per-source raw record into the rfp_opportunities row shape"
      exports: ["normalizeOpportunity", "OpportunityInput"]
    - path: "lib/rfp/ingest/sam-gov.ts"
      provides: "SAM.gov opportunity fetcher using lib/rfp/sources.ts registry; paginates, returns OpportunityInput[]"
      exports: ["fetchSamGovOpportunities"]
    - path: "lib/rfp/ingest/grants-gov.ts"
      provides: "Grants.gov fetcher; POST /search2; returns OpportunityInput[]"
      exports: ["fetchGrantsGovOpportunities"]
    - path: "lib/rfp/ingest/simpler-grants.ts"
      provides: "Simpler Grants fetcher; X-API-Key header; returns OpportunityInput[]"
      exports: ["fetchSimplerGrantsOpportunities"]
    - path: "lib/rfp/ingest/sbir.ts"
      provides: "SBIR fetcher — either correct endpoint or CSV-download/scrape fallback per Phase 4 deferred-items"
      exports: ["fetchSbirOpportunities"]
    - path: "lib/rfp/ingest/run.ts"
      provides: "Orchestrator: runs all configured federal fetchers, normalizes, upserts to rfp_opportunities via createAdminClient(), returns per-source counts"
      exports: ["runFederalIngest", "IngestRunResult"]
    - path: "app/api/cron/rfp-discovery-federal/route.ts"
      provides: "POST handler; bearer-secret auth; calls runFederalIngest(); JSON response with counts; nodejs runtime; force-dynamic"
      exports: ["POST"]
      contains: "Bearer ${process.env.CRON_SECRET}"
    - path: "vercel.json"
      provides: "Adds /api/cron/rfp-discovery-federal entry on `0 */6 * * *` schedule"
      contains: "rfp-discovery-federal"
  key_links:
    - from: "app/api/cron/rfp-discovery-federal/route.ts"
      to: "lib/rfp/ingest/run.ts"
      via: "runFederalIngest() call"
      pattern: "runFederalIngest"
    - from: "lib/rfp/ingest/run.ts"
      to: "rfp_opportunities"
      via: "createAdminClient().from('rfp_opportunities').upsert(... { onConflict: 'source,source_id' })"
      pattern: "rfp_opportunities.*upsert"
    - from: "lib/rfp/ingest/sam-gov.ts"
      to: "lib/rfp/sources.ts"
      via: "imports RFP_SOURCES.sam_gov"
      pattern: "RFP_SOURCES"
---

<objective>
Stand up the federal-source half of the Discovery cron. Every 6 hours, Vercel Cron hits a secured route that ingests opportunities from SAM.gov, Grants.gov, Simpler.Grants.gov, and SBIR.gov in parallel, normalizes them through one shared shape, and upserts them into `rfp_opportunities` idempotently. Resolve the SBIR endpoint gap from Phase 4.

Purpose: Closes DISC-01. Without this, no opportunities ever reach the feed.
Output: One scheduled cron, four per-source fetchers, one normalizer, one orchestrator, one schema extension migration, and an updated `vercel.json`.
</objective>

<execution_context>
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lorenzodaughtry-chambers/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-discovery/05-CONTEXT.md
@.planning/research/rfp-engine/TECH-SPEC.md
@.planning/phases/04-foundations-salvage-port/04-01-SUMMARY.md
@.planning/phases/04-foundations-salvage-port/04-02-SUMMARY.md
@.planning/phases/04-foundations-salvage-port/deferred-items.md
@CLAUDE.md
@supabase/migrations/20260509_rfp_schema.sql
@lib/rfp/sources.ts
@app/api/cron/heartbeat/route.ts
@vercel.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend rfp_opportunities schema for Discovery feed needs</name>
  <files>supabase/migrations/20260510_rfp_opportunities_extensions.sql</files>
  <action>
Create a NEW additive migration. Do NOT alter existing migration files (Phase 04-01 rule).

The current rfp_opportunities columns (per supabase/migrations/20260509_rfp_schema.sql) are:
id, source, source_id, title, agency, type, amount_min, amount_max, deadline, posted_at, raw_json, created_at — UNIQUE (source, source_id).

We are missing columns the feed UI (05-04) and scoring (05-03) will need. Add these via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`:

  - `brief text` — 1-2 sentence summary; populated by ingest where the source provides synopsis/description, otherwise null
  - `keywords text[] NOT NULL DEFAULT '{}'` — extracted keywords for keyword_alignment scoring (05-03)
  - `geo text` — geography string ("US", "NY", "NYC", "national", or country code) for geo_match scoring; nullable
  - `url text` — direct link to the source posting; nullable
  - `needs_review boolean NOT NULL DEFAULT false` — flips true when Quick Import normalization (05-05) couldn't fully fill required fields
  - `last_seen_at timestamptz` — set on every upsert; lets us detect when an opp was withdrawn (no longer appearing in source listings)

Apply the migration via supabase MCP `apply_migration` (project hgxxxmtfmvguotkowxbu, name `rfp_opportunities_extensions`). The /db-safe skill activates automatically — confirm dry-run via `list_tables` showing the new columns before declaring done.

The CHECK constraint on `source` already includes 'sam_gov', 'grants_gov', 'simpler_grants', 'sbir', 'ny_state', 'nyc_dycd', 'foundation_url'. Note: the registry in lib/rfp/sources.ts uses `sbir_gov` as the key but the schema enum uses `sbir`. Add a comment to lib/rfp/sources.ts noting this mapping, and ensure normalize.ts (Task 2) maps `sbir_gov` → `sbir` when writing to the table. Do NOT change the schema enum to add `sbir_gov` — the schema value is the canonical one for storage.

No new RLS policies needed — rfp_opportunities is already globally readable per Phase 04-01 policies. The new columns inherit the existing SELECT policy.

Commit immediately after `apply_migration` succeeds: `feat(05-01): extend rfp_opportunities with brief/keywords/geo/url/needs_review/last_seen_at`.
  </action>
  <verify>
1. Run supabase MCP `list_tables` for project `hgxxxmtfmvguotkowxbu`, schema `public`, table filter `rfp_opportunities`. Confirm all 6 new columns appear with correct types and defaults.
2. Run supabase MCP `execute_sql`: `INSERT INTO rfp_opportunities (source, source_id, title, brief, keywords, geo, url, needs_review) VALUES ('grants_gov', 'TEST-EXT-001', 'Test', 'A brief', ARRAY['workforce','youth'], 'NY', 'https://example.gov/x', false) RETURNING id;` — must succeed; then DELETE that row.
3. `git log -1` shows the new migration file committed.
  </verify>
  <done>The 6 new columns exist on rfp_opportunities in LDC Brain AI; a sample insert with all new columns succeeds; migration file is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Build per-source fetchers + normalizer + ingest orchestrator</name>
  <files>
lib/rfp/ingest/normalize.ts
lib/rfp/ingest/sam-gov.ts
lib/rfp/ingest/grants-gov.ts
lib/rfp/ingest/simpler-grants.ts
lib/rfp/ingest/sbir.ts
lib/rfp/ingest/run.ts
lib/rfp/sources.ts
.planning/phases/05-discovery/deferred-items.md
  </files>
  <action>
**2a. `lib/rfp/ingest/normalize.ts`** — define `OpportunityInput` interface (same shape as the rfp_opportunities row minus id/created_at) and a `normalizeOpportunity(input, sourceName)` function that:
  - Maps registry source key `sbir_gov` → schema enum value `sbir` (the schema CHECK uses `sbir`).
  - Coerces amounts to numbers, deadlines to ISO timestamps, keywords to lowercase trimmed dedupe array.
  - Sets `last_seen_at = new Date().toISOString()` always.
  - Returns the row shape ready for upsert.
  - Validates with a Zod schema; throws if title or source is missing. Catch-and-log at orchestrator level.

**2b. `lib/rfp/ingest/sam-gov.ts`** — exports `fetchSamGovOpportunities(opts: { sinceDays?: number, limit?: number }): Promise<OpportunityInput[]>`.
  - Use `RFP_SOURCES.sam_gov` BASE_URL pattern (read `env.SAM_GOV_BASE_URL` with the same fallback as in sources.ts).
  - Auth: `api_key` query param. Endpoint: `/search`. Window: `postedFrom = now - sinceDays`, `postedTo = now`. Paginate via `offset`/`limit` until empty page or 200 records hit (cap to avoid runaway).
  - When `SAM_GOV_API_KEY` is unset, return `[]` and log `[skip] sam_gov: key not set`. NEVER throw — the orchestrator continues with the other sources.
  - Map fields: `noticeId → source_id`, `title`, `department/subTier → agency`, `type`, `awardCeiling/awardFloor → amount_max/amount_min`, `responseDeadLine → deadline`, `postedDate → posted_at`, `description → brief` (truncate to 500 chars), `placeOfPerformance.country/state → geo`, `uiLink → url`, full payload → `raw_json`.
  - Extract keywords from `naicsCode[]` plus title bigrams (simple split; no NLP).

**2c. `lib/rfp/ingest/grants-gov.ts`** — exports `fetchGrantsGovOpportunities()`.
  - POST to `${BASE_URLS.GRANTS_GOV}/search2` with `{ rows: 100, oppStatuses: 'forecasted|posted' }`.
  - No auth required.
  - Map: `id → source_id`, `title`, `agencyName → agency`, `oppStatus → type`, `awardCeiling/Floor → amount_max/min`, `closeDate → deadline`, `openDate → posted_at`, `synopsis → brief`, full payload → `raw_json`. Geo: 'US' default for federal grants.
  - Extract keywords from `cfdaList` and title.

**2d. `lib/rfp/ingest/simpler-grants.ts`** — exports `fetchSimplerGrantsOpportunities()`.
  - POST to `${BASE_URLS.SIMPLER_GRANTS}/opportunities/search` with `X-API-Key` header. Body: `{ pagination: { page_size: 100, page_offset: 1, sort_order: [{ order_by: 'opportunity_id', sort_direction: 'descending' }] } }`.
  - When `SIMPLER_GRANTS_API_KEY` is unset, return `[]` and log skip. NEVER throw.
  - Map per the API response shape (use raw_json fully — fields tend to mirror Grants.gov but check the actual JSON shape on first run; if shape is unexpected, write what you can and set needs_review=true). Document any field-mapping ambiguities in inline comments.

**2e. `lib/rfp/ingest/sbir.ts`** — exports `fetchSbirOpportunities()`.
  - The TECH-SPEC endpoint is defunct (per Phase 04 deferred-items SBIR-ENDPOINT-UPDATE). Research the current option:
    1. Hit `https://www.sbir.gov/sites/default/files/Solicitations.csv` (commonly published bulk download path); if 200 with CSV body, parse with simple split (no new dep — split on commas inside quotes is fine for SBIR's well-formed export).
    2. If CSV path 404s, hit the `https://www.sbir.gov/api/solicitations` JSON endpoint at the new Drupal 10 path; check both `/api/solicitations` and `/api/topics`.
    3. If both fail, return `[]` and log `[skip] sbir: no working endpoint; tracked in deferred-items.md`. Update `.planning/phases/05-discovery/deferred-items.md` (NEW file in this phase) with the SBIR-ENDPOINT-UPDATE entry carried forward, summarizing which endpoints were attempted and what 200/404/etc. responses were observed.
  - Map: `solicitation_number → source_id`, `solicitation_title → title`, `agency`, `program → type`, `award_amount → amount_max`, `close_date → deadline`, `open_date → posted_at`, `description → brief`, `solicitation_url → url`, `geo='US'`. raw_json holds the full record.

**2f. `lib/rfp/ingest/run.ts`** — exports `runFederalIngest(): Promise<IngestRunResult>` and `IngestRunResult = { source: string, fetched: number, upserted: number, errors: string[] }[]`.
  - `Promise.allSettled([fetchSamGov(), fetchGrantsGov(), fetchSimplerGrants(), fetchSbir()])`. Each rejection logs but never breaks the run.
  - For each non-empty result, call `normalizeOpportunity()` per record, then upsert in a single batch via `createAdminClient().from('rfp_opportunities').upsert(rows, { onConflict: 'source,source_id', ignoreDuplicates: false })` — `ignoreDuplicates: false` ensures `last_seen_at` and any changed fields are updated.
  - Returns counts per source. Keeps the function pure — caller decides what to do with results.

**2g. `lib/rfp/sources.ts`** — add a comment block at the top noting the schema-enum `sbir` vs registry `sbir_gov` mapping. No code change.

Commit per file group:
  - `feat(05-01): add ingest normalizer + Zod opportunity input shape`
  - `feat(05-01): SAM.gov + Grants.gov + Simpler Grants fetchers`
  - `feat(05-01): SBIR.gov fetcher with CSV/JSON fallback chain`
  - `feat(05-01): runFederalIngest orchestrator with allSettled + idempotent upsert`
  </action>
  <verify>
1. TypeScript: `npm run build` passes (or `npx tsc --noEmit`). No `any` types introduced (use proper types or `unknown` + narrowing).
2. Unit-style smoke: write a one-off node script (not committed, just to verify) that calls `runFederalIngest()` against the dev DB and prints the IngestRunResult. Confirm at minimum Grants.gov returns >0 fetched and >0 upserted; SAM/Simpler may skip if keys unset; SBIR either fetches or skips with a documented reason.
3. Re-run the same script: upserted count goes to 0 (no duplicates), fetched stays the same, last_seen_at updates on existing rows.
4. Confirm `rfp_opportunities` rows have `brief`, `keywords`, `geo`, `url` populated for at least the Grants.gov set via `execute_sql`.
  </verify>
  <done>runFederalIngest() executes end-to-end, writes Grants.gov rows (and SAM/Simpler/SBIR when keys/endpoints permit), is idempotent on re-run, and never throws even when individual sources fail or skip.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Vercel Cron route + register schedule</name>
  <files>
app/api/cron/rfp-discovery-federal/route.ts
vercel.json
  </files>
  <action>
**3a. `app/api/cron/rfp-discovery-federal/route.ts`** — Next.js Route Handler. Pattern matches `app/api/cron/heartbeat/route.ts`:
  - `export const runtime = 'nodejs';`
  - `export const dynamic = 'force-dynamic';`
  - `export async function POST(request: NextRequest)`:
    1. Validate `Authorization: Bearer ${process.env.CRON_SECRET}`. If mismatch, return 401 `{ error: 'Unauthorized' }`.
    2. Call `runFederalIngest()`.
    3. Return 200 `{ ok: true, results, duration_ms }`. On thrown error from the orchestrator (should be rare since it uses allSettled), return 500 with a sanitized message — do NOT leak stack traces.
  - Add a `GET` handler that returns 405 with `Allow: POST` so accidental browser visits are explicit.

**3b. `vercel.json`** — append a new entry to the `crons` array:
```json
{
  "path": "/api/cron/rfp-discovery-federal",
  "schedule": "0 */6 * * *"
}
```
This fires at 00:00, 06:00, 12:00, 18:00 UTC. Confirm JSON validity (no trailing commas, sorted alphabetically isn't required but be consistent with the existing block).

Commit: `feat(05-01): cron route + vercel schedule for federal Discovery ingest`.
  </action>
  <verify>
1. Local: `npm run dev`, then `curl -X POST -H "Authorization: Bearer ${CRON_SECRET}" http://localhost:3000/api/cron/rfp-discovery-federal` returns 200 with a results array. Without the bearer, returns 401.
2. `git diff vercel.json` shows exactly one new cron entry, `0 */6 * * *`, path `/api/cron/rfp-discovery-federal`.
3. JSON parse: `node -e "JSON.parse(require('fs').readFileSync('vercel.json','utf8'))"` exits clean.
4. `npm run build` passes (the new route compiles).
  </verify>
  <done>Cron route is reachable, bearer-auth-protected, returns IngestRunResult JSON, and is registered on a 6h schedule in vercel.json.</done>
</task>

</tasks>

<verification>
End-to-end:
1. Apply migration to LDC Brain AI; new columns visible.
2. Local `curl POST /api/cron/rfp-discovery-federal` returns counts; rfp_opportunities has new rows.
3. Second curl: zero new inserts, last_seen_at updates.
4. supabase MCP `execute_sql`: `SELECT source, COUNT(*) FROM rfp_opportunities GROUP BY source;` shows at minimum grants_gov rows; sam_gov/simpler_grants present if keys provisioned.
5. `cat .planning/phases/05-discovery/deferred-items.md` shows SBIR-ENDPOINT-UPDATE status — either CLOSED with the endpoint that worked, or carried-forward with full notes from the attempts.
</verification>

<success_criteria>
- runFederalIngest() ingests from 4 federal sources without manual intervention; missing keys/endpoints SKIP, never crash
- Idempotent upserts on (source, source_id)
- Cron route reachable with CRON_SECRET, registered for 6h cadence
- New schema columns populated for grants_gov rows
- SBIR endpoint research conclusion documented (closed or carried-forward in deferred-items.md)
- Phase 4 deferred-items SBIR-ENDPOINT-UPDATE referenced and resolved (or explicitly carried with current state)
</success_criteria>

<output>
After completion, create `.planning/phases/05-discovery/05-01-SUMMARY.md` covering:
- Tables/columns added
- Per-source ingest counts from a test run
- Final SBIR endpoint resolution
- Any deferred items carried forward
</output>
